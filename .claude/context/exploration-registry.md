## §C. Exploration Registry — What's been mapped

When "Status" column says **Complete** or **Partial**, an agent already burned tokens discovering this. USE THE FINDINGS FILE. Do not re-explore.

| Surface | Status | Findings file(s) | First explored | Last updated |
|---|---|---|---|---|
| Local Office Settings → History table schema (42 cols, headers, pagination, booleans) | Complete | `plans/pending/SUBPLAN_HISTORY_01_MCP_FINDINGS.md` §2 | 2026-04-13 | 2026-04-15 |
| Location Management History table schema (87 cols, headers, booleans, duplicates) | Complete | `plans/pending/SUBPLAN_HISTORY_01_MCP_FINDINGS.md` §1 | 2026-04-13 | 2026-04-13 |
| Local Office Settings → ECT causality (ECT saves create NO history rows) | Complete | `plans/pending/SUBPLAN_HISTORY_01_MCP_FINDINGS.md` §3.5 | 2026-04-15 | 2026-04-15 |
| Location Management → Currency tab (NOT-TRACKED fields) | Partial (6 of 9 parents silently dropped) | `clients/encore/specs_planning/test-cases/setup/locations/locations_currency_test_cases.md` | 2026-04-01 | 2026-04-17 |
| Local Office Settings → Basic Information → Date offsets (validation, masks, fillAndTab pattern) | Complete | `clients/encore/tests/local-office/local-office-settings.spec.ts`; `local-office-settings.page.ts:215` (`fillAndTab`); `local-office-settings.ts` | pre-2026-03 | 2026-04-08 |
| Local Office Settings → ECT → BenefitsMultiplier / LaborCost / HistoricalSubrental | Complete | `local-office-ect.spec.ts`; `local-office-settings.page.ts` (ECT methods); LR-026 notes | pre-2026-03 | 2026-04-09 |
| Location Management → save dialog button variant (Cancel/Ok vs Cancel/Save) | Complete | MCP findings §1 §2; LR-012 | 2026-04-13 | 2026-04-13 |
| Location Settings → Pricing sub-tab — FCC pass (office 1604 USD-only = 5 primary dropdowns; office 1605 multi-currency = 15 = 5 USD+5 CAD+5 MXN). Date inputs are read-only (calendar-only, no typing); save = `POST pricebook/upsert-location-pricebook` + `PUT update-properties` (both 200); dialog confirm = "Ok". **Grid row names DRIFT — live-verify before use** (`2022-eCommerce`/`2022-NP LB1` gone 2026-06-19 → use `2021-Tier 3 Urban A`/`2022-Zone 5 A`/`2022-Zone 1 A`). On 1605 only **MXN Labor + MXN Equipment** dropdowns carry selectable strategies (all 5 CAD + 3 other MXN render but are empty "No pricing strategy found."). Corp-Pricing-uncheck saves 200 but reverts on reload = **BUG-LOC-PRI-001** (app-wide 1604+1605, TC-025 skipped). | Complete | `clients/encore/tests/locations/location-pricing.spec.ts` (37 TCs); `location-pricing.page.ts`; `field-inventories/pricing-2026-06-19.md`; `field-case-catalogs/pricing-2026-06-19.md`; `plans/done/SUBPLAN_PRICING_FCC.md` | 2026-06-19 | 2026-06-19 |
| Location Settings → Shared Setup Locations tab (30 TCs after SP-C added TC-025..030; PLAN_DQU_V6_PILOT_SHARED_SETUP v5.1 closure 2026-05-20 — 5 fixme'd TCs unlocked via per-TC alt-queries (Chicago/Boston/Dallas/Denver/Atlanta), TC-016 body rewritten + cancel-dialog assertions added, TC-026 + TC-030 remain fixme'd as app-bug evidence vehicles per BUG-LOC-SSL-001 + per-row Delete-after-reload bug) + Nav2 baseline + Phase 2 diff (6 divergences classified — 3 INTENTIONAL-UX-CHANGE [SHR-DIV-003/004/005], 2 PARITY-WITH-INTERACTION-OR-TRIGGER-DIVERGENCE [SHR-DIV-001/002], 1 CONFIRMED REGRESSION filed as BUG-LOC-SSL-001 [SHR-DIV-006]). **Nav2-first walk pattern** (LR-ENC-001): walk baseline FIRST (Section A.Index of walk-evidence), classify divergences against e2e, file BUG-DOC-NNN only when nav2 contradicts REQUIREMENTS.md (vs intentional UX change). | Complete | `clients/encore/tests/locations/location-shared-setup-locations.spec.ts`; `clients/encore/specs_planning/_internal/old-site-baseline/shared-setup-2026-05-12.md` (LR-045 baseline + Path C cross-field section); `clients/encore/specs_planning/_internal/walk-evidence-shared-setup-2026-05-15.md` (Sections A.Index + A walked-gaps + A.1 COVERED-SMOKE-PASS); `reports/bugs/BUG-LOC-SSL-001.json` (CONFIRMED-REGRESSION verificationLog 2026-05-19); `plans/done/PLAN_DQU_V6_PILOT_SHARED_SETUP.md` (v5.1 closure); `plans/done/SUBPLAN_DQU_V6_PILOT_SSL_A.md` / `..._B.md` / `..._C.md` / `..._D.md` / `..._E.md` (chunked execution) | 2026-04-07 | 2026-05-20 |
| Local Office Settings → Save Changes AlertDialog (`location-settings-modal-save-changes`) — behavior, selectors, helper | Complete | `clients/encore/src/pages/base.page.ts:350` (`clickSaveWithDialog`), `clients/encore/src/pages/local-office/local-office-settings.page.ts:138` (`clickSaveAndConfirm`), `clients/encore/tests/local-office/local-office-settings.spec.ts` (15+ usages), LR-012, ALL-076 | pre-2026-03 | 2026-04-21 |
| Chain orchestration (per-subplan background sessions, Stop-hook /final-q gating, daily/resume caps) | Complete | [PLAN_CHAIN_PER_SESSION_ORCHESTRATION.md](../../plans/done/PLAN_CHAIN_PER_SESSION_ORCHESTRATION.md) (authoritative design); `.claude/hooks/chain-orchestrator.sh`, `.claude/hooks/lib/chain-state.sh`, `.claude/hooks/lib/chain-guards.sh`; `.claude/skills/chain/SKILL.md` | 2026-04-23 | 2026-04-23 |
| Field-inventory artifact format (frozen contract — frontmatter + 7 mandatory sections + optional sections + grep rules for SP-AAE-02 hook) | Complete | [`field-inventory-spec.md`](../../clients/encore/specs_planning/_internal/field-inventory-spec.md) (spec) + [`field-inventories/_TEMPLATE.md`](../../clients/encore/specs_planning/_internal/field-inventories/_TEMPLATE.md) (skeleton); `PLAN_AGENT_AUTHORING_EFFICIENCY.md` AAE-D9 (access matrix); `agent-mistakes.md` ALL-077 (subplan identity assignment) | 2026-04-23 | 2026-04-23 |
| Encore old-site baseline (navigator2.training.psav.com) — access + Local Office + Location Management History surfaces + Oracle Bundle verdicts | Complete | [`OSB-ACCESS-VERIFY-2026-04-24.md`](../../clients/encore/specs_planning/_internal/old-site-baseline/OSB-ACCESS-VERIFY-2026-04-24.md) (full baseline artifact: §1 access GREEN, §2 selector-parity YELLOW zero testids, §3 tab roam + architectural divergence, §4 Oracle Bundle verdicts per LR-044 for BUG-LOC-MGH-002/002/LI-001/LOC-ECT-001, §5 LM History 87-col schema + Glyphicon boolean render); client CLAUDE.md LR-ENC-001; root CLAUDE.md LR-045; agent-mistakes.md ALL-024 amended + ALL-078 added | 2026-04-24 | 2026-04-24 |
| dependency-gate refactor: skip-cascade removed; per-test nav-guard rolled out across 12 specs; `dependencyGate(['TC-...'])` is annotation-only via `dependsOn` | Complete | [`plans/done/PLAN_DEPENDENCY_GATE_REMOVAL.md`](../../plans/done/PLAN_DEPENDENCY_GATE_REMOVAL.md) (4 framework files + 12 specs + 1 dup-helper cleanup; all 281 dep-gate calls preserved as annotations); pre-flight skip inventory: [`preflight-skip-inventory-2026-05-08.md`](../../clients/encore/specs_planning/_internal/preflight-skip-inventory-2026-05-08.md); dep-gate classification: [`dep-gate-inventory-2026-05-08.md`](../../clients/encore/specs_planning/_internal/dep-gate-inventory-2026-05-08.md); .claude/rules/specs.md annotation note | 2026-05-08 | 2026-05-08 |
| Location Settings → Notes sub-tab (Jira 1712 vertical pilot) — full architecture: 13 fields, single-parent FormArray, col 69 of LM History, `MM/DD/YYYY - <text> \| ...` encoding rule + nav2 baseline + 3 APP bugs (delete-persistence, dialog Ok-vs-Save, auto-empty-row) + Radix tablist activation quirk | Complete | [PLAN_PILOT_NOTES_DISCOVERY.md](../../plans/done/PLAN_PILOT_NOTES_DISCOVERY.md) parent; field-inventory [`notes-2026-05-11.md`](../../clients/encore/specs_planning/_internal/field-inventories/notes-2026-05-11.md); HIST catalog [`hist-root-map-location-management-notes.md`](../../clients/encore/specs_planning/catalogs/hist-root-map-location-management-notes.md); baseline [`notes-2026-05-11.md`](../../clients/encore/specs_planning/_internal/old-site-baseline/notes-2026-05-11.md); bugs `reports/bugs/BUG-LOC-NTS-{001,002,003}.json` | 2026-05-11 | 2026-05-11 |
| Location Settings 10-tab dialog-label inventory + BUG-MGH-001 R1 bisection — all 8 Location Settings sub-tabs use "Ok" confirm button (`/settings/location`); LO Basic Information uses "Save" (`/settings/local-office`); ECT Settings uses NO dialog (direct save). MGH Country populated on all 8 sub-tab saves — empty-Country rows correlate with CI-bot rapid-fire bursts, NOT with WHICH tab/endpoint (revised hypothesis supersedes original endpoint-correlated guess) | Complete | [`walk-evidence-location-settings-2026-05-14.md`](../../clients/encore/specs_planning/_internal/walk-evidence-location-settings-2026-05-14.md); `clients/encore/reports/bugs/BUG-MGH-001.json` (verificationLog appended, rcaDeferred flipped to false, revised hypothesis recorded); `reports/bugs/BUG-LOC-NTS-002.json` (verificationLog appended, deferredChecks resolved) | 2026-05-14 | 2026-05-14 |
| Location Settings → Legal sub-tab — full FCC coverage (15 existing TCs + 1 net-new TC-LOC-LGL-019 negative-enumeration); false-green sweep verdict GREEN; gap-analysis catalog 16-row LR-040 classification; **Path D live finding: ANY DOM mutation of Radix SC combobox (text-only OR with synthetic input/change events) tears down Angular page with `Application error: a client-side exception has occurred` — defensive characteristic, blocks naive textContent-tamper negative tests; pivot to negative listbox enumeration as the safe automatable mechanic**; pre-built page-object FCC hooks `saveAndConfirm()` + `ensureDefaultState()` for runner reuse | Complete | `clients/encore/tests/locations/location-legal.spec.ts` (16 tests + 3 OMITTED comment-tracked); `clients/encore/src/pages/locations/location-legal.page.ts` (saveAndConfirm + ensureDefaultState at lines 161-198); `clients/encore/src/data/locations/location-legal.ts` (LEGAL_INVALID_SC_VALUE); `clients/encore/specs_planning/_internal/false-green-sweeps/legal-2026-05-27.md`; `clients/encore/specs_planning/_internal/field-case-catalogs/legal-2026-05-27.md`; `plans/done/SUBPLAN_LEGAL_FCC.md`; `plans/done/PLAN_LEGAL_BASELINE_FIX.md` (2026-05-28: per-test `beforeEach` baseline + hardened `ensureDefaultState` whole-cycle retry+throw → Legal spec self-heals from a dirty/alt-value start; net-zero-on-stale-state isolation defect fixed, LR-019/LR-025; proven 17/17 ×2 from a deliberately-dirtied 1604 with `--retries=0`) | 2026-05-27 | 2026-05-28 |
| Location Settings → Account & Address sub-tab — full FCC field-coverage (26 existing TC-LOC-ACC-001..028 + 3 net-new 029/030/031: Phone2 clear-persist, Account# filter `AC000107`, Address-search clear-restores; blended-at-top, no `@fcc`). False-green sweep GREEN (0 FALSE-GREEN). Exhaustive (field×case) gap matrix 3 (a)/13 (b)/8 (c). **Live walk findings (2026-05-29, Playwright CLI):** all tab content renders inside the `<next-location-settings>` **shadow root** → CLI `eval` MUST deep-query-pierce (`document.querySelector` does NOT pierce; Playwright locators + `click`/`fill` commands DO). Account List dialog testid PRESENT (`location-settings-modal-account-list`); **Select-Customer-Address dialog testids ABSENT** (role+text fallback `[role="dialog"]:has-text("Select Customer Address")` — re-verified absent, NOT changed since 2026-04-29). Account List State=59 / Country=255 opts → LR-025. **State/Country dropdown filter returns MIXED-state results** (selecting CA → 25 rows incl. Mexican states) → dropdown-filter result-semantics deferred (c). Phone 1 account-linked → save-persist NOT-AUTOMATABLE (TC-021); address selection display-only non-persist (TC-027); tab-switch unsaved-dialog absent (TC-024). **CLI auth runbook:** `playwright-cli -s=e2e open <url>` lands on app `/navigator/auth/sign-in` until `playwright-cli -s=e2e state-load clients/encore/.auth/encore-state.json` then re-`goto`; nav2 baseline (`navigator2.training.psav.com/#/setup/locationdetail/1604`) SSO-bridges from the same e2e session (old "Account And Address" tab = billing-config, new picker/phone surface baseline-absent → baseline-partial). | Complete | `clients/encore/tests/locations/location-account-address.spec.ts`; `clients/encore/src/pages/locations/location-account-address.page.ts` (`saveAndConfirm`/`ensureDefaultState`/`searchAccountByNumber`); `clients/encore/specs_planning/_internal/field-inventories/account-address-2026-05-29.md` (+ -tab.png, -address-dialog.png); `_internal/old-site-baseline/account-address-2026-05-29.md`; `_internal/false-green-sweeps/account-address-2026-05-29.md`; `_internal/field-case-catalogs/account-address-2026-05-29.md`; `plans/done/SUBPLAN_ACCOUNT_ADDRESS_FCC.md` (Phase 4 RUN + Phase 6 closure PENDING separate session per AUD-017) | 2026-05-29 | 2026-05-29 |
| Location Settings → Left Panel (Basic Information) — 14 fields = **6 read-only** (Office, Local Office, **Line Of Business [read-only in EDIT mode by design — Encore NM-831/NM-1140]**, Pay To Address, eCommerce Active, Enable Productions Orders) + **8 editable** (Local Office Name, Active, Live Date, Tax Mode, Country, Region, Servicing Branch Office, Union). Country cascade (change clears Tax Mode + Region, disables Save at TaxModeID=0; cross-tab Job Costing USA-only + Remit PST Canada-only). **Tax Mode / Live Date / LOB have NO data-testid** → label-anchored `div:has(> label:text-is("X")) button[role="combobox"]` (Playwright CSS pierces shadow + supports :has/:text-is). Live Date VOLATILE on shared 1604 (assert format). Local Office Name **maxlength=255** (not 50). Servicing Branch **218** options. 26 TCs automated; TC-024 + Servicing/LiveDate-persist deferred (c). **Authoring gotchas: ALL-089** (fill→pressSequentially, setRadixCheckbox→check()/uncheck(), wait Save-disabled before reload, volatile-field format-assert). | Complete | `clients/encore/tests/locations/location-left-panel-basic-information.spec.ts`; `clients/encore/src/pages/locations/location-left-panel-basic-information.page.ts`; `_internal/field-inventories/left-panel-basic-information-2026-06-03.md`; `_internal/old-site-baseline/left-panel-basic-information-2026-06-03.md`; `_internal/field-case-catalogs/left-panel-basic-information-2026-06-03.md`; `plans/done/SUBPLAN_LEFT_PANEL_BASIC_INFORMATION_FCC.md` | 2026-06-03 | 2026-06-03 |
| XLSX deliverable cleanliness pipeline (encore_test_cases.xlsx) — generated, not hand-edited; reason column is multi-source (spec `test.fixme`/`// FIXME` reasons + committed `export_test_cases/blocked-reasons.json` seed; the gitignored `reports/fixme-registry.json` is GC-pruned on a 2nd `fixme:scan` and is NOT a durable reason home). Shared rule module `scripts/xlsx-lint-rules.mjs` (vocab deny-list + integrity C1 reason⇒not-Pass / C2 enum incl Manual / C4 Manual⇒blank) enforced at build (self-fail) + commit (pre-commit 5b) + ship (verify-no-forbidden). `test.fixme(true,…)` inside a body is invisible to `playwright --list` → assumed Pass; attribute reasons by enclosing `test('TC-…')` backward-walk, never by a TC ID in the reason text. UI-role nouns (combobox→dropdown, spinbutton→field) translated in `humanize.ts:scrubInternalVocab`. **2026-06-10 revert-recovery (PLAN_EXCEL_REVERT_RECOVERY): a silent restore of the gitignored MD rode into the committed workbook because parity was ID-set-only — recovered per-sheet from CD `45be5528` golden + spec titles; `check-tc-parity.ts` now carries guardrail-4 (FAIL on any TC ID on >1 sheet) and guardrail-5 (FLAG gross spec↔xlsx title divergence; FAIL on ≥50% divergent + numbering past spec max — the revert signature), both negative-tested vs the broken artifact. Parser gotcha: a trailing MD section whose text contains `Notes:` is swallowed into the LAST TC block's exported Notes cell (loose regex in to-csv.ts) — every TC block needs an explicit `**Notes**:` field; never title MD sections "<x> Notes:". TestRail workbook refresh = `scripts/_gen-testrail.ts` (full regen from deliverable; 19 sheets, 11-col).** **MERGED 2026-06-11 (PLAN_DELIVERABLE_MERGE_TESTRAIL_FORMAT): `_gen-testrail.ts` + the `encore_test_cases_testrail.xlsx` twin are RETIRED — the single `encore_test_cases.xlsx` now carries the TestRail step-expanded layout (13-col: `TC ID·Title·Module·Submodule·Test Data·Type·Priority·Coverage Status·Automation Status·Notes / Reason·Preconditions·Steps (Step)·Steps (Expected Result)`) AND real status columns, emitted directly by `to-xlsx.ts` via the shared `export_test_cases/testrail-format.ts` (parseSteps/perStepExpected/deriveTestData). First row of each case = cols 1-11; continuation step-rows = cols 12-13 only. `Notes / Reason` merges Notes + reason (`Blocked — ` marker is execution-gated + idempotent; lint C1/C3/C4/C5 re-derive the segment via `splitNotesReason`). `--with-run` now does a REAL `npx playwright test --reporter=json` run (actual Pass/Fail; default `--list-only` = assumed-pass). Regression-guarded: `npm run test:xlsx-merged-shape` (fails on old column set or a 2nd `*_testrail.xlsx`) + `test:xlsx-continuation-row` + `test:sp00-with-run`.** **DECLUTTERED 2026-06-11 (PLAN_DELIVERABLE_NOTES_REASON_DECLUTTER): `Notes / Reason` is now REASON-ONLY and the LAST column — `composeReason(reason, execution)` emits only the curated execution reason (tc.notes/MD Notes are NOT surfaced; they stay in source). Pass rows are empty by the integrity tripwire; the ~21 curated Pending/Manual reasons live in `blocked-reasons.json`. Permanent C9 anti-pollution gate in `lintWorkbook` (C9a populated-Pass / C9b `Cleanup after test:` breadcrumb / C9c non-curated blank-exec) fires at build+commit+ship; new test `npm run test:xlsx-compose-reason`.** | Complete | `plans/done/PLAN_XLSX_DELIVERABLE_CLEANUP.md`; `plans/done/PLAN_EXCEL_REVERT_RECOVERY.md`; LR-ENC-004 in `clients/encore/CLAUDE.md`; `scripts/xlsx-lint-rules.mjs`; `scripts/check-tc-parity.ts` (guardrails 4+5); `export_test_cases/{to-xlsx,sp00-augment-logic,humanize,blocked-reasons.json}`; `clients/encore/specs_planning/_internal/excel-revert-recovery-deviations-2026-06-10.md` | 2026-06-05 | 2026-06-10 |
| Corporate Pricing (net-new module, S0 foundation) — Search (`/settings/corporate-pricing`: 9 cols, 591 items virtualized, filters Pricebook/Strategy/Location/Currency/Is Internal/Is Labor/Active Only, action bar New[split→Equipment/Labor]/Pricing Override/Loc Pricing Export-Import/Export/Import/Grid Options) + Pricebook Details (`/details/<guid>`: 2 tabs **Pricing Strategy + Pricing Detail only — NO History/NM-1444**) + **NM-1440 New Pricebook BUILT** (`/add?type=equipment\|labor`, route-param present). Detail grid ID/Product Group Name/Price/New Price/Max Discount, ~3707 draggables/~4861 inputs virtualized (HIGH complexity, LR-053). **Near-zero data-testid** (shadow-pierced live audit: 3 generic e2e-* on Search, 0 on Details/Detail → text/role/grid-header/content-anchor selector strategy, Doctrine 4). Unicode ✔ booleans (LR-036). **baseline-absent** (purely e2e, NO nav2 equivalent — user-confirmed; do NOT search nav2). 2 mutation fixtures: detailFixture=2021-PB6 (Inactive, S3) / strategyFixture=2022-NP Tier 1 (Active, S2). Grid "Active Only"/"Is Active" = strategy flag, NOT pricebook-record status. **S2/1441 Strategy live findings (2026-06-05): Save button TEXT FLIPS `Save`→`"Saving..."`→`Save` during commit — a `text-is("Save")` disable-wait mismatches mid-save; settle on the `"Pricebook saved successfully"` toast instead (cross-screen: S3 Detail Save likely same). Add-New is DIALOG-gated ("New Pricing Strategy" modal). A SAVED new strategy becomes legacy and loses its Remove control (not UI-reversible → use reversible existing-row edits for save-cycle restore). Strategy editor checkboxes: Is Internal/Is GSO disabled when Is Productions checked.** | Complete (S0 foundation; **Wave-1 P1 ALL DONE 2026-06-05** — S1/1445 Search 18 `TC-CPR-SRC-001..018` green ×2, S2/1441 Strategy 25 `TC-CPR-STR-000..025` green, S3/1443 Detail 20 `TC-CPR-DET-000..020` green; **S4 closure DONE 2026-06-05** — DO-OR-DIE multi-agent audit (8 dims B1-B8, fresh-context ≥3 adversarial skeptics/PASS) → VERIFIED-CLEAN after one remediation round (10 defects fixed at MD-source/tooling/page-object/draft layers + re-audited fresh-context); scoped suite **64 passed** 0-fail/flaky/skip; GARDENER dedup = 5 shared primitives in `CorporatePricingBasePage`; master P1-milestone-annotated, parent stays **PENDING** per F16). **S1/Search live findings: module is React/Next.js (`waitForAngularStable` no-op; grid loads async — "0 items found" footer paints BEFORE data → readiness must wait for `tbody tr`/`th`, not the footer); D2 — filtering is SERVER-SIDE on the Search button (typing/toggling only STAGES, no call; Reset restores from cache no call), NOT client-side per DOCX (raised /encore-questions); D1 — DOCX 8 cols → 9 live (raised); React text inputs need the native value-setter (NOT `pressSequentially`/`fill`); Playwright `:text-is` misses action-bar labels → shadow-walk `textContent`; `isVisible({timeout})` ignores its timeout → use `waitFor`.** | `clients/encore/specs_planning/_internal/walk-evidence-corporate-pricing-2026-06-05.md`; `clients/encore/specs_planning/_internal/old-site-baseline/corporate-pricing-2026-06-05.md`; `plans/done/SUBPLAN_CORP_PRICING_00_FOUNDATION.md`; `plans/pending/PLAN_CORP_PRICING_MASTER.md`; `clients/encore/src/{selectors,pages,data}/corporate-pricing/`; `plans/done/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md`; `clients/encore/specs_planning/_internal/corp-pricing-closure-audit-2026-06-05.md`; `clients/encore/specs_planning/_internal/corp-pricing-audit-demands-2026-06-05.md`; `clients/encore/specs_planning/_internal/corporate-pricing-missing-testids-report.md` | 2026-06-05 | 2026-06-05 |
| Corporate Pricing → Pricebook Details → **Pricing Detail tab (NM-1443, S3 P1, Management mode)** — shadcn HTML `<table>` (ID/Product Group Name/Price/New Price/Max Discount), ~2430 rows + ~3707 draggable source items, **fully rendered (NOT windowed)** → content-anchored reads by Product Group Name (anchors "Balloon Light Decor" 277/615.00, "Analog Mixer 12 - 23 Ch" 280/195.00; assert by content, never count — LR-022/LR-053). **Override model**: New Price = staging override → on Save becomes the row's Price (input clears); Base Price (Price col) read-only (no input) yet updates to the saved override (CPR-DETAIL-Q4 divergence). **Dirty lever asymmetry**: editing **Max Discount reliably enables Save; a New-Price-only edit does NOT reliably enable Save** (CPR-DETAIL-BUG-A candidate) though its value commits with the batch — so save-cycle tests use Max Discount as the dirty lever (New-Price persist co-dirties via Max Discount). Save **dialog-gated** ("Save Changes" alertdialog) + **commits ALL dirty rows in one batch** (endpoint `POST /navigator/api/location/pricing/save`; reload+re-read for persistence, never a page-URL listener — LR-056). Grid renders **FLAT — no expand/collapse in mgmt mode** (DOCX hierarchy absent; page chevrons are app-nav → CPR-DETAIL-Q3). Double-click + drag do NOT add (mgmt defensive, verified). **Heavy page → per-test timeout 150s** (30s default times out on ~20s page-open + bounded-retry restore stack). Confirms S2's Save-text-flip note cross-screen — `saveAndConfirm` disable-wait is best-effort `.catch()` + reload is load-bearing (suite green regardless). | Complete (S3 P1 — 20 TC-CPR-DET-001..220 green ×full-suite; New-Pricebook add → 1440, numeric FCC → 1443_DETAIL_FCC_P2) | `clients/encore/tests/corporate-pricing/corporate-pricing-detail.spec.ts`; `clients/encore/src/pages/corporate-pricing/corporate-pricing-detail.page.ts`; `clients/encore/src/selectors/corporate-pricing/pricing-detail.ts`; `clients/encore/src/data/corporate-pricing/detail.ts`; `clients/encore/specs_planning/_internal/field-inventories/corporate-pricing-detail-2026-06-05.md` | 2026-06-05 | 2026-06-05 |
| Corporate Pricing → **Product Group Override** (`/settings/corporate-pricing/pg-override`, Wave-1.5 live-discovered) + **Search toolbar I/O** (Export ▾/Import ▾ 4-variants, Loc Pricing Export/Import, Grid Options) | Complete (recon + scaffold; **Override FCC DONE — W15-A 2026-06-09, 28 `TC-CPR-OVR-001..528` green ×2**; toolbar I/O FCC DONE — W15-B 2026-06-09, 17 `TC-CPR-TIO-001..617` green ×2) — Override screen **CONFIRMED BUILT 2026-06-08** (recon-stale "destination unbuilt" cleared). **Equipment\|Labor** `role=tab` (aria-selected; tab-switch reloads grid). Grid is **location-gated** (empty "No results." until "Select a location" card → modal picker: search + per-row checkbox + Select/Cancel/Close). **10 columns** (Location, Product Group, Product Group Name, Currency, Current Price, Override Price, Max Discount %, Active, Mod Date, **Updated By**) — recon (D9) said 9, live adds Updated By (Q-WV15-1). **`Active` = Radix checkbox `[role=checkbox][aria-checked]` — LR-036 NEW (4th) boolean render** (NOT Unicode ✔/SVG lucide-check/Glyphicon; read aria-checked). Currency filter ALL/USD/CAD/MXN; Active-only default OFF; "Filter Product Groups Override..." = client-side live filter; rows-per-page 10/20/30/40/50. **Override Price/Max Discount % = `div[role=button]` click-to-edit cells — EDIT MECHANISM RESOLVED W15-A 2026-06-09 (recon's "inert cells / RBAC" was a FALSE NEGATIVE — a proper Playwright `click` reveals an active `spinbutton`; the recon likely used a raw-JS `.click()` that doesn't fire React onClick). Commit = native value-setter (React-controlled; `.fill()` no-ops) + `Enter`; Active checkbox toggles + dirties; Save → "Save Changes" dialog matched via CSS `[role="alertdialog"]` + TEXT buttons (Playwright `getByRole('alertdialog')` MISSES this shadow/portal-nested dialog) → `POST corporate-price-pg-override` → toast. Grid `<th>` nests a "Resize column" button → use `:has-text` not `:text-is` for header-anchored table selectors. Max Discount % CAPPED AT 100 (>100 rejected, editor won't commit). LR-009 net-zero + reversibility verified.** Override `Export`/`Loc Pricing Export` = **direct CSV downloads** (`ProductGroupOverrides-*.csv` / `LocationPricebooks-*.csv`); Search `Export ▾`/`Import ▾` = same 4 variants (All Equipment Pricing / All Labor Pricing / All Equipment Max Discount / All Labor Max Discount). ZERO data-testids. **W15-B trigger-level FCC (2026-06-09):** Export variants fire `GET /navigator/api/location/pricing/pricing-export?isLabor={t/f}&isMaxDiscount={t/f}&locale=en-US` (params map 1:1 to variant; CSV download) — answers Q-WV15-2/NM-1604; Loc Pricing Export = `location-export?locale=en-US`. **Import variants open a CUSTOM in-app dialog** "Import &lt;variant&gt;" (Browse/Cancel/Upload/Close + `input[type=file]`), NOT a native chooser, NO network on trigger (real upload→EDGE_P3). **Grid Options = `button[aria-label="Grid Options"]`** (icon, sr-only label — `:text-is` MISSES it, LR-029 correction) → menu of 9 `menuitemcheckbox` (columns, all checked); toggle hides `<th>`, **persists across reload** (server-pref), restore by toggling back. Real download/upload round-trip deferred to EDGE_P3 (LR-040(b)). **NM-2267 net-new (2026-07-09):** +9 TCs `OVR-029..037` on office **1606** (location-picker modal detail, Grid Options visibility, override Export/Import dialog, NM-1463 auto-activate, sort-inactive mirror of DET-049, NM-2206 current-price guard, Max-Discount ≤100) green ×2; `OVR-023` kept fixme (the ">100 rejected" recovery contradiction is unresolved on 1606 — over-cap sets aria-invalid+red but recovery stays broken); pagination `out-of-scope` (1606 grid = 7 rows < the 10-row page size; options already OVR-011); currency-gated add-override picker parked (office 1101 + 1604 import-defect gated; not tracked as a subplan). **Fixed a pre-existing shared `selectLocation` Radix-modal open-race flake** (search input mounts before editable → bounded re-open retry; stabilizes the whole spec's per-test `beforeEach`, not just the net-new TCs). | `clients/encore/specs_planning/_internal/field-inventories/corporate-pricing-override-2026-06-08.md` + `...corporate-pricing-toolbar-io-2026-06-08.md`; `_internal/old-site-baseline/corporate-pricing-override-2026-06-08.md`; `_internal/encore-questions-drafts/corporate-pricing-wave15-divergences-2026-06-08.md`; `clients/encore/src/pages/corporate-override/corporate-override.page.ts` + `src/selectors/corporate-override/override.ts` + `src/data/corporate-override/override.ts`; `plans/done/SUBPLAN_CORP_PRICING_W15_0_RECON.md`; **W15-A FCC (2026-06-09)**: `field-inventories/corporate-pricing-override-2026-06-09.md`, `field-case-catalogs/override-2026-06-09.md`, `clients/encore/tests/corporate-override/ (core + nm2268..nm2273)`, `clients/encore/specs_planning/test-cases/setup/corporate-pricing/corporate_pricing_override_test_cases.md`, `plans/done/SUBPLAN_CORP_PRICING_W15_A_OVERRIDE_FCC.md`; **W15-B toolbar I/O FCC (2026-06-09)**: `clients/encore/tests/corporate-pricing/corporate-pricing-toolbar-io.spec.ts`, `clients/encore/src/data/corporate-pricing/toolbar-io.ts`, toolbar helpers in `corporate-pricing-search.page.ts` + selectors in `selectors/corporate-pricing/search.ts`, `specs_planning/test-cases/setup/corporate-pricing/corporate_pricing_toolbar_io_test_cases.md` + test-plan, `plans/done/SUBPLAN_CORP_PRICING_W15_B_TOOLBAR_IO_FCC.md`; **NM-2267 (2026-07-09)**: `plans/done/SUBPLAN_CORP_PRICING_NM2267_OVERRIDE.md` | 2026-06-08 | 2026-07-09 |
| Corporate Pricing → **New Pricebook create flow (NM-1440, Wave-1.5 priority)** — `/add?type=equipment\|labor` | Complete (30 `TC-CPR-NPB-001..330` green ×2: individual 31-pass + all 30 green within the 94-test full CPR suite). **Live findings 2026-06-09:** form is React/Next **light DOM** (only `next-route-announcer` shadow host — plain CSS resolves all fields, unlike the shadow-rooted Detail/Account tabs); React-controlled inputs need the **native value-setter** (`.fill()` does NOT commit React state → Save stays disabled) — extracted to `CorporatePricingBasePage.setReactInput`. **Save gate = Name + Year + ≥1 strategy** (product groups optional = Empty-Shell). **Type combobox is DISABLED** (route-param-fixed; Equipment/Labor chosen at the `+New ▾` toolbar). Currency USD/CAD/MXN. Strategy add = dialog (`#new-strategy-name` + 4 flags, **no Type field**; empty-name Add = no-op). **Double-click adds a product group** in create mode (S3's deferred New-Pricebook-mode ADD — works here, defensive in mgmt mode); catalog is type-specific (Eq ≈3707 / Labor ≈547). **Save = `POST /navigator/api/location/pricing/save` (LR-056: ignore the `…/add?type=` RSC framework POSTs) → dialog-gated "Save Changes" → redirect to `/details/<new-guid>`. NO UI delete/deactivate anywhere → create is IRREVERSIBLE → spec is NO-COMMIT** (asserts Save reachability + Cancels). 5 divergences raised (CPR-1440-Q1..Q5). **NM-2263 extension (2026-06-30): +19 TCs (TC-CPR-NPB-032..050), 50 green ×2** — create-mode drag-add positive control (real `mouse.move→down→move→up`, never `.dragTo()`); `New ▾` Equipment/Labor menu-item clicks (not URL); update-existing mgmt-mode entry pair (deep edit/save/persist cited to TC-CPR-DET-*/STR-*, never duplicated); **NM-2022** = pricebook name has NO client-side uniqueness validation (strategy names DO); **NM-2057 NOT-REPRODUCED** = empty Price Year shows a visible RED border (`oklch(0.577 0.245 27.325)`) + `aria-invalid=true` (a real indicator, not silent); Axis-2 surface-behavior band render-state/empty-vol/persistence/result-fidelity covered (result-fidelity promoted — the source list HAS a "Search ID or Name…" box), pagination/sorting/combination out-of-scope. Empty-grid empty-state renders as a `tbody tr` (so `getDetailGridRows` returns 1 not 0 on an empty grid). **Labor Save coverage (2026-06-30, LR-066): +2 TCs (TC-CPR-NPB-051/052), 52 green ×2.** The Labor route previously had only a Save-*enable* check; 051 = Labor Save→dialog→Cancel (mirror of 024), 052 = Labor commit→persist→Search (mirror of 031, `laborGroupA='Banners Design'`). **052 surfaced a real divergence: the Corporate Pricing Search HIDES Labor pricebooks unless the `Is Labor` filter checkbox is on** (Equipment shows by default) — `setCheckbox('isLabor', true)` is required to find a committed Labor book. Also hardened the SHARED `confirmSaveAndGetNewId` commit (bounded retry of page-Save→confirm + `waitForURL({waitUntil:'commit'})` instead of the heavy `/details` `'load'`) — the single-confirm flaked first-attempt; Equipment TC-031 verified unaffected. | `clients/encore/tests/corporate-pricing/corporate-pricing-new-pricebook.spec.ts`; `clients/encore/src/pages/corporate-pricing/corporate-pricing-new-pricebook.page.ts`; `clients/encore/src/selectors/corporate-pricing/new-pricebook.ts` (`np`-prefixed); `clients/encore/src/data/corporate-pricing/new-pricebook.ts`; `clients/encore/specs_planning/_internal/field-inventories/corporate-pricing-new-pricebook-2026-06-09.md`; `_internal/encore-questions-drafts/corporate-pricing-newpricebook-divergences-2026-06-09.md`; test-cases + test-plan under `specs_planning/test-{cases,plans}/setup/corporate-pricing/`; `plans/done/SUBPLAN_CORP_PRICING_1440_NEW_PRICEBOOK.md`; `plans/done/SUBPLAN_CORP_PRICING_NM2263_NEW_PRICEBOOKS.md` (NM-2263 extension) | 2026-06-09 | 2026-06-30 |
| Corporate Pricing → Search **FCC P2** (NM-1445, Wave-2) + **⚠ toolbar I/O live app-change** | Complete (Search FCC; toolbar flagged) | `clients/encore/tests/corporate-pricing/corporate-pricing-search.spec.ts` (FCC describe `TC-CPR-SRC-019..030` at top, P1 below); `corporate-pricing-search.page.ts` (`probePricebookBoundary` = §2.1 announced+escapable oracle, never auto-Escapes; `selectFirstRealLocation` LR-025 retry); `_internal/field-inventories/corporate-pricing-search-2026-06-10.md` + `_internal/field-case-catalogs/corporate-pricing-search-fcc-2026-06-10.md`; `plans/done/SUBPLAN_CORP_PRICING_1445_SEARCH_FCC_P2.md`. **Full Search query-param contract (live-verified):** `pricebookName` / `pricingStrategyName` (NOT `strategyName` — P1 guessed wrong) / `currencyId` (USD=1,CAD=2,MXN=3) / `locationNo` (office #) / `isInternal` / `isLabor` / `isActive` (param OMITTED when Active-Only unchecked, not `=false`). Pricebook input has NO maxlength (250-char ok→0); special chars accepted literally + URL-encoded + crash-safe + Tab-escapable (plain input ≠ Radix combobox/ALL-088); whitespace-only → full list (ignored server-side); Is Labor = a different/larger population, not a narrowing; Reset is client-side + idempotent (double-reset = 0 calls); compound = single query with all staged params. **⚠ Toolbar I/O (W15-B) tests are STALE**: the live app changed 2026-06-10 — Import ▾ now opens a **Year+Currency** dialog (was file-chooser "Choose a file to import data"); Export ▾ variants no longer fire a direct `pricing-export` request. 10 tests (`TC-CPR-TIO-002/603/604/605/608/609/610/611/612/613`) red until re-walked → remediated in `plans/done/SUBPLAN_CORP_PRICING_TOOLBAR_REMEDIATION.md`. (Detail `TC-216` save-dirty flake passes in isolation, `CPR-DETAIL-BUG-A` area.) | 2026-06-10 | 2026-06-10 |
| **ID/naming grammar (TC + BUG) — repo-wide audit + re-grammar + registry + guards** | Complete (PLAN_ID_NAMING_AUDIT_AND_REMEDIATION 2026-06-11) — grammar is `TC-{MOD}-{SUB}-{NNN[A]}` / `BUG-{MOD}-{SUB}-{NNN}`, codes registered in **`export_test_cases/module-codes.json`** (single source of truth — mint codes THERE first; `types.ts` KNOWN_SUB_CODES is parity-asserted against it). Corp Pricing re-IDed `TC-LOC-CPR-NNN`(bands) → `TC-CPR-{SRC,STR,DET,NPB,OVR,TIO}-NNN` 001-based; LI normalized (NE→078..114, SKIP-BILLING→070; letter-suffix = registered insertion mechanism). Old↔new map: `_internal/id-audit-2026-06-10/id-rename-map.csv`; dated pre-rename artifacts carry an ID-RENAME banner, plans/done untouched (historical). Guards: parity guardrails 6 (ID↔dir/file/sheet + workbook cells + blocked-reasons/allowlist liveness + gap ledger) + 7 (BUG grammar) in `check-tc-parity.ts`; lint **C8** (+`loadModuleRegistry`, `HEADER_ALIASES` closing the TestRail-workbook lint false-green) in `xlsx-lint-rules.mjs`; pre-commit gate 5 also fires on xlsx/module-codes/blocked-reasons. Negative-tested: C8 vs pre-fix workbook = 162 hits; guardrail 6g caught a live off-by-one during execution. | `clients/encore/specs_planning/_internal/id-audit-2026-06-10/` (verdict-report.md = pattern classes + rulings; findings-merged.json = 104 verified findings; id-rename-map.{json,csv}); `export_test_cases/module-codes.json`; `plans/done/PLAN_ID_NAMING_AUDIT_AND_REMEDIATION.md` | 2026-06-10 | 2026-06-11 |
| Location Settings → Auto Add-On sub-tab — full FCC coverage (19 existing TCs, **honest-zero net-new**: 5 simple independent checkboxes, full field×case coverage proven by TC-020 bulk-invert + TC-011 multi-toggle + dialog/nav paths). **LR-019 load-bearing fix landed**: per-test `ensureDefaultState(AUTO_ADDON_DEFAULTS)` in `beforeEach` (bounded 3-retry whole-cycle, throws — Legal pattern), TC-001 inline restore slimmed → spec self-heals from a dirty start; LR-052 `clickSave` fixed-sleep poll → `waitForFunction`. **Live findings (2026-06-11, Playwright CLI):** 5 checkbox testids `location-settings-checkbox-auto-add-on-{isDefault}_{name}` (`isDefault=true` only for Labor); Save dialog "Save Changes"/Cancel·**Ok**; Unsaved-changes "Unsaved changes"/Stay·Discard on full-nav-away only (sub-tab switch silent); save endpoint `PUT /navigator/api/location/update-properties` (+ `POST .../pricebook/upsert-location-pricebook`) — LR-056 filter on `/navigator/api/`, the `?_rsc=` GETs are RSC noise; **item list is COUNTRY-scoped** (`GET /navigator/api/core/auto-addon-types?countryId=1`) constant within a country, NOT per-location → TC-016 premise corrected (kept ID); nav2 baseline PRESENT with same 5 items (parity). false-green sweep GREEN (0 unfixed; STATE-LEAK + LR-052 fixed). Full spec green ×3. F5 blind `toggleCheckbox` KEPT-with-justification (mitigated by ensureDefaultState). DQU_18 SUBSUMED, GENERATOR_AUDIT RESOLVED-BY-pointer. | Complete | `clients/encore/tests/locations/location-auto-addon.spec.ts`; `clients/encore/src/pages/locations/location-auto-addon.page.ts`; `clients/encore/specs_planning/_internal/field-inventories/auto-addon-2026-06-11.md`; `_internal/old-site-baseline/auto-addon-2026-06-11.md`; `_internal/false-green-sweeps/auto-addon-2026-06-11.md`; `_internal/field-case-catalogs/auto-addon-2026-06-11.md`; `_internal/phase-0-verification-auto-addon-2026-06-11.md`; `plans/done/SUBPLAN_AUTO_ADDON_FCC.md` | 2026-06-11 | 2026-06-11 |
| Location Settings → **launcher dialogs** — Pay To Address (left panel) + Master Bill To Address (account-address) | Complete (all phases DONE 2026-06-11 — **A** 10 `TC-LOC-LP-028..037` Pay To launcher; **B** 2 `TC-LOC-ACC-032/033` Master Bill To; both launchers PERSIST, 0 bugs; both DONE siblings carry a 2026-06-11 Post-Audit Correction) | **Pay To Address is a LAUNCHER, not a disabled textbox** — clicking the `<label>` (React onClick) opens dialog **"Pay To List"** (5 filters Pay To ID[auto-focus]/Name/Address/Phone/Fax + Search/Reset; 13-col sortable table; per-row checkbox; Select-disabled-until-checked; Cancel/Close-X; single page, rows-per-page=20). **Playwright `.click()` on the label is BLOCKED** (label bound to disabled input → "not enabled") — drive via `click({force:true})`/`dispatchEvent('click')`. Search `GET /navigator/api/location/getLocationPayToList` (~4.7s); save `PUT /navigator/api/location/update-properties`; "Save Changes" dialog = Cancel·**Ok**. **Pay To selection PERSISTS** (financial.payToId; restore anchor=ID, name ambiguous — 2 "Encore" rows). **Master Bill To** launcher (`btnAccMasterAddress`) → shared "Select Customer Address" dialog: Master select updates Master `dd`s (Venue unchanged) + **PERSISTS** (`billToAddress`) — **diverges from Venue launcher** (ACC-027 non-persist) → per-launcher coverage gap. **LR-057 landed** (affordance probe mandatory; per-launcher dedup; no-taxonomy-row HALT). | `clients/encore/specs_planning/_internal/field-inventories/left-panel-basic-information-2026-06-11.md` (`## Launcher dialogs`); `_internal/walk-evidence-account-address-master-bill-to-2026-06-11.md`; `_internal/rca-launcher-dialog-misses-2026-06-11.md`; `_internal/false-green-sweeps/launcher-dialogs-2026-06-11.md`; `_internal/old-site-baseline/{left-panel-basic-information,account-address}-2026-06-11.md`; `_internal/phase-0-verification-launcher-dialogs-2026-06-11.md`; `_internal/field-case-catalogs/launcher-dialogs-2026-06-11.md`; `clients/encore/tests/locations/location-left-panel-basic-information.spec.ts` + `location-account-address.spec.ts`; `plans/done/SUBPLAN_LAUNCHER_DIALOG_GAPS_FCC.md` | 2026-06-11 | 2026-06-11 |
| Walk-coverage enumerator pilot (PLAN_EXHAUSTIVE_WALK_GUARANTEE Phase 0) — Location Settings → Pricing (1604 USD-only/5 dropdowns + 1605 multi-currency/15 dropdowns; shadow DOM, cmdk palettes, 32-row ENABLE-cascade grid) + nav2 testid-less baseline. Machine-enumerated per-Walk_State denominators (1604 pricing Pass-A=83, 1605=93, nav2=443 keys/1 collision/STABLE); self-expand→fixpoint PROVEN (83=83); cascade is ENABLE-not-mount; M2 dropdown-option exclusion validated live (cmdk +101 = 100 opts + 1 search). **3 design findings → plan revisions before Phase 1**: F1 Pass-B event-listener infeasible from page eval → focusable-only; F2 Heuristic-A↔B structural divergence 27/2 → M4 must be union + symmetric-diff-REVIEW, not hard-DENY (else gate never clean); F3 grid 186 cells → M2 per-column-archetype collapse (else denom explodes 83→269). **4 ops**: readiness-gate every transition · native-click Radix · beforeunload dialog handler · restore-before-navigate. Verdict PROCEED-WITH-REVISIONS. **Phases 1–6 BUILT & LIVE-VALIDATED 2026-06-19** (`go ultrathink`): `scripts/walk-coverage/enumerate-page.mjs` is a Playwright script (storageState auth + CDPSession) — deterministic on real pricing (1604=79 across 2 runs / 1605=89 = +10 CAD+MXN dropdowns), self-expand→fixpoint, cascade detect→toggle→accumulate→restore (addedKeys=0 = ENABLE-cascade confirmed), CDP `getEventListeners` G1-recovery isolated the **Pay To Address** launcher. Heuristic-A v2 net (canonical scanner roles/native/attr; landmark+container excluded; disabled enumerated-and-marked). M4 = union-denominator + A△B-review (F2). Closure check **Cx** (`validate-plan-closure.mjs`, non-overridable, mirrors C6 via `coverage_mode` in `closure-config.json`, landed `announce`) + shared `coverage-manifest.mjs` + execution-completion 2nd branch + **LR-062**. To re-run a walk denominator: `npm run walk:enumerate -- --office=<O> --module=pricing`. | Complete (Phases 1–6 BUILT + live-validated; gate `announce`-ramping) | `scripts/walk-coverage/` (`enumerate-page.mjs` + `cross-check.mjs` + `lib/{deep-pierce,coverage-manifest,test-enumerate-fixtures,test-coverage-manifest}.mjs` + `critic-prompt.md`); `scripts/validate-plan-closure.mjs` (Cx) + `.claude/hooks/lib/check-execution-completion.mjs` (2nd branch) + `.claude/closure-config.json` (`coverage_mode`); `.claude/rules/inventory.md` (LR-062); `clients/encore/specs_planning/_internal/{walk-coverage-pilot-pricing-2026-06-19.md,field-inventory-spec.md}`; `plans/done/PLAN_EXHAUSTIVE_WALK_GUARANTEE.md` | 2026-06-19 | 2026-06-19 |
| Corporate Pricing → **keystone RE-WALK** (drift/false-negative/gap audit under the LR-062 enumerator) | Complete (audit; remediation = B1/B2/B3 pending) | **Keystone findings (live 2026-06-23):** (1) **Override add-affordance is a currency-gated Product-Group Picker (NM-1472) — prior "no add affordance" was a FALSE NEGATIVE**: it renders ONLY at a SPECIFIC location + SPECIFIC currency (not ALL); Equipment picker=3358 draggables, **Labor picker=420 (office 1101, NM-1881 — M4 closed)**; **double-click AND drag both ADD** override rows (proven, reversible — staged adds discard on `beforeunload`+reload, never Saved). (2) **Toolbar `Export ▾`+`Import ▾` DRIFTED**: both now open a shared **Year(s)(1–3)+Currency precondition dialog** (`Continue` disabled until both) — was direct `pricing-export` CSV (Export) / file-chooser (Import); `Loc Pricing Export`(direct CSV)/`Loc Pricing Import`(file-chooser)/Search Grid Options(9-col) UNCHANGED. (3) **Detail mgmt-mode no-add CONFIRMED defensive-by-design (NM-1443)** via 3 methods (manual full-pointer seq + playwright drag + dblclick all → 2430→2430), with the Override picker as the same-session **positive control** that the primitives DO fire — settles the `.dragTo()`-only unsound evidence; human contra-observation NOT reproduced. **Cold-start gotcha:** first authenticated load of any CPR page in a cold Playwright session renders blank (`next-auth CLIENT_FETCH_ERROR`, no login redirect) → enumerator returns `denominator=1`; RE-RUN recovers (Search=79). Enumerator denoms: Search 79/Strategy 35/Detail 38/New-Pricebook 33/Override 70. | `clients/encore/specs_planning/_internal/corp-pricing-drift-ledger-2026-06-19.md`; `_internal/old-site-baseline/corporate-pricing-rewalk-2026-06-19.md`; `_internal/field-inventories/corporate-pricing-override-2026-06-19.md`; `reports/walk-coverage/*corporate-pricing*.{json,manifest.md}`; `plans/done/SUBPLAN_CORP_PRICING_REWALK_AUDIT.md`; parent `plans/pending/PLAN_CORP_PRICING_REWALK_REMEDIATION.md` | 2026-06-23 | 2026-06-23 |
| Corporate Pricing → Pricing Strategy **DEEP coverage (NM-2261 — create multiple Pricing Strategies)** — 38 net-new `TC-CPR-STR-026..063` green ×2 (full band 63 TCs) | Complete | **Live findings (2026-06-26, Playwright CLI):** New-Pricing-Strategy dialog Strategy Name **maxlength=100** (NOT 255); **duplicate name BLOCKED** with inline error "A pricing strategy with this name already exists." (**contradicts the NM-2059 "dup allowed" lead → stale/already-fixed**; encoded live-blocked); empty/whitespace name → **Add disabled** (trim); special chars accepted+preserved; **NM-2047 mutual-exclusion** — Is Productions checked → disables Is Internal+Is GSO in BOTH editor and dialog; dialog flag defaults Is Active:true / GSO,Internal,Productions:false; **revert name/flag to original → Save disabled** (net-zero, LR-009); dirty **survives Strategy↔Detail sub-tab switch** (silent) but **full nav-away (breadcrumb) raises the "Unsaved changes" alertdialog** (Stay/Discard) + native beforeunload (fixture auto-accepts it); editor flags render as **Radix `aria-checked`** (nested check img) — the strategy **LIST has NO boolean columns** (corrects the plan's "list renders boolean cols" premise); **"Search strategies…" filters the list client-side** (Total reflects the FILTERED count) → **result-fidelity IN-SCOPE** (corrects the plan's out-of-scope assumption). **Mutation boundary**: persisting a NEW strategy is IRREVERSIBLE (no UI delete; no disposable fixture) → create-multiple mechanics covered IN-SESSION (reload discards) + save-gating/reachability on the no-commit New-Pricebook page; DB-persist of a new strategy is the only NOT-AUTOMATABLE slice. SBC families render-state/empty-vol/persistence/result-fidelity covered QUICK+DEEP; pagination/sorting/combination out-of-scope. **Drift note**: fixture `2022-NP Tier 1` lost its location assignments since 2026-06-05 (TC-013 re-coupled to structure not data, ALL-092); legacy toast test ×2-flake hardened (TC-024). | `clients/encore/tests/corporate-pricing/corporate-pricing-strategy.spec.ts` (63 TCs, ×2 green); `corporate-pricing-strategy.page.ts` (+dialog/editor/list/nav helpers, hardened `saveAndConfirm`, `hasLocationsTable`); `corporate-pricing-new-pricebook.page.ts` (+`removeStrategy`); `src/data/corporate-pricing/strategy.ts` (`deep`); `specs_planning/test-{cases,plans}/setup/corporate-pricing/corporate_pricing_strategy_*`; `plans/done/SUBPLAN_CORP_PRICING_NM2261_STRATEGY.md` | 2026-06-26 | 2026-06-26 |
| Corporate Pricing → Pricing Strategy **"Locations Using Pricing As Default" grid — cross-surface coverage (SUBPLAN_CORP_PRICING_COVERAGE_FOLLOWUPS, NM-2260/2261 follow-up)** | Complete | **Live findings (2026-06-29, Playwright CLI):** the grid is a **read-only cross-surface back-reference** — an office appears in a strategy's grid ONLY because that office selected the strategy as its **Primary Pricing** on the **Location → Pricing tab** (`selectPrimaryDropdownOption`, `drpPrimaryEquipmentPricingUSD`). **Zero-mutation positive control (LR-061 C):** office 1604's Primary Equipment Pricing = `2026-Tier 2 Resort B` ⇒ that strategy's grid shows `["1604","Parker Palm Springs"]` (pricebook `2026-PB5`, GUID `d4f8d502-ca92-5fdf-91d6-5b1bee109f54`). The fixture `2022-NP Tier 1` grid is currently EMPTY (its 1991/7011 defaults reassigned away) — exactly what made the old `for…of getStrategyLocations(){expect}` loop pass **VACUOUSLY**. **Fix:** `TC-CPR-STR-013` rewritten as a self-seeded cross-surface integration test (set 1604's Primary Equipment Pricing → known strategy → assert 1604 surfaces → restore; net-zero) — no longer ALL-092-structure-only. **Systemic guard landed:** `scripts/check-vacuous-grid-assertions.mjs` (warn-only in `.githooks/pre-commit`; flags `for…of`/`.forEach` over a row getter with no collection-bound non-zero guard; 15 fixture tests; 0 findings repo-wide). Office 1101 has 596 corporate pricebooks (corporate data present, LR-ENC-005). | `clients/encore/specs_planning/_internal/walk-evidence-corporate-pricing-2026-06-29.md`; `clients/encore/tests/corporate-pricing/corporate-pricing-strategy.spec.ts` (TC-CPR-STR-013); `src/data/corporate-pricing/strategy.ts` (`crossSurfaceSeed`); `scripts/check-vacuous-grid-assertions.{mjs,test.mjs}`; `plans/done/SUBPLAN_CORP_PRICING_COVERAGE_FOLLOWUPS.md` | 2026-06-29 | 2026-06-29 |
| Corporate Pricing → **Loc Pricing Export — REAL download round-trip + file verification (NM-2262)** | Complete | **First real file-download infra in the repo.** Reusable `captureCsvDownload(trigger, apiPathFragment)` (private) + thin `downloadLocPricingExport()` wrapper on `corporate-pricing-search.page.ts`, returning `CsvDownloadResult {filename, content, headers, rows, rowCount, requestUrl}` — arms `waitForEvent('download')` + `waitForRequest` on the SAME click, reads from the browser temp path (nothing lands in the repo), quote-aware + BOM-stripped + faithful CSV parse (no field-trim, interior blanks kept). **Live 2026-07-06 (office 1604):** download event FIRES (`waitForEvent('download')` works — it is NOT a fetch+blob); filename `LocationPricebooks_<YYYYMMDD>_<HHMMSS>UTC.csv` (underscore+UTC, not the older hyphen); 11 headers `LocationNo,PricingStrategy,PriceBook,Currency,IsInternal,IsLabor,IsAlternate,IsProduction,UseDate,StartDate,EndDate`; ~38010 rows = **ALL-LOCATIONS dataset** (rows begin at office 1101, a DIFFERENT dataset than the on-screen strategy grid → the file is the oracle, never a grid row-for-row diff); currencies ⊆ {USD,CAD,MXN}; flags 0/1. TC-CPR-TIO-018..024 (018–023 green ×2, 024 data-blocked empty-office skip) + TC-012 endpoint smoke re-verified. **Split 2026-07-08: these Loc Export tests moved to `corporate-pricing-loc-export.spec.ts` (own submodule `loc_pricing_export`, renumbered `TC-CPR-LEX-001..007`); the NM-2264 Export ▾ tests → `corporate-pricing-export-all.spec.ts` (EXA); the NM-2305 Loc Import tests → `corporate-pricing-loc-import.spec.ts` (LIM); TC-012/013 baseline stay in `corporate-pricing-toolbar-io.spec.ts`.** **NM-2264 (Export ▾ variants) reuses `captureCsvDownload`** — pass the Export button + `pricing-export` path; no core change. **Still red (NOT NM-2262):** the 8 Export ▾/Import ▾ drift tests (TC-CPR-TIO-002/003/004/005/008/009/010/011, Year+Currency dialog since 2026-06-10) owned by NM-2264/NM-2305. **Gotcha (graduated):** esbuild transpiles specs WITHOUT typechecking, so a green `npx playwright test` does NOT prove the spec typechecks — re-run `tsc --noEmit` after every spec rewrite (a caught `noUncheckedIndexedAccess` blocker). | `clients/encore/tests/corporate-pricing/corporate-pricing-toolbar-io.spec.ts`; `clients/encore/src/pages/corporate-pricing/corporate-pricing-search.page.ts` (`captureCsvDownload`/`downloadLocPricingExport`/`splitCsvLine`); `clients/encore/src/data/corporate-pricing/toolbar-io.ts` (`locExport`); `plans/done/SUBPLAN_CORP_PRICING_NM2262_LOC_EXPORT.md` | 2026-07-07 | 2026-07-07 |
| Corporate Pricing → **Export ▾ All — Year(s)+Currency dialog + real per-variant download round-trip (NM-2264)** | Complete (22 Export TCs green ×2) | **Live 2026-07-07 (office 1604):** the 4 Export variants gate behind a shared **"Export" dialog** — Year(s) multi-select (2021–2028, **caps at 3**, a 4th is silently refused, no aria-disabled) + Currency (USD/CAD/MXN); **Continue disabled until BOTH set**; Cancel & Close(X) dismiss. On Continue → `GET pricing-export?isLabor&isMaxDiscount&currencyId&locale&years` [200]. **currencyId USD=1/CAD=2/MXN=3** (live). **`years` is REPEATED params** (`years=2026&years=2027&years=2028`) — use `getAll('years')`, NOT `get()`. **Downloaded CSV is a WIDE MATRIX** (`Product Group Id, Product Group Name, <one col per pricebook>`; row-2 = currency-per-column) — filenames `EquipmentPricings.csv`/`LaborPricings.csv`/`EquipmentMaxDiscounts.csv`/`LaborMaxDiscounts.csv` (no timestamp). Equipment (≈3256 PGs) vs Labor (≈422) = disjoint populations + different pricebook columns; Pricing vs Max Discount = same shape, distinguish by the `isMaxDiscount` param (Max-Discount cells mostly blank). **Currency scopes pricebook-column volume**: USD Equip ≈79 cols, MXN ≈13, **CAD = 0** (base cols only) → CAD is a live empty/minimal-scope oracle. Reuses NM-2262 `captureCsvDownload` via public `downloadExportVariant(variant, years, currency)` (extended to capture concrete HTTP status). **CLI gotcha:** `playwright-cli run-code` sandbox has NO `require`/`URL` globals — read files via `download.saveAs()`, parse URLs by string. **Import ▾ 007-011 still drifted — owned by NM2265.** | 2026-07-07 | 2026-07-07 |
| Framework → **delegation-chain communication/telemetry + framework escalation machinery (uplink recon)** — can any agent ask a question mid-run? (answer: NO, structurally — every dispatch is single-turn `-p --no-ask-user`; only post-hoc channels exist: BLOCKERS_DEVIATIONS, `--interrogate` re-dispatch, chain PAUSE_NOTICE, file-based agent-notifications). Full affordance inventory (wrapper flags/stall guard/ledger fields/exit codes), 11-item uplink gap-map (question-class enum ABSENT, advisory schema ABSENT, control files ABSENT, freeze-resume ABSENT, cache ABSENT, graduation PARTIAL), wrapper attachment anchors D1–D5, per-pipeline-agent ask-affordance table, LR-069 ramp summary. Cross-family reviewed (gpt-5.5): ACCEPT×2, claims C1–C10 VERIFIED. | Complete | `.claude/state/ua-worker/uplink-recon-delegation-0712/result.md`; `.claude/state/ua-worker/uplink-recon-framework-0712/result.md`; `.claude/state/ua-worker/uplink-recon-review-0712/result.md`; consumer plan `plans/pending/PLAN_UPLINK_PROTOCOL.md` | 2026-07-12 | 2026-07-12 |
| Encore → Location Settings → **Service Charge** (NM-3344) — both sub-tabs. Basic Information: 29 interactive elements, 79 service-type rows, percentage + toggle fields, save cycle. Service Charge History: 30 elements, read-only audit grid (Modified By renders a GUID rather than a name — recorded as a discussion item). Office 1604 Parker Palm Springs. Two known product defects carried as annotated failing tests. Enumeration of this surface exposed and fixed a sign-in-redirect false-green in the walk tool. | Complete | `clients/encore/specs_planning/_internal/field-inventories/service-charge-basic-information-2026-08-10.md`, `clients/encore/specs_planning/_internal/field-inventories/service-charge-history-2026-08-10.md`, `clients/encore/specs_planning/_internal/field-case-catalogs/service-charge-2026-08-10.md`, `clients/encore/specs_planning/_internal/old-site-baseline/service-charge-2026-08-11.md`, `clients/encore/specs_planning/_internal/walk-evidence-service-charge-2026-08-10.md` | 2026-08-10 | 2026-08-11 |
| (add new surfaces below when first-explored) | | | | |

**Schema for adding a row** (do this in `/reflect` at end of any session that explored new territory):

```
| {Module / page / tab / feature} | Complete \| Partial ({what fraction}) \| Deferred | {findings file paths, comma-separated} | YYYY-MM-DD | YYYY-MM-DD |
```

---

