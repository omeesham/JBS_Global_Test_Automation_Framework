---
name: project_copilot_takeover_system
description: "The local Claude-CEO / Copilot-workforce delegation takeover (v3) — where it lives, how it works, what's gated"
metadata: 
  node_type: memory
  type: project
  originSessionId: 13db75ca-25ea-40ee-bf91-e834882822d7
---

**Swiss-Alps secret, local-only, never pushed.** Built 2026-07-09 on top of the [[feedback_copilot_vs_claude_code]] worker extension.

Doctrine: **Claude = final-verdict reader + oneliner converter, nothing more** — unless things get messy or Rutvik asks, then Claude stretches its review/interrogation muscles. Copilot = entire workforce doing the full 8-duty stack; GPT (council-reviewer) reviews and emits a ≤10-line DIGEST that is the ONLY thing Claude reads by default. Suspicion → Claude interrogates via GPT (`--interrogate`), not by re-reading the work; personal deep-review only on trust-collapse (logged as a system bug).

Where it lives (ALL outside the repo):
- `~/.claude/delegation/` — DUTY_STACK.md, config.json (`AUTO_SELF/GATE/MAX_WORKERS`), ticket-template.md, tickets/, reports/, locks/, self_incidents.log.
- `~/.copilot/agents/` — council-worker (sonnet-4.6), council-planner (opus-4.6), council-reviewer (gpt-5.5), council-verifier (gpt-5-mini).
- `~/.claude/hooks/` — delegation-gate.mjs (DENYs Claude repo-source writes when GATE=on; scoped SELF_GRANT + pipeline-identity escape), ua-worker-guard.mjs v2 (DENYs unjustified Claude Agent spawns).
- Repo, git-EXCLUDED: `.claude/skills/ultra-agents/worker-ext.md` (v3 doctrine) + `copilot-worker.sh` (wrapper: verified model registry, `--ticket` mode, atomic slot-lock, `--interrogate`).

Verified model registry (probed, NOT assumed): opus-4.6/sonnet-4.6 = max; gpt-5.5 = xhigh; gpt-5-mini = high; haiku-4.5 = no effort enum (flag omitted). gpt-4.1/gemini-2.5-pro/gpt-5.3-codex NOT on account (hard error). Unknown model → wrapper exit 2 (kills the silent effort-fallback bug).

Frugality law: **one headshot, not an AK47** — minimum dispatches, cheapest-capable-first (T0 gpt-5-mini → T2 sonnet default → T3 opus → T4 gpt-5.5 review-only), every fan-out >1 justified in the Delegation receipt.

**Delegation-first rule (2026-07-10, Rutvik: "laziest but smartest delegator")**: before Claude does ANY unit of work itself — could a shell+edit worker do it? Yes → ticket. DEFAULT-DELEGATE: live playwright-cli browser walks/testid dumps, verification batteries (≥3 commands or any spec/browser run), all drafting (rows/specs/docs/xlsx), diff review (T4 digest — never line-read after a green review), RCA legwork, and defect fixes (BOUNCE to the worker, not SELF_GRANT self-fix). CLAUDE-ONLY is a closed 7-item list (talk to Rutvik, ticket/dispatch/digest mechanics, identity+activity-log+plan-status ceremony, control-file edits, memory, Chrome-MCP visuals, final judgment). Simple case = 1 worker + 1 cross-family reviewer, both green → accept. Inline self-work a worker could have done = routing incident (self_incidents.log + "I coded myself" confession in the Receipt). Full text: worker-ext.md "Delegation-first decision rule". A SessionStart primer (`~/.claude/hooks/delegation-primer.mjs`, registered in settings.json) is LIVE since 2026-07-10 (Rutvik's explicit "go") — it injects the delegation-first block into every session starting in a repo that carries worker-ext.md while GATE=on. Three enforcement layers now: session START (primer) + during (delegation gate + spawn guard) + session END (final-q Receipt).

**Off-repo parity + prosecutor-reviewer (2026-07-10, Rutvik: "if worker moved out of repo, reviewer also moves out; reviewer = death-sworn enemy — guilty until proven not")**: any ticket whose work/evidence involves EXTERNAL state (live app walks, network, env mutations, untracked artifacts, ran-X-saw-Y observations — NOT ordinary in-repo test re-runs, those stay layer-1) can NEVER be greened on paper — an independent agent re-executes the same off-repo actions fresh (shell-enabled dispatch, `--mode edit`; read mode denies shell) and the reviewer diffs claims vs re-execution evidence. Reviewer presumes guilt: starting verdict MATERIAL_ISSUES, every claim marked VERIFIED/UNPROVEN/ENV-BLOCKED, only independently-verified evidence acquits, verified claims settled (no eternal bounce loops), UNDISCLOSED off-repo activity punished harder than honest mistakes. Dispatcher sets `OFF-REPO: yes|no` (workers never self-classify); adaptive re-executions replay the first run's recorded steps/oracles; env flake = one retry then ENV-BLOCKED; one-shot unreproducibles = UNPROVEN → escalate. Fast path excludes off-repo tickets until re-execution passes. Pyramid layers 3+4 in worker-ext.md + council-reviewer brief; stress-tested by gpt-5.5 (3 rounds total this day, all findings folded).

**LIVE since 2026-07-09** — both hooks registered in `~/.claude/settings.json` (Rutvik's GO given); Bash matcher was dropped same day (Rutvik-approved: pattern detection false-positived on `>`/`sed` inside echo strings — Edit/Write is the real guard).

**v2 loophole-hardening (Rutvik standing approval)**: 6 PROTECTED control files (hooks dir, settings.json, delegation config.json kill-switch, DUTY_STACK.md, repo copilot-worker.sh + worker-ext.md) deny Claude edits even inside allowed paths — escape only via Rutvik-approved SELF_GRANT (reason ≥20 chars, TTL ≤60 min, session+path-scoped) with every use logged to `~/.claude/delegation/grants-audit.log` and surfaced in the Delegation receipt. Allow-paths are repo-root-anchored (no `src/plans/evil.ts` tricks); scoping is target-based (any session, any cwd). 12/12 fixtures proven on the live hook. Honest limits: Bash ungated (approved), pipeline-identity exemption open until Phase D (pipeline → tickets, separate later plan).

**CHEATPROOF acceptance layer LIVE (2026-07-15, PLAN_DELEGATION_CHEATPROOF DONE)**: acceptance of delegated work now reads machine facts, never report prose. `~/.claude/delegation/gates/` holds `envelope.mjs` (pre-dispatch path snapshot) + `verify-run.mjs` (post-return verifier with sha256 self-integrity check against Rutvik-owned pin `private/gates.sha256`; tamper → exit 3, no verdict). **Three-way verdict law** (worker-ext "Acceptance Law"): GENUINE=accept, FABRICATED=hard-bounce with JSON reasons, UNPROVABLE=route to human/Claude judgment — NEVER auto-bounce (the verifier is a prover-of-honesty, not a fraud-detector; on the 10-fixture corpus it never false-accuses). Verifier in LR-069 announce-ramp for first ~10 live acceptances — earned its keep on round 1 (false-FABRICATED an honest round via relative-path resolution; ramp absorbed it). Schema is now **VERIFY_ARTIFACTS** (tee'd files + sha256, re-hashed + re-executed) across DUTY_STACK, ticket-template, and all 5 seat files — pasted prose is dead as evidence. Wrapper has fresh-file guarantee (duplicate run-id → exit 2 — kills shared-verdict-file collusion). labor-gate v3 (bash/sh -c recursion, 16-case oracle) DENY mode live. G1 hook exempts `tickets/` (trust-vector: gates aim at copilot's claims, never Claude's dispatch path). Private fixture corpus at `~/.claude/delegation/private/` (answer key — never share with workers); rotation policy: every hand-caught fabrication becomes a fixture, re-score after any gate edit. Ticket rules: [[ticket-literal-paths]]; probe rules: [[file-based-probes-only]].
