# PLAN_79 leak ledger — §4.6. One row per defect found during this plan's work, appended at find-time.

| # | found | what leaked | root cause | instance fix | permanent block | class | status |
|---|---|---|---|---|---|---|---|
| 1 | 2026-08-20 | stale §4.6 cross-reference in plan body (pointed at a section that did not exist) | hand-written section cross-refs have no checker | repointed to §5.3 point 5 | none — single hand-edit, no generating mechanism beyond ordinary authoring | — | ONE-OFF |
| 2 | 2026-08-20 | two dispatch launches silently did nothing (V1 wave-2; C1 army convergence) — exit 0, zero output | orchestrator shell habit: multiple heredocs stacked in one Bash command; second heredoc swallows the rest of the command | re-ran each with a single clean heredoc | rule: ONE heredoc per Bash command, ticket writes verified by `wc -c` before dispatch; graduated to auto-memory `feedback_one_heredoc_per_command.md` (loads every session) | schema | BLOCKED |
| 3 | 2026-08-20 | recommendation layer molded to the colleague's degraded practice (single-row `<ol>` steps) because "it works in their TestRail" | no standing law separating "proves it imports" from "quality bar" — worker tickets carried mold-to-sample framing | recommendation reversed same session (multi-row stays) | L12 anti-mold law baked into PLAN_79 §4.5 (binding on every seat + every ticket); S1 slop re-audit re-judged all standing recommendations under it | rewire | BLOCKED |
| 4 | 2026-08-20 | risk-register top-5 carried 3 overstated rows (hazard counts dressed as proven corruption; UNPROVEN detector behavior stated as fact) | converger promoted evidence counts into failure mechanisms without re-derivation | c2 cross-family fight refuted them; corrected picture relayed, register shipped WITH the fight file | standing pattern already blocked by PLAN_75-TEMP fight law (canary + re-derivation before anything reaches Rutvik) — this row records the catch working | gate | BLOCKED |
