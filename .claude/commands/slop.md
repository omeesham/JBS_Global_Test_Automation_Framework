---
description: Anti-over-engineering DROP/KEEP audit (alias — routes to /audit slop)
argument-hint: "[review|audit] <target>"
---

Run the `audit` skill in `slop` mode. Pass through any arguments verbatim:

`/audit slop $ARGUMENTS`

Mode-routing rules and DROP/KEEP algorithm: see `.claude/skills/audit/SKILL.md` §SLOP.
