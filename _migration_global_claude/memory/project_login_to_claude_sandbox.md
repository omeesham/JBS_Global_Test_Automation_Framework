---
name: ""
metadata: 
  node_type: memory
  originSessionId: b2d19670-2556-4801-9a96-eac361dcd7b2
---

"Login to Claude" = a customer-compute QA product: the user chats, the AI runs on the **customer's own Claude on their own PC** (a thin local "Claude Connector"), our server holds orchestration + all data. Built 2026-06-17 by executing `~/.claude/plans/https-github-com-omeesham-jbsintelliqe-w-mutable-anchor.md`.

**Sandbox (never touches originals):** `C:\Users\rutvi\projects\intelliqe-sandbox\` — `demo/` (clone of `omeesham/JBSIntelliQE`, the frontend) + `backend/` (clone of `RutviK-JBS/qa_agentic_framework_global`, our `website/backend`). Both on branch `sandbox/login-to-claude` with **`git remote set-url --push origin DISABLED_NO_PUSH`** (originals un-pushable). Reuses the machine's existing `intelliqe_postgres` Docker container (postgres/admin/postgres:5432).

**Built + verified (P0-P4, all additive):**
- Backend on **:3002** (`backend/website/backend/run-sandbox.sh` sources `.env.sandbox`; secrets there). Per-user layer: migration `003_per_user_connector.sql` (`user_ai_config`/`user_connector_registry`/`user_connector_pairing`/`user_job_queue`, keyed on **username**), `services/user-connector.service.ts`, `routes/user-connector.routes.ts`. The **one guarded contract edit** = `chat.routes.ts` `/ask` dispatch-to-Connector branch (else server fallback) + `execution_mode` marker. DB-polling rendezvous (restart-safe). `req.userId` is normalized to username by `tenant.middleware.ts`; backend reads JWT from **`x-auth-token`**.
- Frontend (demo): `pages/ClaudeConnectorPage.tsx` wizard, `vite.config.ts` proxy (feature paths → :3002, rest → :3001), `api.ts` `x-auth-token` shim, `AuthContext` role-bridge (super_admin→admin), Sidebar nav.
- **Connector** (`intelliqe-sandbox/connector/`, runnable Node core): register/heartbeat/claim/run-local-`claude`/complete; env-scrub strips `CLAUDE*` **and** `ANTHROPIC_API_KEY`; mirrors server spawn (`-p - --model M --output-format json`, tree-kill); `scripts/install.ps1`+`uninstall.ps1` (Startup-folder shortcut + HKCU `claudeconnector://` protocol handler). Controls: Pause/Resume (website), Quit (Ctrl+C), Disconnect (`--disconnect`).
- Harnesses (re-runnable): `backend/website/backend/sandbox-tests/p1-rendezvous.mjs` (11/11) and `health-check.mjs` (8/8, incl. a **real** `execution_mode==='cli'` round-trip through local Claude).

**Hardening + UX pass (2026-06-17, trial-grade, all verified):** adversarial /find-bugs + /review on clones → fixed real bugs. Backend: A2 (once a Connector is engaged, dispatch NEVER falls back to server billing — honest error only), B3 (fail-fast a *queued* job when connector offline, never a *claimed* one), C1 (single-use pairing `consumed_by IS NULL`), C2 (timingSafeEqual worker-secret), C3 (heartbeat staleness in SQL), A3 (`utils/require-secrets.ts` refuses dev-default secrets when NODE_ENV=production / CONNECTOR_REQUIRE_SECRETS=1). Connector: A1 (`cleanEnv` now scrubs EVERY `ANTHROPIC_*`+`CLAUDE*`, not just the API key — kills all per-token billing vectors), B1 (graceful shutdown finishes the in-flight job, no orphan), B2 (`complete()` retry 3×), B4 (exit on missing CLI / 4xx register), and `isLoggedIn` is now zero-quota via `claude auth status --json` (was a real model-turn probe).
**Nav relocation (Rutvik request):** "Login to Claude" removed from the primary sidebar → now a **tab inside System Configuration** (`ConfigTabNav` + `SystemConfigurationPage` render `<ClaudeConnectorPage embedded />`, config-independent so it renders even when the demo's `/configurations` 500s); `/claude-connector` route redirects there. Scoped to admin/qa roles by that page's gate.
**Seamless sign-in BUILT (Group G, was pending):** the in-app button now drives `claude auth login --claudeai` (subscription, never `--console`/API) ON the user's PC via a DB control channel (migration `004_connector_login.sql` adds `login_status`/`login_url`; `request-login`/`login-status` routes; connector opens the browser or surfaces the auth URL). New `signing-in` UI phase + per-phase UX (D1 unreachable, F1 pre-login guidance, F2 honest copy). KEY: the CLI auth command is **`claude auth login`** (NOT `claude login`, which doesn't exist) — G0 probe finding.
**Verified:** tsc clean (both), `p1-rendezvous` 11/11, new `p2-hardening` 14/14 (A1/C1/A2/G1 simulated), `health-check` 8/8 (real CLI round-trip `execution_mode:'cli'`), and a real Chrome drive of localhost (tab relocation + live "Ready" badge from :3002). Plan: `~/.claude/plans/twinkling-launching-book.md`.
**Still the user's manual step:** actually clicking "Login to Claude" while online runs `claude auth login` which would re-auth the live session — I did NOT trigger it (Rutvik tests the real seamless sign-in with his 2nd account). Packaged unsigned Windows `.exe` + tray icon still deferred to after that passes. See [[project_encore_deliverable_channel]] for how real deliverables ship.

**Last-mile: installer + adversarial re-audit (2026-06-18, all additive, push disabled):** Re-audit found **22 bugs** (2 critical, 5 high) — ALL fixed across `connector`/`backend`/`demo` (3 separate git repos, commit with `-c user.name=RutviK-JBS -c user.email=...`). Highlights: **F-RTE-02 per-connector token** (mint at register → SHA-256 hash on registry row → `x-connector-token` timingSafeEqual on every mutation; closes cross-user connector hijack; migration `005_per_connector_auth.sql` — *renamed from `005_connector_token.sql` because `.gitignore` `*token*` silently ate it*). **F-SVC-01 billing-trust**: a stale-but-engaged connector (in-flight job OR within freshness+grace) NEVER falls back to server-billed Claude (`isConnectorEngaged`); connector emits a **background-timer heartbeat** so a long job stays "online". **F-INST-01 (critical)**: install.ps1 wrote a UTF-8 **BOM** that broke `JSON.parse` → `dev-secret` fallback → dead cold-start; fixed with `[IO.File]::WriteAllText(..., UTF8Encoding($false))`. F-RTE-01 (JWT header-only), F-RTE-03 (login_url phishing validation), F-CON-04 (no-shell `spawn` for openInBrowser), F-WIZ-01/02/03 (`derivePhase` ordering, extracted to `demo/.../connectorPhase.ts` for unit tests).
**Windows installer BUILT + tested (Node SEA .exe):** `connector/build/build-win.mjs` (esbuild CJS → SEA blob → postject) → **`claude-connector-setup.exe` ~91 MB**, bakes the worker secret, self-installs on first run (`src/selfinstall.mjs` → protocol + Startup → the .exe). esbuild+postject are connector **devDeps**. Artifacts gitignored (`build/dist/`, `demo/.../public/download/*.exe|*.dmg`). `--selftest`/`--once`/self-install all pass. **macOS AUTHORED (handoff to run on a Mac):** `scripts/install.sh` + `build/build-mac.sh` (.dmg w/ AppleScript URL-handler .app wrapping the SEA) + LaunchAgent + `disconnect()` darwin branch.
**124 AUTO checks green, 0 fail:** last-mile.mjs 37/37 (`npx tsx`), test-install.ps1 14/14 (temp APPDATA + HKCU backup/restore), SEA WIN-01..05, MAC-06 static 10/10, and existing harnesses still pass after the token change (p1 12/12 — now self-cleans superadmin so the suite is order-independent — p2 15/15, b3 4/4, lifecycle 29/29). Backend `tsc`: ONLY the 3 pre-existing `worker/index.ts` errors (dotenv-flow — NOT ours, proven by stash test). HANDOFF steps (clean-Win install+SmartScreen, all macOS, live `claude auth login`, real `execution_mode:'cli'` round-trip) in `connector/HANDOFF_TESTS.md`; plan/cases in `connector/LAST_MILE_{PLAN,TEST_CASES}.md`.

**Full user control + subpage redesign (2026-06-18, additive, push disabled):** Rutvik wanted real CRUD/leave controls (was only Pause). Built **Test connection** (in-app button: sends a tiny ping through the user's own Claude, shows the reply — no chat needed), **Disconnect** (sever IntelliQE↔Claude, helper stays installed) and **Remove** (uninstall the helper from the PC). KEY semantic per Rutvik: **we NEVER run `claude auth logout`** — the user's personal Claude login is never touched in any flow; "disconnect"=forget the pairing, "remove"=uninstall our helper. Server data = NOT a forget-me: keep API key + history by default, optional toggles to also remove each. Architecture: device-side actions ride the **same DB command-channel as login** — wizard sets `pending_command` (migration `006_connector_commands.sql`), connector reads it on `/api/connector/claim` (now returns `{command:'disconnect'|'remove'}`), acts locally, then `POST /api/connector/command-confirm` (worker-secret+token) → server deletes registry+pairing. Disconnect is liveness-aware (online→command+confirm; offline→immediate-forget so never "stuck connected"). Remove applies the user's data-removal flags at REQUEST time (JWT route), defers registry/pairing delete to confirm. New service fns (`requestCommand/claimCommand/usernameForConnector/disconnectConnector/removeServerData/clearApiKey`) + routes (`/connector/test|disconnect|remove|command-confirm|clear-key`). Connector: `selfinstall.mjs` now has shared `uninstallLaunchPoints()` + `selfRemove()` (teardown + delete config dir + **Windows delayed self-delete** of the running .exe via detached `cmd /c ping & del` — a process can't delete its own image; best-effort, inert leftover if locked); `index.mjs` `handleCommand()`; `disconnect()` delegates to the shared teardown. Frontend **subpage redesigned** (simpler, less redundant): status card carries the one contextual action (+inline Test when Ready), setup wizard only pre-connect, Manage (Pause+Disconnect), red Remove danger zone, collapsible Advanced (API key + Remove key), confirm modal with data toggles. **AUTO: lifecycle-control.mjs 25/25** (Test happy/offline-fast/needs-login/error/401, Disconnect online+offline+removeHistory+409, Remove data-matrix keep/key/history/both, command-confirm 401/403, clear-key, login-channel regression) + **test-selfremove.mjs 7/7** (temp-APPDATA teardown, captured delayed-delete). Regressions green (p1 12, lifecycle-contract 29 standalone, last-mile 39). Backend tsc only the 3 pre-existing worker/index.ts errors; frontend tsc + Vite transform clean. Committed 3 repos (backend `67c3eda`, connector `39f9cd8`, demo `bb369e0`, identity-flagged, no push). Handoff (real-PC Remove vanish + reboot, macOS uninstall) added to `HANDOFF_TESTS.md` §D; cases in `connector/LIFECYCLE_CONTROL_TEST_CASES.md`. Plan: `~/.claude/plans/lazy-swinging-castle.md`.

**Full lifecycle manual-QA pass (2026-06-17):** Wrote a test-case matrix FIRST (`sandbox-tests/LOGIN_TO_CLAUDE_TEST_MATRIX.md`) then executed every button. Artifacts (re-runnable): `sandbox-tests/lifecycle-contract.mjs` (28/28) + `b3-failfast.mjs` (4/4) = 32 contract checks. Drove all 8 UI phases by DB-forcing state + pressing buttons in Playwright (not-connected/offline/needs-login/signing-in/ready/paused/unreachable + signing-in-precedence). Nav/role matrix: super_admin/client_admin/qa_engineer see the tab in sidebar; data_engineer NOT in sidebar but reaches it via direct URL (demo's sidebar-filter model, route not hard-guarded — flagged). **BUG FOUND+FIXED (additive):** Save API key stored the frontend's `__ENC__` transit-obfuscated blob (corrupted at-rest key + garbage hint). Fix = `decryptField(rawApiKey)` in our `routes/user-connector.routes.ts` POST /connector/config (mirrors auth.routes/jira.routes); re-verified hint `sk-ant...88`, contract still 28/28. **Reproduced the "nothing happens"**: cold Login fires `claudeconnector://…` → ERR_ABORTED "scheme has no registered handler". Proved the connector-SIDE of the launch (parses the protocol URL → pairs → registers → online, zero-quota) WITHOUT a registry write. Two real gaps to a true cold-start, both deferred-installer scope: (1) `claudeconnector://` OS handler unregistered (registry write — policy-denied without explicit user auth; install.ps1 does it), (2) `/download/claude-connector-setup.exe` 404s. NEVER-ASSUME catch: a silently-failed psql query (selected non-existent `is_paused` col) made the registry look empty — corrected before building on it.
