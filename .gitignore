# Windows null device artifact
nul

# Temporary per-task files (worker MCP configs)
.tmp/

# Node / TypeScript
node_modules/
npm-debug.log
yarn-error.log
yarn.lock
pnpm-lock.yaml
.build/
/dist/
/dist-pipeline/
# Per-client build artifacts (parallel to /dist/ and /dist-pipeline/).
# Post-2026-05-19: clients don't ship pre-built artifacts — `src/` lives inside
# clients/<id>/ and Playwright loads .ts reporters directly.
clients/*/dist/
client-delivery/
*.tsbuildinfo
*.js.map
*.d.ts.map

# Playwright
test-results/
playwright-report/
playwright/.cache/
.playwright/

# Logs
logs/*
*.log
!logs/README.md
*.out

# Reports — all generated output ignored, dirs created at runtime by global-setup.ts + ensure-report-dirs.js
reports/*
!reports/.gitkeep
/allure-results/
/allure-report/
/html-report/
screenshots/
!screenshots/.gitkeep

# Environment
.env
.env.local
.env.*.local
# Use .env.example / .env.server.example as templates — never commit real .env files
.env.server

# OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.directory

# IDEs
.idea/
*.iml
*.iws
*.ipr
.vscode/
*.code-workspace
.fleet/

# Testing
coverage/
# ...but not the tracked /coverage skill (see line ~117: .claude/skills/ is shipped to collaborators)
!.claude/skills/coverage/
.nyc_output/

# Temporary
temp/
tmp/
*.tmp

# Database
*.db
*.sqlite
*.sqlite3

# Security files
*token*
*.pem
*.key

# Auth sessions
.auth/

# Root artifact junk (defensive — anchored to repo root)
/li_run*.txt
/seed.spec.ts

# MCP browser session artifacts
.playwright-mcp/

# Private skills (never commit)
.claude/skills/end-day/
.claude/skills/end-week/
specs_planning/_internal/daily-status-bank.json

# Agent audit reports (internal only - proprietary)
specs_planning/audits/*.md
!specs_planning/audits/.gitkeep

# Agent performance tracking (internal only - proprietary)
specs_planning/agent-performance.json
specs_planning/_internal/agent-activity-log.md

# Playwright browsers cache
.browsers/
ms-playwright/

# Claude Code — team files tracked, personal files ignored
# Tracked for collaborators: CLAUDE.md, .claude/skills/, .claude/agents/,
#   .claude/context/, .claude/PROTOCOL.md, .claude/AGENT_SCHOOL.md,
#   .claude/launch.json, website/.claude/launch.json
.claude/settings.local.json
website/.claude/settings.local.json
.claude/channel/
.claude/worktrees/
.claude/scheduled-tasks/
.claude/plans/
.claude/private/
current_plan.md

# Chain orchestration runtime state (see PLAN_CHAIN_PER_SESSION_ORCHESTRATION.md; only .gitkeep tracked)
.claude/state/*.json
.claude/state/*.jsonl
.claude/state/*.log
.claude/state/*.lock
.claude/state/chain.STOP
.claude/state/chain-sessions/
.claude/state/chain-archive/
.claude/state/chain-sessions-green/*
!.claude/state/.gitkeep
!.claude/state/chain-sessions-green/.gitkeep
# Parity-mission staging + recon (SECRET-tier mirrors of ~/.claude control surface; never tracked)
.claude/state/parity-staging/
.claude/state/parity-recon/

# Bundle smoke test sandbox (ephemeral — created outside repo, but guard against accidental in-repo recreation)
encore_bundle_sandbox/

# Server env — shared for team onboarding
# config/environments/.env.server

# Screenshots / junk artifacts (root level only)
/phase4-chat.jpeg
/*.jpeg
/*.png

# Generated artifacts (not tracked)
export_test_cases/exports/
specs_planning/_internal/agent-metrics-report.md
specs_planning/_internal/test-id-registry.json

# Website (JBS IntelliQE)
website/frontend/node_modules/
website/backend/node_modules/
website/frontend/dist/
website/backend/dist/
website/frontend/.env
website/backend/.env

# -----------------------------------------------------------------------------
# Multi-tenant layout (SP-MT-01 — see PLAN_MULTI_TENANT_RESTRUCTURE)
# -----------------------------------------------------------------------------
# `clients/<id>/` is tracked — per-client source, specs, config, and docs live
# here. Per-client generated artifacts mirror the root-level rules above.

# Per-client personal env overrides (env files at client root post-2026-06-05 POM restructure)
clients/*/.env.local
clients/*/.env.*.local
clients/*/.env.server

# Per-client agent / pipeline artifacts — never ship to clients via git archive.
# These patterns live HERE (not in clients/<id>/.gitignore) so the per-client
# .gitignore stays customer-neutral (no internal terminology in shipped output).
clients/*/CLAUDE.md
clients/*/specs_planning/
clients/*/readable_externals/
clients/*/docs/
clients/*/api-testing/REQUIREMENTS_API.md
clients/*/.auth/

# Per-client private skills (mirror of specs_planning/_internal/daily-status-bank.json)
clients/*/specs_planning/_internal/daily-status-bank.json

# Per-client agent audit reports (mirror of specs_planning/audits/*.md)
clients/*/specs_planning/audits/*.md
!clients/*/specs_planning/audits/.gitkeep

# Per-client agent runtime state (mirror of specs_planning/_internal/*)
clients/*/specs_planning/_internal/agent-activity-log.md
clients/*/specs_planning/_internal/agent-performance.json
clients/*/specs_planning/_internal/agent-metrics-report.md
clients/*/specs_planning/_internal/test-id-registry.json

# Scratch / probe scripts under per-client scripts/ — underscore-prefix convention.
# Live-probe / live-verify experiments stay out of the shipped tree and out of commits.
clients/*/scripts/_*.mjs
clients/*/scripts/_*.js
clients/*/scripts/_*.ts

# Playwright CLI session debug output
.playwright-cli/page-*.yml
.playwright-cli/console-*.log
# 2026-05-26 (SUBPLAN_XLSX_PREP_01 Phase 2e): tighten patterns — prior gaps
# accumulated 4 PNG screenshots + 1 storage-state JSON as tracked-then-staged-D.
# storage-state JSON carries live Microsoft Entra session cookies; treat as
# shipping-safety, not cosmetic.
.playwright-cli/page-*.png
.playwright-cli/storage-state-*.json
# 2026-06-08 (SUBPLAN_CORP_PRICING_W15_0_RECON Phase 2.5): Export/Loc-Pricing-Export
# buttons trigger real file DOWNLOADS into .playwright-cli/ (e.g. ProductGroupOverrides-*.csv,
# LocationPricebooks-*.csv) — these carry live client pricing data; never commit (LR-049 spirit).
.playwright-cli/*.csv
.playwright-cli/*.xlsx

# Playwright CLI scratch probe scripts + session yaml/json dumps (debug-only, never tracked)
.playwright-cli/*.js
.playwright-cli/eq-*.json
.playwright-cli/regguard/
.playwright-cli/recon/
.playwright-cli/*-report.png
# Root-level CLI walk/snapshot yaml dumps (scratch)
/eq-*.yml
/pg-*.yml
/pgoverride-*.yml
# Recovery / verification scratch dir
.recover-scratch/

# Regression snapshot debug files
.claude/state/regression-snapshots/*.txt
.claude/state/regression-snapshots/*.json

# Excel lock files (auto-created when workbook open)
~$*.xlsx
~$*.xls

# Windows-path scratch artifacts from bash redirect mishaps
/C:*

# Runtime/session state — never shipped (2026-05-28; closes git check-ignore gaps:
# .claude/state/*.json|*.lock match direct children only, not these subdir/sibling cases)
.claude/scheduled_tasks.lock
.claude/state/closure-attempts/
.claude/state/regression-snapshot-*.txt

# OVR-032 CSV-assertion probe scratch (root-level; never tracked)
/capture-ovr032*.txt

# Runtime key-rotation state (root-level; regenerated, never shipped — 2026-07-17 cleanB recon)
/rotation-state.json

# Delegation / copilot / uplink orchestration infra — kept on disk, NEVER shipped to the team repo
# (a colleague's Claude is a direct WORKER, not a delegator — this infra would degrade their agent).
# 2026-07-21: untracked via `git rm --cached`; history left intact. Do not re-add.
.claude/hooks/lib/uplink/
plans/pending/PLAN_UPLINK_PROTOCOL.md
plans/pending/SUBPLAN_UPLINK_WAVE2.md
plans/pending/PLAN_LAZY_CEO_DELEGATOR.md
plans/pending/PLAN_COPILOT_INTEGRATION_ULTRAAUDIT.md
plans/pending/PLAN_COPILOT_HARNESS_PARITY_GAPS.md
plans/done/PLAN_AUDIT_COPILOT.md
plans/done/PLAN_COPILOT_CLAUDE_ABSOLUTE_PARITY.md
plans/done/SUBPLAN_REPO_02_CLAUDE_COPILOT_CONSOLIDATION.md
plans/done/SUBPLAN_REPO_13_COPILOT_ACCOUNTABILITY.md

# This remediation plan documents the scrubbed PII/markers verbatim — kept local, never shipped
plans/pending/PLAN_REPO_MARKER_AND_PAIRING_REMEDIATION.md
