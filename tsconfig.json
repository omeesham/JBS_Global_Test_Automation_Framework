/**
 * @agent-doc
 * PURPOSE: TypeScript compiler configuration for the framework. Defines compilation targets, module resolution, type checking strictness, and output settings.
 * OWNER: human-only
 * IMPACT: critical - All TypeScript compilation depends on this. Breaking it prevents builds. Type checking rules affect code quality.
 * DEPENDS-ON: None (TypeScript built-in)
 * USED-BY: TypeScript compiler (tsc), VS Code IntelliSense, npm run typecheck
 * RULES: Never disable strict checks. Keep ES2022 target for modern features. Keep paths aligned with package.json. Changes require full rebuild and typecheck.
 */

{
  "compilerOptions": {
    // Language and Environment
    "target": "ES2022",
    "lib": ["ES2022", "dom"],
    "module": "commonjs",
    "moduleResolution": "node",

    // Path aliases (multi-tenant — see PLAN_MULTI_TENANT_RESTRUCTURE).
    // SP-MT-02: aliases point to final multi-tenant layout.
    //   @framework/* resolves to framework src/
    //   @client/* resolves to clients/encore/src/ (hardcoded ACTIVE_CLIENT=encore for SP-MT-02;
    //     SP-MT-06 parameterizes this when a second client is onboarded).
    //   @client-tests/* resolves to clients/encore/tests/
    "baseUrl": "./",
    "paths": {
      "@framework/*": ["src/*"],
      "@client/*": ["clients/encore/src/*"],
      "@client-tests/*": ["clients/encore/tests/*"]
    },
    
    // Emit
    "outDir": "./.build",
    "rootDir": "./",
    "sourceMap": true,
    "removeComments": false,
    "declaration": true,
    "declarationMap": true,
    
    // Interop Constraints
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true,
    "isolatedModules": true,
    
    // Type Checking
    "strict": true,
    "noImplicitAny": true,
    "strictNullChecks": true,
    "strictFunctionTypes": true,
    "strictBindCallApply": true,
    "strictPropertyInitialization": false,
    "noImplicitThis": true,
    "alwaysStrict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    
    // Completeness
    "skipLibCheck": true,
    
    // Experimental
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    
    // Advanced
    "resolveJsonModule": true
  },
  "include": [
    "**/*.ts"
  ],
  "exclude": [
    "node_modules",
    ".build",
    "reports",
    "logs",
    "playwright-report",
    "client-delivery"
  ]
}
