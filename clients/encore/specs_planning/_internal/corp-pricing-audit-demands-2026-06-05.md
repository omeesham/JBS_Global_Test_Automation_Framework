# Corporate-Pricing Closure-Audit — Master Obligation Checklist

Derived from source by Stage A of the corp-pricing-closure-audit Workflow, 2026-06-05; NOT a hardcoded list.

Synthesized from 5 independent demand sets (A1 path-rules/Encore client rules; A2 LEARNED_RULES + AGENT_SHARED_RULES; A3 client CLAUDE.md + agent-mistakes.md; A4 export/lint/parity tooling; A5 plan-contract). 202 raw demands deduplicated into the canonical obligations below. Each row keeps the strongest (most specific / closest-to-enforcement) file:line citation; the merged source IDs are listed in the Obligation cell where multiple agents derived the same rule.

---

## spec — corporate-pricing-{search,strategy,detail}.spec.ts

| ID | Obligation | Source (file:line) | Applies-to |
|---|---|---|---|
| M-SPEC-01 | Per-test baseline reset: every CRUD/save TC in the 3 specs MUST reset baseline PER-TEST in beforeEach (not first-test-only) — retries re-run a single test + its beforeEach but never TC-001's body. Helpers must be a hardened ensureDefaultState bounded-retry that THROWS if still dirty after N cycles (clickSaveWithDialog returns success even on a disabled Save). search.spec.ts treats a fresh load as baseline. [A1-002, A3-020] | .claude/rules/specs.md:26 | spec |
| M-SPEC-02 | No hardcoded structural-count assertions (.toBe(N)/.toHaveLength(N)) unless the count IS the feature; Detail grid is ~2430 rows / virtualized — use content/behavior/existence checks (expect.poll toBeGreaterThan/toBeLessThan). LR-053 strict-row-count ban where placeholder-bug rows exist. [A1-003, A2-015, A3-024, A5-D24] | docs/read_only_docs/AGENT_SHARED_RULES.md:731 | spec |
| M-SPEC-03 | No `.toBe(true)` on boolean OR-expressions (opaque CI diagnostics) — use a branching structured-diff assertion or expect([x,y]).toContain(a). Grep the 3 specs for OR-expression .toBe(true) (boolean cells Is Internal/Labor/Active present). [A1-005, A3-026] | .claude/rules/specs.md:109 | spec |
| M-SPEC-04 | No fixed waitForTimeout(N) inside count/state polling loops — use waitForFunction / expect.poll polling the actual transition. Verify the 'No fixed waits (LR-052)' header claims against page-object source, not on faith. [A1-006, A3-026] | .claude/rules/specs.md:122 | spec |
| M-SPEC-05 | Network listeners (page.on('request')/waitForRequest/waitForResponse/page.route) that observe save behavior MUST filter on the backend API endpoint (req.url().includes('/navigator/api/'); Pricing-Detail = /navigator/api/location/pricing/save per ALL-090), NEVER a substring of the page URL — Next.js App-Router RSC/hydration POSTs fire 0-8s after reload masquerading as saves. CPR IS Next.js. Independently verify EVERY listener in 3 specs + 4 page-objects. [A1-007, A3-025] | .claude/rules/specs.md:146 | spec |
| M-SPEC-06 | No placeholder/stub tests: no test.fixme(), no empty describes, no comment-only stubs; every describe has >=1 executable test; every failure classified BUG/FEATURE_CHANGE/TEST_DEFECT/UNCERTAIN before any code change (no bug laundering). Unimplementable TCs are omitted with logged justification, never left as fixme. EXCEPTION: a deliberately-failing TC is permitted ONLY as a bug-evidence vehicle under the 3-clause gate. [A2-013, A3-021, A5-D36] | docs/read_only_docs/AGENT_SHARED_RULES.md:249 | spec |
| M-SPEC-07 | No CPR TC marked Automated without an actual test RUN — typecheck and --list are NOT runs; audit the CODE for false-green (assertions must assert PERSISTED live values after reload), regardless of recorded S1/S2/S3 counts. Post-write quality self-check mandatory on every .spec/.page edit; validate a fix by running ONLY the failing test (--grep TC-ID) THEN a full regression — never the reverse. [A2-014, A3-022, A5-D36] | clients/encore/specs_planning/_internal/agent-mistakes.md:199 | spec |
| M-SPEC-08 | Implement TC steps FAITHFULLY first — no preemptive workarounds (reload-retries, conditional blocks, loosened assertions) baked in without first proving the steps fail; any workaround MUST cite run-evidence on the same/adjacent line, and the TC definition must match the spec's actual steps. [A3-023] | clients/encore/specs_planning/_internal/agent-mistakes.md:204 | spec |
| M-SPEC-09 | Reliable interaction primitives (ALL-089/ALL-090): pressSequentially (not fill — fill bypasses maxlength and may not dirty → silent no-op save), check()/uncheck() (not bare Radix .click), wait for Save to return DISABLED before reload (save-then-reload race), assert FORMAT not value on volatile shared-office-1604 fields. Pricing-Detail single-Save batch grid: leave only the intended row dirty (one global Save commits ALL dirty rows); ensureDefaultState restores EVERY anchor touched; New-Price-only edits often don't dirty (co-dirty via Max Discount); per-test 150s describe timeout (not a fixed wait). [A3-029, A5-D33] | clients/encore/specs_planning/_internal/agent-mistakes.md:291 | spec |
| M-SPEC-10 | Radix-combobox negative-validation tests MUST NOT mutate the combobox <span> textContent (crashes the host Angular page 'Application error') — use page.route body-injection on /navigator/api/ saves, OR negative listbox enumeration (getComboboxOptions + not.toContain), OR Save-button-disabled isolation. The load-bearing observable is Save-disabled, not visible text. [A3-030] | clients/encore/specs_planning/_internal/agent-mistakes.md:288 | spec |
| M-SPEC-11 | Radix Select with 50+ options: wrap open+click in a retry loop (max 3; Escape→wait-hidden→re-open→scrollIntoViewIfNeeded→click, reduced per-attempt timeout); combobox selection on common-substring options uses exact match getByRole('option',{name,exact:true}), not :has-text contains. [A3-031] | .claude/rules/specs.md:96 | spec |
| M-SPEC-12 | Angular/React form interactions press Tab after fill to fire blur/change and reuse proven repo recipes (fillAndTab, clickSaveAndConfirm, setRadixCheckbox/check) rather than reinventing event-dispatch on a failed attempt. [A3-032] | clients/encore/specs_planning/_internal/agent-mistakes.md:175 | spec |
| M-SPEC-13 | All test data lives in clients/encore/src/data/testdata/corporate-pricing/ — every input string/number and expected value, never hardcoded in specs; shared constants in common.data.ts (structural counts and explained inline arithmetic are exempt). A spec carrying inline business test data is an audit finding. [A2-016] | docs/read_only_docs/AGENT_SHARED_RULES.md:734 | spec |
| M-SPEC-14 | CPR specs/data MUST use the authorized test entity Office 1604 only — never substitute office/account/location values unless the user explicitly named different ones. Verify hardcoded entities resolve to 1604. [A3-033] | clients/encore/specs_planning/_internal/agent-mistakes.md:66 | spec |
| M-SPEC-15 | Where a TC's expected value / selector target / field behavior conflicts with live DOM, DO NOT edit the spec to match a wrong TC — set the item to pending_planning, log the exact discrepancy, report it (truth hierarchy: DOM wins). Assert against DOM reality, not blindly against TC text. [A3-048] | clients/encore/specs_planning/_internal/agent-mistakes.md:188 | spec |
| M-SPEC-16 | FCC taxonomy instantiation: for each enumerated field, the FCC blocks MUST instantiate the per-control-type case templates (positive/BVA/negative/save-cycle). Detail numeric fields (New Price, Max Discount) require min/mid/max + min-1/max+1/decimal-step-boundary + non-numeric/multi-dot/negative + revert-disables-Save; read-only fields (Base Price col, TC-LOC-CPR-206) take no edit cases. Each case runs independently: baseline→act→save→reload→verify→cleanup. [A4-025, A4-027] | clients/encore/specs_planning/_internal/field-case-generation.md:8 | spec |
| M-SPEC-17 | FCC Tier-1 save verification (always required): every editable-field save case persists via page reload + re-navigation + DOM read of the persisted value (reloadAndNavigateTo*Tab helpers). An in-memory-UI-only assertion does NOT satisfy Tier-1. [A4-026, A5-D17] | clients/encore/specs_planning/_internal/field-case-generation.md:14 | spec |
| M-SPEC-18 | Helpers-not-gospel: every XLSX-seeded helper case MUST be verified against live DOM, every [ASSUMPTION] flag corrected, re-IDed to TC-LOC-CPR-NNN, and dropped/added per live reality — no [ASSUMPTION] survives. [A5-D06, A5-D28] | plans/pending/PLAN_CORP_PRICING_MASTER.md:51 | spec |
| M-SPEC-19 | Two distinct mutation fixtures: strategyFixture (S2 only; 2022-NP Tier 1 / 5f2a4088…, Active) and detailFixture (S3 only; 2021-PB6 / 91acb5ca…, Inactive) are DIFFERENT pricebook records, each used only by its owning screen; mutation/override tests restore via bounded-retry ensureDefaultState with zero cross-run drift under workers:2. [A1-002, A5-D08, A5-D30, A5-D32] | plans/pending/PLAN_CORP_PRICING_MASTER.md:53 | spec |
| M-SPEC-20 | TC-count completeness per screen: Search 18 (TC-LOC-CPR-001..018), Strategy 25 (101..125), Detail 20 (201..220) authored + automated; columns cover the 8 DOCX-named Search columns + row→details navigation (D4); route-param Must covered P1 (D3/F2c, NM-1440 BUILT); staging-price→New-Price mapping recorded (D6/F14). [A5-D11, A5-D12, A5-D29, A5-D30, A5-D32] | plans/done/SUBPLAN_CORP_PRICING_1445_SEARCH_P1.md:112 | spec |
| M-SPEC-21 | Every TC traces to a DOCX line OR is explicitly LABELED as inference and MCP-confirmed (F6): Strategy header 'read-only' (DOCX 138 'reference'), Detail 'no Add/Remove on existing groups' (DOCX 206), currency-format 'blocks Save with a message' (DOCX 226). 'Empty discount = no discount' is XLSX-only (not DOCX) → excluded from P1. [A5-D23, A5-D41] | plans/done/SUBPLAN_CORP_PRICING_1443_PRICING_DETAIL_P1.md:56 | spec |

## page — corporate-pricing page objects + corporate-pricing.page.ts (CorporatePricingBasePage)

| ID | Obligation | Source (file:line) | Applies-to |
|---|---|---|---|
| M-PAGE-01 | GARDENER dedup (ALL-026/ALL-020/GEN-022): any setup/nav/assertion/cleanup pattern repeated across 2+ of the 3 page-objects MUST be extracted into corporate-pricing.page.ts (CorporatePricingBasePage extends BasePage, NO .base.page.ts infix; each per-screen PO extends it). Flag EVERY un-extracted repetition; copy-pasted test blocks = defect; 3+ data-only-different TCs = data-driven describe loop. Verify signatures before calling across POs (LR-001) — wrong param = silent corruption. ZERO un-extracted repetition at closure. [A1-016, A2-004, A3-018, A3-019, A5-D18, A5-D37, A5-D50] | clients/encore/specs_planning/_internal/agent-mistakes.md:73 | page |
| M-PAGE-02 | No empty catch blocks (LR-003): every catch re-throws, sets error state, or logs+documented-fallback. detail.page.ts:210 carries a documented-fallback comment (compliant) — confirm no other catch silently swallows, especially in ensureDefaultState bounded-retry loops. [A1-017] | .claude/rules/data.md:30 | page |
| M-PAGE-03 | No raw/inline selector literals inside page-object methods (GEN-001/GEN-024/MNT-004): all selectors come from src/selectors/index.ts via getElement/getLocator keys. Flag any hardcoded data-testid/CSS literal in a CPR page object. [A3-016] | clients/encore/specs_planning/_internal/agent-mistakes.md:191 | page |
| M-PAGE-04 | Save-dialog handling (LR-012): Save dialogs are SHARED (dlgSaveChanges/btnSaveChangesConfirm) unless live-proven otherwise. CPR is React — confirm any 'Save Changes' alertdialog selector (detail.spec.ts:109-112) was live-verified and is content-anchored (getByRole('alertdialog').toContainText('Save Changes')), not a borrowed Angular testid. Distinguish the save DIALOG from the save ENDPOINT (/navigator/api/location/pricing/save, ALL-090). [A1-036, A3-017] | clients/encore/CLAUDE.md:136 | page |
| M-PAGE-05 | Boolean/grid cell reads (LR-036/GEN-028/GEN-029) MUST branch on the LIVE-verified render format (Unicode ✔ vs shadcn lucide-check SVG vs empty) and use the real HTML tag (verified via el.tagName) — textContent returns empty for BOTH states on SVG tables → silently-wrong assertions. MCP-verify per table; never assume a format. [A1-038, A3-027] | clients/encore/CLAUDE.md:151 | page |
| M-PAGE-06 | Before getColumnByHeader('X') on any CPR grid, detect duplicate column headers (field-inventory or live th text) — getColumnByHeader returns first-match only and is silently wrong on duplicates (precedent: LM History 'Currency' at col 6 AND col 64). Use index/adjacent-context access for pricing-currency columns and document each duplicate. [A3-028] | clients/encore/specs_planning/_internal/agent-mistakes.md:203 | page |
| M-PAGE-07 | Page objects extend CorporatePricingBasePage; JSDoc on all public methods; sane signatures; no business logic in selectors; typecheck clean. [A5-D39] | plans/pending/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md:78 | page |

## selector — corporate-pricing/{search,details,strategy,pricing-detail,index}.ts + selectors/index.ts

| ID | Obligation | Source (file:line) | Applies-to |
|---|---|---|---|
| M-SEL-01 | Separate selector namespaces per page (LR-017): CPR is a DIFFERENT module from per-location Pricing — MUST NOT reuse/edit selectors/locations/pricing.ts. Each partition (search/details/strategy/pricing-detail/index) has its own collision boundary, registered in selectors/index.ts buildAllSelectors() with collision detection passing; duplicate keys in spread = silent shared-selector overwrite (GEN-027). [A1-037, A3-015, A3-053, A5-D38, A5-D57] | clients/encore/CLAUDE.md:142 | selector |
| M-SEL-02 | No raw page.* in specs (POM only); selector keys follow {prefix}{PascalName} (btn/lnk/txt/drp/chk/rdo/tbl/div/nav/mod/lbl/ico); locator priority data-* > id > [data-name] > semantic > class > text > XPath; never nth-child/auto-IDs/deep class chains. Selector strategy = testid-first→role→grid-header→content-anchor; ZERO hardcoded env (currency/office/locale) in selector files. [A2-017, A5-D05, A5-D16, A5-D38] | docs/read_only_docs/AGENT_SHARED_RULES.md:746 | selector |
| M-SEL-03 | Every CSS selector in the CPR selector files (PLN-027) MUST be live-verifiable (querySelector resolves non-null on the live page) — never constructed from HTML assumptions. Verify against real DOM tags (el.tagName), NEVER derive CSS selectors from accessibility-tree element types (AX 'rows'/'cells'/'draggables' ≠ HTML tr/td); content is shadow-DOM-wrapped. [A1-014, A3-014] | clients/encore/specs_planning/_internal/agent-mistakes.md:155 | selector |
| M-SEL-04 | Missing-testid audit (LR-029) MUST be done against the LIVE DOM via a recursive shadow-root-piercing walk (document.querySelectorAll does NOT pierce shadow roots and undercounts), NEVER from selector files alone (false positives); cross-reference selector values against what EXISTS; any API-triggering interaction checks network (4xx/5xx = potential APP_BUG, never ignored). Emit corporate-pricing-missing-testids-report.md from a fresh shadow-pierced walk; verify walk-evidence §7 recommendations live, don't copy verbatim. [A1-015, A2-018, A3-013, A5-D42] | .claude/rules/inventory.md:54 | selector |
| M-SEL-05 | No networkidle in page objects/specs (LR-023/ALL-064): no waitForLoadState('networkidle') or waitUntil:'networkidle' — use waitForAngularStable() + a concrete data-loaded signal (dropdown text, grid rows, aria-checked); reloads use waitUntil:'domcontentloaded'. Grep all CPR page-objects/specs clean of networkidle. [A2-005] | docs/read_only_docs/LEARNED_RULES.md:13 | selector |

## md — corporate_pricing_{search,strategy,detail} test-cases MD

| ID | Obligation | Source (file:line) | Applies-to |
|---|---|---|---|
| M-MD-01 | Every CPR MD TC header MUST match MD_HEADER_TC_PATTERN (`## ` or `### `, then TC-LOC-CPR-NNN, then a literal colon). A header missing the trailing colon, or using a wrong `#` depth, is uncounted and breaks parity. All 63 currently match (18+25+20). [A4-003] | scripts/check-tc-parity.ts:27 | md |
| M-MD-02 | Every CPR MD TC MUST keep an explicit `**Preconditions**:` block. Independently confirmed: humanize.ts:150-169 TAB_MAP has NO 'CPR' key, so generatePreconditions falls through to 'Basic Information tab is active' for any CPR TC lacking an MD Preconditions block; shipped XLSX preconditions read from the MD column (to-xlsx.ts:231/251), so a missing/empty MD Preconditions would silently ship the wrong precondition. Closure-blocking, not take-on-faith. [A3-009, A4-013, A5-D54] | export_test_cases/humanize.ts:150 | md |
| M-MD-03 | Queue-path STRUCT gates (planner-post-complete, no --force bypass): each CPR MD consumed by a queue item MUST contain `## FIELD INVENTORY` (>=1 data row), `## Validation Rules` (may be 'N/A — reason'), and `## MCP_VERIFICATION_LOG` (with `| Date |` and `| Selector verification |` rows). Missing any exits 1. [A4-028] | scripts/planner-post-complete.ts:60 | md |
| M-MD-04 | COVERAGE-001 (queue path): CPR MD TC count MUST be >= ceil(fieldCount * 1.5) from the FIELD INVENTORY data-row count (each field needs >=save+persist + one interaction TC). Each CPR MD TC SHOULD carry an `**Automatable**:` field (Yes/No/Blocked:[reason]) within 30 lines of its header (WARN-only). [A4-029, A4-031] | scripts/planner-post-complete.ts:137 | md |
| M-MD-05 | Test-cases MD + test-plans: parity clean (check:tc-parity); FCC structural per LR-ENC-002 (MD + test-plan + XLSX land WITH the spec, never 'later'); a Selector-Mapping table present in each test-plan; if a test-plan exists, its **Updated** date >= the test-cases **Updated** date (STRUCT-004 / ad-hoc Q2). [A3-046, A5-D40, A4-031] | plans/pending/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md:79 | md |

## xlsx — encore_test_cases.xlsx (GENERATED — fix at source, never hand-edit)

| ID | Obligation | Source (file:line) | Applies-to |
|---|---|---|---|
| M-XLSX-01 | Spec↔MD↔XLSX TC parity is structural (LR-ENC-002/ALL-071): every CPR spec TC (63 P1 IDs) MUST appear as an MD `## TC-` header AND an XLSX row whose 'TC ID' cell starts with 'TC-'; `npm run check:tc-parity` MUST exit 0 and the 63 IDs must be the present-in-all-three set. setDiff(specIds, mdIds)/setDiff(specIds, xlsxIds) any non-empty = exit 1 CRITICAL = RED. Spec TC IDs must match TC_PATTERN literal form TC-LOC-CPR-NNN. The 104 MD-only TCs are planned-not-implemented (informational) but each needs an ALL-033/LR-027 classification. Never lazy-defer MD/XLSX. [A1-034, A2-001, A3-001, A3-002, A4-001, A4-002, A4-004, A5-D07] | scripts/check-tc-parity.ts:119 | xlsx |
| M-XLSX-02 | LR-ENC-004 vocab deny-list: no client-facing CPR cell may contain any banned token — framework names (Angular/Playwright/data-testid/.page.ts/.spec.ts/getByTestId/test.fixme/FormControl), bug/ticket IDs (BUG-*, NM-\d{2,5} incl. NM-1445/1440/1441/1443), spec-helper camelCase calls, DOM/backend internals, ARIA role nouns (combobox/spinbutton/tabpanel/listbox), raw aria-*/role= markup, Radix, internal tags ([FIXME]/(FCC)), test hostname (cloudapps-e2e|encoreglobal.com), HTTP verbs+endpoints, slang (app bug/flake/vanish/no-op), speculation (hypothesis/unverified/RCA/CORRECTION), any bare ISO date 202X-NN-NN. One match = build/commit/ship fail. [A1-035, A3-004, A4-005] | scripts/xlsx-lint-rules.mjs:76 | xlsx |
| M-XLSX-03 | C1 integrity (hard): no CPR row with Automation Execution=='Pass' may carry a non-empty If-Failed reason — build THROWS [Data Integrity Exception], no coercion (the join-bug guard: a blocked sibling's fixme reason must never attach to a passing test; runtime fixmes attribute to the enclosing test('TC-LOC-CPR-...') by backward-walk <=40 lines, NOT to a TC ID in the reason text). [A1-035, A3-005, A4-006, A4-014] | scripts/xlsx-lint-rules.mjs:240 | xlsx |
| M-XLSX-04 | C2 enum: every CPR row Coverage Status ∈ {Automated, Pending Automation, Manual, ''} and Automation Execution ∈ {Pass, Fail, Skipped, Blocked, ''}. C4: Manual ⇒ BOTH Execution AND Reason blank (rationale lives in MD Notes). [A3-006, A4-007, A4-008] | scripts/xlsx-lint-rules.mjs:247 | xlsx |
| M-XLSX-05 | C5: any non-empty If-Failed reason on a CPR row MUST be a client sentence of >=4 whitespace-delimited words (terse <4-word = dev shorthand = FAIL). NO CPR TC is in TERSE_REASON_ALLOWLIST (only 8 Local-Information IDs are grandfathered). [A3-007, A4-009] | scripts/xlsx-lint-rules.mjs:261 | xlsx |
| M-XLSX-06 | C6: within each CPR sheet TC IDs are UNIQUE and emitted in ascending compareTcId order (localeCompare 'en' {numeric:true}); emit-side sort (to-xlsx.ts:463) and lint-side comparator MUST stay byte-identical. A duplicate or out-of-order CPR ID = FAIL. [A4-010] | scripts/xlsx-lint-rules.mjs:278 | xlsx |
| M-XLSX-07 | C3 (warn): a CPR row with Execution=='Blocked' + blank reason emits a non-fatal warning — does not block closure, but a clean closure SHOULD carry a reason on every Blocked CPR row (live workbook reports 0 warnings). [A4-011] | scripts/xlsx-lint-rules.mjs:272 | xlsx |
| M-XLSX-08 | Disposition consistency (V2): a CPR row that is environment/data-dependent (needs a different office/account/environment, NOT an app bug) reads as 'Pending Automation' + blank Execution + an environment reason, routed via blocked-reasons.json execution:'' — NEVER 'Blocked' (which connotes app-bug-blocked). [A3-008, A4-033] | clients/encore/CLAUDE.md:121 | xlsx |
| M-XLSX-09 | Deliverable cells are GENERATED, never hand-edited: TC ID/Title/Steps/Expected/Notes/Preconditions from the 3 MD files; spec-automated 'If Failed' from spec test.fixme(true,'<reason>') strings + // FIXME comments; MD-only blocked/skipped/Manual reasons from blocked-reasons.json. Any leak fixed at SOURCE (MD/spec/blocked-reasons.json), not the .xlsx (overwritten every build). Coverage/Execution derive from `npx playwright test --list --reporter=json` in clients/encore. [A3-003, A4-012, A4-015] | clients/encore/CLAUDE.md:101 | xlsx |
| M-XLSX-10 | CPR rows route to the correct sheet by MD basename slug → toSheetName(); each slug must be <=31 chars (search=24/strategy=26/detail=24, OK). If any CPR MD is renamed to a slug >31 chars, an explicit SHEET_NAMES short-code mapping is MANDATORY or the build HALTs. Overview banner text must be free of internal vocab (e.g. no '(mode: list-only)' suffix). [A4-016, A4-032] | export_test_cases/to-xlsx.ts:97 | xlsx |
| M-XLSX-11 | New Corp-Pricing rows vs MD vs spec counts EXACT (ad-hoc Q4: XLSX module-sheet TC-row count == that module's MD `## TC-` header count, 18/25/20); new-content quality benchmarked against existing module rows (column semantics/format/vocab parity). [A4-030, A5-D35] | plans/pending/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md:74 | xlsx |
| M-XLSX-12 | LATENT housekeeping (CLASSIFY, never launder): TAB_MAP has no CPR entry (humanize.ts:150-169; to-csv.ts:498) → the RETIRED to-csv.ts:704 emits '[CSV] Unknown submodule code CPR'. Shipped XLSX preconditions read from MD Preconditions (to-xlsx.ts:231/251), so this is a latent gap, NOT deliverable corruption. Per COP-005/PLN-014 any new TC submodule code needs to-xlsx.ts subMap + lint KNOWN_SUB_CODES + humanize.ts TAB_MAP updated. Record as a Phase-2.5 Adjacent-Sweep item (DO-NOW add CPR to TAB_MAP, or SPAWN/APPEND with grep-verify) — bare deferral = HALT; never silence the warning without classification. EBUSY (Excel lock) at the final write is an environment lock, not a defect — re-run xlsx:build to green with Excel closed before declaring GREEN. [A1-035, A3-010, A5-D54] | export_test_cases/humanize.ts:150 | xlsx |
| M-XLSX-13 | The CPR deliverable + its XLSX rows ship ONLY via `npm run client:ship` (git archive HEAD), never cp -r/tar/zip; the ship-time verify-no-forbidden deny-list lints the workbook on a CLEAN git-archive extract and hard-fails on leak. Confirm no CPR client-facing artifact carries deny-list content. [A1-028, A3-052] | .claude/rules/pipeline.md:321 | xlsx |
| M-XLSX-14 | The same xlsx-lint-rules.mjs runs at THREE gates (DRY): build-time self-fail (to-xlsx.ts re-lints its own output), commit-time (pre-commit step 5b), ship-time (verify-no-forbidden). A CPR vocab/integrity violation blocks all three. xlsx:lint exit 0=clean / 1=violation / 2=missing. Re-verify xlsx:lint exit 0 against the REBUILT (post-EBUSY) workbook. [A4-017, A4-018] | scripts/xlsx-vocab-lint.mjs:20 | xlsx |

## process — runs, verification ceremonies, browser-tool, identity, activity-log

| ID | Obligation | Source (file:line) | Applies-to |
|---|---|---|---|
| M-PROC-01 | Run-all-is-the-only-truth (LR-018): prove the suite green by running ALL 3 specs together, then each failing spec individually (classify run-all-only vs always-fails), fix always-fails first, re-run individually, run-all AGAIN to catch serial contamination, iterate until run-all green. Per-spec green counts (18+25+20) are NOT closure evidence; only a clean run-all-together result is. Use the FILENAME filter `npm test -- corporate-pricing` under .env.local; `npx playwright test --list` runs from clients/encore/ (0 from repo root). [A1-001, A2-002, A3-051, A5-D26, A5-D47] | .claude/rules/specs.md:12 | process |
| M-PROC-02 | Clean-before-judgment + run-twice (LR-024/ALL-070): clean ALL artifacts (`npm run clean`, clear .auth) and run FRESH before ANY RCA/verdict; never re-run a failing spec before completing RCA Steps 1-3 (overwrites failure-summary.json/error-context.md/screenshots); run a flaky spec TWICE to confirm deterministic vs intermittent. The verified-clean full run must be confirmed THIS run (dirs gone), counts from the deduped this-run summary.json, never stale stdout. [A1-004, A2-003, A3-050, A5-D53] | .claude/rules/specs.md:75 | process |
| M-PROC-03 | Browser-tool announcement (LR-038/LR-054): every browser-interacting closure step (LR-007 live spot-check; LR-029 shadow-pierced testid walk) declares BrowserTool=cli (catalog/locator/unattended class — CLI primary; Chrome only for visual/CSS or an explicit in-execution pause: step, neither applies). Log choice+reason. No manufactured 'CLI can't do live interaction' / 'MFA might fire' HALT — the automation user has no second factor; CPR runs locally vs .env.local (LR-ENC-003/ALL-077), never CI_ENV=e2e locally; a HALT needs ALL THREE LR-039 preconditions (creds missing AND MFA documented-active AND headed CLI unavailable), which is FALSE for Encore. [A1-019, A3-049] | .claude/rules/browser-tool.md:23 | process |
| M-PROC-04 | Plan-claim verification before closure (LR-020): the master's TC counts, file references, and cross-plan filenames verified against the live tree; the 63 CPR P1 IDs resolve via `npx playwright test --list` and are spec↔MD↔XLSX 1:1 — don't trust the global parity number alone. Evidence-based fixes/verdicts (R08/ALL-046): each cites `RCA | TC-XXX | category | evidence: <file:field> | fix: ...`; error message = source of truth; any verdict not backed by a concrete read = UNVERIFIABLE = RED (override cannot launder a missing artifact). [A1-020, A2-012, A4-030] | .claude/rules/pipeline.md:12 | process |
| M-PROC-05 | Inheritance verification (ALL-028/R15/ALL-072): before grading any inherited artifact (orchestrator inputs, S1/S2/S3 records, parity/lint results) read it fully, spot-check >=3 claims against source/live DOM, fix-or-escalate false claims, NEVER propagate unverified; treat every 'blocked/impossible/latent' handoff claim as UNRELIABLE until independently re-verified (LR-039 READ side). Before auditing CPR, search agent-mistakes.md for ALL rule prefixes filtered to this module (esp. ALL-090/ALL-088). [A2-008, A2-011, A3-047] | docs/read_only_docs/AGENT_SHARED_RULES.md:219 | process |
| M-PROC-06 | Critical self-audit, evidence-cited (ALL-030/ALL-054/AUD-001): the COMPLETE-phase self-audit asks 'what did I get WRONG?', produces a reconciliation table (>=3 rows claim|evidence-source|verified-result), justifies any zero-finding on 3+-step work as suspicious, and cites the actual value not pass/fail ('63 in spec, 63 in MD+XLSX (TC-LOC-CPR-001..018/101..125/201..220)', not 'parity matches'); for each SKIP'd/Blocked TC verify the ERROR condition was actually exercised, not the valid state read. [A2-010, A3-037] | docs/read_only_docs/AGENT_SHARED_RULES.md:238 | process |
| M-PROC-07 | Activity-log integrity (LR-028/LR-037/R06): before session end append a row to clients/encore/specs_planning/_internal/agent-activity-log.md (`| timestamp | agent | done | files | DESCRIPTION |`) with a wall-clock timestamp >= the latest mtime (and git-commit time if tracked) of EVERY file in its Files column — no backdating (1-min tolerance); validate via `npm run validate:activity-log:preflight`. Missing row = invisible session = audit finding; OWNER edits to pipeline-owned paths still require a row (keys on FILES). Unexpected behaviors (CPR-DETAIL-BUG-A, the React-vs-Angular comment defect) go to agent-mistakes.md. [A1-030, A2-006, A3-045] | .claude/rules/pipeline.md:50 | process |
| M-PROC-08 | Identity discipline (LR-043): run `node scripts/check-subplan-identity.mjs <plan>` before TodoWrite (Phase 0.1 HALT on exit 1); every Edit/Write to a pipeline-owned path is gated against the ground-truth identity's §2 (last /identity invocation, not banner text). OWNER short-circuited; pipeline identities (WATCHDOG/GARDENER) §2-gated — a GARDENER dedup edit to page-objects must fall in GARDENER's §2 or use the [OVERRIDE-REQUEST]+user-authorization one-shot handshake. [A1-033] | .claude/rules/hooks-identity.md:46 | process |
| M-PROC-09 | TodoWrite tagging contract + 8 ceremony obligations: the /execute closure enumerates and tags all 8 ceremonies — Phase 0 context-load, Phase 0.1 identity↔§2, Phase 0.5 /relevant+LR injection, Phase 2.5 Adjacent-Sweep (DO-NOW/SPAWN/APPEND with grep-verify; bare deferral=HALT), Phase 3.5 finalization (Status DONE + Execution Summary + git mv + plans:reindex + parent-cascade), Activity-log row, /final-q exit, Per-Identity Matrix Closure Audit. Every entry carries a closed-taxonomy tag ([/skill:matchtype] / LR-NNN(reason) / [manual](reason) / [ceremony]). [A1-029] | .claude/rules/pipeline.md:200 | process |
| M-PROC-10 | Stage A: the demand checklist is DERIVED from source (one agent per source A1-A5), every obligation carries a file:line citation, deduped into ONE master checklist emitted to corp-pricing-audit-demands-2026-06-05.md — NOT a hardcoded list. [A5-D43] | plans/pending/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md:65 | process |
| M-PROC-11 | Stage C: EVERY Stage-B PASS must survive >=3 independent, perspective-diverse skeptics prompted to REFUTE 'this corner is clean' (default refuted if uncertain; majority-refute ⇒ downgrade to DEFECT); every FAIL confirmed real. [A5-D44] | plans/pending/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md:83 | process |
| M-PROC-12 | The closure audit runs as a SEPARATE session from the builders (AUD-017: an auditor never self-grades same-session work — two signals true [activity-log row <6h same identity + target file, AND self-authored audit/Signed heading] = HALT; remediation = external WATCHDOG writes findings into its OWN PLAN_..._AUDIT.md, never back into the deliverable). Workflow subagents are Opus-class, fresh-context, NEVER Haiku (audit = judgment). [A2-009, A3-038, A5-D52] | docs/read_only_docs/AGENT_SHARED_RULES.md:752 | process |
| M-PROC-13 | All identities, every screen: HUNTER→GIVER→BUILDER→HEALER→WATCHDOG→GARDENER, no identity skipped; FCC parity structural (MD+test-plan+XLSX land in the SAME change as the spec). Phase 0.5 walk-through MANDATORY before code; Divergence Ledger D1-D8 confirmed against live DOM in S0 before any dependent P1 assertion (F19); each child's GIVER steps reflect the ledger (S0 corrected D3=1440 BUILT, confirmed D5=History absent). [A5-D07, A5-D55] | plans/pending/PLAN_CORP_PRICING_MASTER.md:152 | process |

## divergence — baseline-truth classification + field-inventory artifacts

| ID | Obligation | Source (file:line) | Applies-to |
|---|---|---|---|
| M-DIV-01 | Baseline-truth + truth hierarchy (ALL-024/ALL-078/LR-ENC-001): old-site DOM > live new-site DOM > artifacts > REQUIREMENTS > test plans > test cases. CPR records baselineScope: baseline-absent (net-new, no nav2 equivalent, user-confirmed) — explicitly ALLOWED, NOT a HALT and NOT a manufactured baseline diff. Every observed-vs-intended divergence is classified (regression-from-baseline / intentional-UX-with-justification / baseline-absent) and routed via /encore-questions; baseline-absent items flag for clarification, never silently encoded as expected behavior. The intent oracle is DOCX NM-1445, not nav2. [A1-018, A2-019, A3-011, A3-012, A4-024] | clients/encore/specs_planning/_internal/agent-mistakes.md:77 | divergence |
| M-DIV-02 | Angular-rule applicability (net-new-module flag): CPR is React/Next.js + shadcn DataTable, NOT Angular (search.spec.ts:16; master 75; PLAN 78). LR-009 (recovery≠original-value), LR-010 (aria-invalid expect.poll cross-field), LR-011 (reload to clear NaN model corruption) are Angular-form-internal and do NOT structurally apply — closure must NOT demand Angular cross-field/aria-invalid polling semantics here. The framework-agnostic 'save-success ≠ pristine / reload+re-read is load-bearing' semantic (LR-026 manifestation #1) DOES apply and is correctly used (detail.page.ts:196/210/223; strategy.page.ts:244/272-273). [A1-008, A1-009] | .claude/rules/angular.md:13 | divergence |
| M-DIV-03 | FACTUAL-INCONSISTENCY DEFECT to flag: three artifacts label the dirty-state mechanism 'Angular' on a module proven React/Next.js — strategy.spec.ts:12, strategy.page.ts:273 ('Angular dirty…'), and the strategy/detail header 'Angular' wording. The underlying CODE (reload+re-read) is correct, but the 'Angular' attribution is a factual error that will mislead the Council/maintainers — require correction to framework-neutral wording ('form dirty/save handled defensively' / 'shadcn/React dirty-state'). [A1-010] | .claude/rules/angular.md:36 | divergence |
| M-DIV-04 | Field-inventory artifact compliance (LR-013/LR-007/PLN-049): corporate-pricing-{search,strategy,detail}-2026-06-05.md exist (0 days stale) with all 8 frontmatter keys (Module/Client/MCP_Session_Date/MCP_Session_Tool∈{Claude in Chrome,Playwright MCP}/MCP_Tool_Reason/Author_Identity∈{GIVER,OWNER,WATCHDOG}/Page_URL/Test_Entity) and all 7 mandatory sections in fixed order (URL(s) visited / Live-state caveat / Field Inventory / Labels+Section Names / Save-cycle observations / Known App Bugs / Staleness signal); MCP_Session_Date == filename date. The Field Inventory table uses the exact 8-col header. Spot-check 2-3 random fields per screen on live DOM (LR-007); drift = full re-walk + refreshed artifact. [A1-011, A3-034, A4-019, A4-020, A4-022] | clients/encore/specs_planning/_internal/field-inventory-spec.md:44 | divergence |
| M-DIV-05 | Field-inventory testid completeness (LR-014): every Field Inventory row has a non-empty data-testid OR the literal fallback `(none) — use aria-label="<X>"` / `(none) — use text content` — never blank (blank forces selector guessing). CPR is sparse-testid (Search 3-5 generic e2e-*; Strategy/Detail 0; shadow-DOM-wrapped) so the fallback is expected on many rows but must be the literal string. [A1-012, A3-035, A4-021] | clients/encore/specs_planning/_internal/field-inventory-spec.md:92 | divergence |
| M-DIV-06 | Baseline_Artifact key is OPTIONAL for CPR (baselineScope: baseline-absent, no nav2 equivalent — mandatory only when scope is full/baseline-partial); the SP-AAE-02 hook does NOT enforce it. The search inventory includes it (→ old-site-baseline/corporate-pricing-2026-06-05.md, baselineScope: baseline-absent) but its absence on strategy/detail is NOT a violation. [A4-023, A5-D04] | clients/encore/specs_planning/_internal/field-inventory-spec.md:57 | divergence |
| M-DIV-07 | Default field values, enabled/disabled states, dropdown option lists, column headers, and row counts in test data/assertions (LR-015/PLN-020/PLN-023) come from a dated live-DOM read (the field-inventory date) — never from REQUIREMENTS/Jira/test plans, never approximate ('~55 rows'). Confirm test-data constants trace to the 2026-06-05 inventories. [A1-013, A3-013, A3-036] | .claude/rules/inventory.md:44 | divergence |
| M-DIV-08 | Divergence Ledger D1-D8 each RAISED via /encore-questions, not silently absorbed (Doctrine 2): D1 (8 DOCX cols ↔ 9 live, Is Productions+Currency split) — cover all 8, assert live count, raise the split; D2 (filtering client-vs-server) network-verified (live = SERVER-SIDE on Search) and recorded; D5 (3 DOCX mgmt tabs ↔ 2 live, no History) — assert 2, raise History-absent (CPR-STRAT-Q1, routes to gated stub 1444); plus CPR-DETAIL-Q3 (flat grid/no hierarchy) and CPR-DETAIL-Q4 (Price→override not pinned). D6 doc→live header mapping recorded (Base Price→Price, Override Price→New Price, Override Discount→Max Discount, staging-price→New Price). [A5-D02, A5-D09, A5-D10, A5-D13, A5-D14, A5-D34, A5-D41] | plans/pending/PLAN_CORP_PRICING_MASTER.md:48 | divergence |
| M-DIV-09 | CPR-DETAIL-BUG-A documented-not-absorbed: editing New Price alone often does NOT enable Save; Max Discount reliably does — documented in field-inventory §Known App Bugs + test-cases Clarifications (Doctrine 2), with the save-cycle dirty lever (Max Discount) honest; not yet a BUG-*.json (to be confirmed/filed in FCC-P2 per LR-044). [A5-D33] | plans/done/SUBPLAN_CORP_PRICING_1443_PRICING_DETAIL_P1.md:149 | divergence |
| M-DIV-10 | F2d (unbuilt destinations): only genuinely-unbuilt destination pages may be deferred citing DOCX line 22 'URL: TBD' (the Price-Override navigation destination); its presence/affordance is still covered. [A5-D21] | plans/done/SUBPLAN_CORP_PRICING_1445_SEARCH_P1.md:22 | divergence |

## closure — SUBPLAN_CORP_PRICING_99 closing gates + Stage-D verdict

| ID | Obligation | Source (file:line) | Applies-to |
|---|---|---|---|
| M-CLOSE-01 | LR-040 closure-gate completeness: EVERY enumerated item (each P1 TC, each deferred FCC item, each divergence D1-D8) classified (a) MCP-proven with cited timestamp+row-diff, (b) inference with a GREP-VERIFIABLE line item in a NAMED existing pending/done subplan (the Wave-2 FCC stubs SUBPLAN_CORP_PRICING_1445_SEARCH_FCC_P2 / 1441_STRATEGY_FCC_P2 / 1443_DETAIL_FCC_P2 must EXIST in plans/pending with grep-verifiable items — verify before closing), or (c) user-flagged bug-candidate ID + Pending-decisions entry. Bare 'TRACKED by inference'/'scope-pushed' = phantom-handoff finding; any unclassifiable item = HALT. ALL-033/LR-027 justifications (NOT-AUTOMATABLE/DEFERRED/APP-BUG/MERGED) per dropped TC, incl. the 104 MD-only and Wave-2 FCC deferrals (007/008/030 search, numeric Detail). [A1-021, A1-023, A3-040, A3-041, A5-D03] | .claude/rules/pipeline.md:63 | closure |
| M-CLOSE-02 | LR-046 strict-line discipline: any strict numeric/boolean plan line (18/18, 25/25, 20/20, all 63 P1, parity clean, zero lint hits; closure's zero-UNVERIFIABLE / zero-ALL-026-repetitions / zero-hardcoded-env / zero-data-testid-assumptions / zero-test.fixme) satisfied AS WRITTEN — if live state needs materially more work, HALT-and-ASK the user; do NOT rescope via APPEND/SPAWN/DO-NOW (forbidden even when a recipient exists). A strict line at N>0 cannot be closed by creating a recipient subplan; rescoping without prior user authorization = automatic RED. Re-verify the S1/S2/S3 'green ×2'/'green ×full-suite' strict lines, don't inherit. [A1-025, A3-042, A5-D49, A5-D58] | .claude/rules/pipeline.md:138 | closure |
| M-CLOSE-03 | LR-027 execution-summary + parent-cascade: before git mv to done/, set Status: DONE + Executed date + an Execution Summary (TCs implemented count+IDs / TCs dropped with per-TC justification / MCP verification results / doc changes / dated pass confirmation). Annotate each child's DONE line inside PLAN_CORP_PRICING_MASTER.md body (parent in pending) as `- [child.md](../done/child.md) — **DONE 2026-06-05**, <summary>`. The master carries an explicit do-NOT-flip-parent directive (F16 — Wave-2 FCC + edge + gated stubs still pending) so the auto-CLOSE of the parent is SUPPRESSED, but the per-child annotation is NOT suppressed; honor both. git mv only the closed Wave-1 children (S1/S2/S3 + S4); plans:reindex; validate-closure:changed C1-C6 PASS on the closed children. [A1-021, A1-022, A3-043, A5-D51] | .claude/rules/pipeline.md:46 | closure |
| M-CLOSE-04 | LR-041 model/thinking/permissionmode: the closing subplan declares **Model** (claude-opus-4-8 or claude-sonnet-4-6), **Thinking** (closure/multi-rule-judgment = Opus xhi/max + a **Justification** line), and **PermissionMode**; forbidden combos (Sonnet lo/max, Opus lo/mid) not used. [A1-024] | .claude/rules/pipeline.md:86 | closure |
| M-CLOSE-05 | LR-048 Per-Identity Satisfaction Matrix (REQUIRED — touches .spec.ts + test-cases.md + XLSX): a `## Per-Identity Satisfaction` table, one row per identity (HUNTER/GIVER/BUILDER/HEALER/WATCHDOG/GARDENER), each Concrete-deliverable cell EXACTLY one of: an existing repo-relative file path, `(skipped: <reason >=20 chars>)`, or `(none)`. Vague prose ('spot-check log', 'parity outputs', 'proof of work') REJECTED at authoring + closure-check C6. Every non-(none) cell's Acceptance command appears in Phase 3.5 with evidence-emission format. If GARDENER extracts a method, replace its (skipped:…) cell with the actual file path (F13). [A1-026, A3-044, A5-D56] | .claude/rules/pipeline.md:270 | closure |
| M-CLOSE-06 | LR-048 Structural Minimum: the closing subplan contains, in order — Title+Frontmatter (incl. BrowserTool), Context, Bootstrap (every dependency rule + parent plan path), Phase 0 dependency+browser gate, Phase 0.5b baseline-first walk (REQUIRED for WATCHDOG/audit; baselineScope: baseline-absent allowed), Phase 1+, Per-Identity Matrix, Acceptance criteria (classified (a)/(b)/(c) per LR-040), Handoff (chat-only). Missing Phase 0.5b on a WATCHDOG/audit subplan = violation. [A1-027] | .claude/rules/pipeline.md:254 | closure |
| M-CLOSE-07 | LR-055 machine-gated closure: Status: DONE (and any parent annotation edit) requires validate-plan-closure.mjs PASS — C1 (no forbidden incompleteness tokens, e.g. NOT-WALKED/'not captured'), C2 (Execution Summary skeleton present), C3 (every cited artifact path exists on disk — field-inventories, baseline, walk-evidence, missing-testid report all resolve), C4 (no phantom/circular handoff + parent-cascade annotation present in pending parent), C5 (strict-line vs deviation axis-match), C6 (Per-Identity Matrix cells = existing-file-path / (skipped:>=20) / (none)). C2-C6 NON-overridable; verify Depends-on/skill cites via --content-from-stdin dry-run BEFORE the flip (truncated-error avoidance). [A1-031] | .claude/rules/plan-closure.md:13 | closure |
| M-CLOSE-08 | LR-042 /final-q non-skippable exit with v2 evidence-emission: end with a `## /final-q audit` heading carrying **Verdict**: GREEN|YELLOW|RED, every cross-check stating 'ran <cmd> → output: <snippet>'; no prose 'done' summary in place of /final-q; if hook-failures.log is non-empty the verdict floors to YELLOW; for a chain-spawned child, absence of a parseable verdict pauses the orchestrator. [A1-032] | .claude/rules/hooks-identity.md:16 | closure |
| M-CLOSE-09 | AUD-018 resolution-label honesty: in the audit resolution table distinguish PATCHED (root cause fixed) from DOCUMENTED (gap acknowledged in prose, not fixed) from DEFERRED / NOT-TESTABLE — never PATCHED as a catch-all; a finding labeled PATCHED that only adds a prose caveat is a mis-label hiding residual risk. [A3-039] | clients/encore/specs_planning/_internal/agent-mistakes.md:254 | closure |
| M-CLOSE-10 | Stage D DO-OR-DIE gate: aggregate every obligation to VERIFIED-CLEAN | DEFECT | UNVERIFIABLE. ANY DEFECT or ANY UNVERIFIABLE = automatic RED = HALT = no closure (no rounding up to YELLOW); re-run the WHOLE workflow after fixes until 100% VERIFIED-CLEAN. Verdict emitted to corp-pricing-closure-audit-2026-06-05.md. Override authorizes only honest classification of ALREADY-gathered evidence — it can NEVER convert a missing artifact / phantom handoff / unmet strict line / EBUSY-blocked build into evidence (§19.5 override non-sticky, single-invocation, logged, explicit-only). [A2-020, A5-D45, A5-D48] | plans/pending/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md:85 | closure |
| M-CLOSE-11 | Verdict floor: workflow RED ⇒ Phases 2 + 3 do NOT run; master not annotated; NO child moved to done/. Enforce the floor structurally. [A5-D46] | plans/pending/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md:137 | closure |
| M-CLOSE-12 | Closure Acceptance run-gate: `npm test -- corporate-pricing` green from a VERIFIED-CLEAN run with counts taken ONLY from THIS run's deduped summary.json; check:tc-parity exit 0 across all three screens; xlsx:build + xlsx:lint clean; every obligation has a verdict; ZERO UNVERIFIABLE permitted. [A5-D47, A5-D48] | plans/pending/SUBPLAN_CORP_PRICING_99_AUDIT_CLOSURE.md:128 | closure |
