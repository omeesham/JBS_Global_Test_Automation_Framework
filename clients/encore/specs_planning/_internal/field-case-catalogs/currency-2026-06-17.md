# Field-Case Catalog — Currency

**Subplan**: SUBPLAN_CURRENCY_FCC.md (Phase 2)
**Identity**: GIVER
**Date**: 2026-06-17
**Inputs**: `field-inventories/currency-2026-06-17.md`, `false-green-sweeps/currency-2026-06-17.md`, `rca-currency-baseline-remediation-2026-06-17.md`, `tests/locations/location-currency.spec.ts` (27 runtime TCs)
**Fields under test**: 3 read-only Currency Code cells (USD/CAD/MXN); 3 Selected checkboxes; 3 Is-Default checkboxes (enabled only when Selected); 3 Merchant comboboxes (USD=2 opts, CAD=1, MXN=0); 3 grid rules (cascade-enable, single-default mutual-exclusion, at-least-one-selected). No text/numeric/date/file fields, no multi-row FormArray (fixed 3 rows).

## Final outcome: **1 net-new TC** (TC-LOC-CUR-028)

The 27 documented TCs provide near-complete (surface × case) coverage. The **one genuinely-uncovered assertion** is the LR-009/LR-026 *revert-to-saved → Save re-disables* (net-zero smart-diff): TC-015 toggles a checkbox and reverts it but never asserts Save returns to disabled; no other TC asserts post-revert Save-disable. The plan's own §2.1 gap matrix lists this cell as `(a) net-new`; the sibling Auto-Add-On suite carries it (its TC-005) while Currency lacks it. One behavior test (no save) fills it. USD-merchant save-cycle (the plan's other illustrative net-new) is already (b) — TC-022 proves change→save→reload→restore.

## Step 2.0b — Coverage ledger (de-dup oracle; every runtime TC → proven outcomes)

| TC | What it proves (end-assertions) | Surface(s) |
|---|---|---|
| TC-001 | grid renders 3 rows + 4 column headers (count = feature) | grid structure (+ inline baseline reset) |
| TC-002 | USD default after reload: Selected✓, IsDefault✓, Merchant=316370 | USD Selected/IsDefault/Merchant default |
| TC-003 (loop CAD) | CAD default: Selected✗, IsDefault disabled | CAD Selected/IsDefault default |
| TC-004 (loop MXN) | MXN default: Selected✗, IsDefault disabled | MXN Selected/IsDefault default |
| TC-005 | select CAD → CAD IsDefault enables (cascade) | grid rule: cascade-enable |
| TC-006 | set CAD default → USD default auto-unchecks (single-default) | grid rule: single-default |
| TC-007 | unselect CAD → CAD IsDefault disabled + unchecked | grid rule: unselect-disables-IsDefault |
| TC-008 | USD Merchant = 2 options incl. 316370 + 316426 | USD Merchant options |
| TC-009 | CAD Merchant = 1 option (316446) | CAD Merchant options |
| TC-010 | MXN select → MXN IsDefault enables+checks; MXN Merchant "No Matches Found" | MXN IsDefault cascade + MXN Merchant empty |
| TC-011 | select CAD merchant → field value updates (in-memory) | CAD Merchant select |
| TC-012 | CAD merchant value persists when CAD unselected (in-memory) | CAD Merchant ↔ Selected independence |
| TC-013 | uncheck USD (only selected) → Save disabled; USD IsDefault disabled | grid rule: at-least-one-selected |
| TC-014 | save w/o default → "save-changes" dialog (not error); cancel | Save dialog (no-default) |
| TC-015 | Save disabled initially; enabled after CAD check | Selected→Save-enable |
| TC-016 | USD merchant → alternate (316426) value updates → back to 316370 (in-memory) | USD Merchant change (in-memory) |
| TC-017 | multi-currency selected w/o default → "save-changes" dialog | Save dialog (multi, no-default) |
| TC-018 | Currency Code cell read-only (no editable input) | read-only Currency Code |
| TC-019 | merchant dropdown accessible for unselected currency | CAD Merchant accessibility |
| TC-020 | all three currencies Selected simultaneously | multi-select |
| TC-021 | CAD Selected persists after save+reload | CAD Selected persist |
| TC-022 | **USD Merchant → 316426 persists after save+reload, then restore** | **USD Merchant save-cycle** |
| TC-023 | CAD IsDefault + USD-default-clear cascade persists after save+reload | IsDefault persist + cascade persist |
| TC-024 | CAD Selected + USD Merchant combined persist after single save+reload | combined save persist |
| TC-025 | cancel save-changes dialog → reload shows CAD unchecked (discard) | cancel = no-persist |
| TC-026 | beforeunload dialog fires on dirty-form reload | dirty-state beforeunload |
| TC-027 | no-default state (USD IsDefault unchecked) persists after save+reload | no-default persist |

## Step 2.1 — Exhaustive (surface × case) gap matrix

Cell classification: **(a)** implement net-new · **(b)** already-covered-by-outcome (cite TC) · **(c)** not-applicable / deferred (cite reason). STRICT-LINE-A: no cell omitted. STRICT-LINE-B: de-dup by PROVEN OUTCOME.

### Selected checkboxes (USD / CAD / MXN)

| Surface | default-state | toggle→Save-enables | check-persist (save+reload) | uncheck-persist (save+reload) | revert→Save-disables (LR-009/026) |
|---|---|---|---|---|---|
| Selected USD | (b) TC-002 | (b) at-least-one is the USD-toggle behavior (TC-013) | (b) USD selected is the baseline, proven across all save+reload TCs (021-024,027) | (c) cannot save 0 currencies — at-least-one guard (TC-013); uncheck-USD-persist is structurally impossible | **(a→028)** form-level smart-diff, proven once on CAD |
| Selected CAD | (b) TC-003 | (b) TC-015 | (b) TC-021 | (b) TC-021 cleanup (uncheck CAD → save) + TC-024 cleanup | **(a) TC-LOC-CUR-028** — the proving instance |
| Selected MXN | (b) TC-004 | (b) TC-010 + TC-020 | (b)-by-outcome — same form-level persist mechanic as CAD (TC-021); MXN has no merchant so a dedicated persist adds no new outcome | (b)-by-outcome (TC-020 cleanup) | (b)-by-outcome (028 proves form-level) |

### Is-Default checkboxes (USD / CAD / MXN)

| Surface | default | cascade-enable (Selected→IsDefault) | single-default (check clears others) | disable+uncheck on unselect | persist (save+reload) |
|---|---|---|---|---|---|
| IsDefault USD | (b) TC-002 (checked) | (b) USD selected at baseline → enabled (TC-002) | (b) TC-006/TC-023 (USD cleared when CAD set) | (b) TC-013 (uncheck USD → IsDefault disabled) | (b) TC-027 (no-default persist) + TC-023 (USD-clear persist) |
| IsDefault CAD | (b) TC-003 (disabled) | (b) TC-005 | (b) TC-006 | (b) TC-007 | (b) TC-023 |
| IsDefault MXN | (b) TC-004 (disabled) | (b) TC-010 | (b)-by-outcome — same single-default mechanic (TC-006/023) | (b)-by-outcome (TC-007 mechanic) | (b)-by-outcome (TC-023 mechanic) |

### Merchant comboboxes

| Surface | option-list | select→update | persist when unselected | save-cycle (select-alt→save→reload→restore) |
|---|---|---|---|---|
| USD Merchant (2 opts) | (b) TC-008 | (b) TC-016 | n/a (always selected) | **(b) TC-022** — already proven (the plan's illustrative net-new is already covered) |
| CAD Merchant (1 opt) | (b) TC-009 | (b) TC-011 | (b) TC-012 | **(c) deferred** — 1 option, no alternate to round-trip (steering #2; data-thin/office-seeded) |
| MXN Merchant (0 opts) | (b) TC-010 (No Matches) | n/a (no options) | n/a | **(c) deferred** — 0 options (steering #2) |

### Grid rules (cross-field)

| Rule | Verdict | Discharging TC |
|---|---|---|
| cascade-enable (Selected→IsDefault) | (b) | TC-005 (+ live walk 2026-06-17) |
| single-default mutual-exclusion | (b) | TC-006, TC-023 (+ live walk) |
| at-least-one-selected (Save-disable guard) | (b) | TC-013 (+ live walk: uncheck USD → Save disabled, escapable) |
| unselect-disables-IsDefault | (b) | TC-007 |

### Read-only + form-level + state cases

| Case | Verdict | Discharging TC |
|---|---|---|
| Currency Code read-only (`affordance: none`, LR-057) | (b) | TC-018 + live click-probe 2026-06-17 (0 interactive descendants ×3 cells) |
| Save dialog appears (no-default / multi) | (b) | TC-014, TC-017 |
| Cancel save discards (round-trip) | (b) | TC-025 |
| beforeunload on dirty reload | (b) | TC-026 |
| combined multi-field single-save persist | (b) | TC-024 |
| all-three-selected | (b) | TC-020 |
| **revert checkbox to saved → Save re-disables (net-zero smart-diff)** | **(a) TC-LOC-CUR-028** | **net-new** — absent from ledger |

**STRICT-LINE-G** (max-coverage floor): every applicable cell resolves to (a) net-new [1], (b) cited [all others], or (c) named deferral [CAD/MXN merchant save-cycle; USD uncheck-persist structurally impossible]. No unclassified cell.

## Step 2.2 — Net-new TC catalog

| ID | Label | Surface | Case-class | Shape | Expected outcome | Restore obligation |
|---|---|---|---|---|---|---|
| TC-LOC-CUR-028 | Reverting a currency selection to its saved state re-disables Save (no net change) | Selected CAD (form-level smart-diff) | revert→Save-disables (LR-009/026) | behavior (no save) | from enforced baseline: check CAD → Save enabled (poll); uncheck CAD (revert to saved) → Save returns to disabled (poll) | none — no save issued; `beforeEach` re-enforces baseline next test (STRICT-LINE-F n/a — non-persisting) |

- **Shape**: ordinary `test()` with `expect.poll` (LR-052) — NOT `saveAndVerifyCase` (no save-cycle) → `saveAndVerifyCase` import remains absent; `@fcc` tag absent.
- **De-dup (STRICT-LINE-B)**: proves the form-level smart-diff revert→disable outcome once; USD/MXN checkbox-revert and merchant-revert are (b)-by-outcome (same form-level mechanic).

## Bug candidates

None. No live-walk divergence (2026-06-17) survived the LR-044 oracle (old-site nav2 parity + REQUIREMENTS-level expectation). No `BUG-LOC-CUR-NNN` filed.

## Manual-TC reconciliation (steering #1)

MD header claimed `27 / 25 automated / 2 manual`. Per-TC `Status` column: all 27 = `✅ Automated`. The "2 manual" is stale arithmetic (the 2 loop-parametrized TC-003/004 ARE automated). **Header corrected to `28 / 28 / 0`** (27 existing automated + TC-028 net-new). No genuinely-manual TC exists; nothing to attempt-automate.

## Net-new TC list

**TC-LOC-CUR-028** (1 net-new behavior TC). USD-merchant save-cycle = (b) TC-022 (not net-new); CAD/MXN merchant save-cycle = (c) deferred (steering #2).
