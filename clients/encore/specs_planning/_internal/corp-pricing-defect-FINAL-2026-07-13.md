## RECEIPT

RED=2 YELLOW=5 GREEN/DROPPED=1 MISSING-NEW=0 UNCHECKED-JIRA=6

- A10 RED: Max Discount >100 on Pricing Detail is confirmed as a validation/focus trap; `333` sets `aria-invalid=true`, Save is disabled, no validation text appears, and focus stays trapped after another-cell click.
- A11 GREEN/DROPPED: New Price high value is not a confirmed defect; `5646464` sets `aria-invalid=true` and blocks Save, so no persistence/shift symptom reproduced.
- A12 RED: Product Group Override for office 1604 is confirmed broken; grid API returns HTTP 500 duplicate key 4543 while 1606 returns 200 and export still has 731 rows for 1604.
- C4 YELLOW: Pricing Detail renders a flat 2,430-row grid plus 3,707-item source list with no page-size control; behaves as reported but needs product/design confirmation.
- C5 YELLOW: Price Year accepts decimal/short values and allows Save enabled, but Save click is a silent no-op with no dialog, URL change, validation message, or API call.
- C6 YELLOW: No delete/deactivate/remove affordance is documented by the fighter's saved-pricebook walk; because creation is irreversible, product must confirm intended recovery path.
- C7 YELLOW: 1604 override data was not cleaned; export has 731 rows, so the empty grid is A12's server error, not a separate data-restore defect.
- D2 YELLOW: Testid coverage gap is confirmed by staged live DOM inventory; this is automation support, not a product bug.

Nav2 gate: `playwright-cli goto "https://navigator2.training.psav.com/#/"` was run; live eval returned `hasCorporatePricing:false`, `hasPricebook:false`, and prior old-site baseline artifact records Corporate Pricing as baseline-absent, so A10/C4/C5/C6 have no nav2 intended-behavior oracle to apply.

Completeness sweep: accessible staging/evidence showed exactly the 8 ticket items (A10,A11,A12,C4,C5,C6,C7,D2); no missing-new candidate was found in accessible artifacts.

## Finalized tracker rows

| Color | Type | # | Area | How to reproduce | What we observed | What we expected | Possible cause | Status |
|---|---|---|---|---|---|---|---|---|
| RED | Product bug | A10 | Corporate Pricing - Pricing Detail (Max Discount validation) | Open office 1604 Corporate Pricing Details for `2021-PB6`, switch to Pricing Detail, enter `333` in `Balloon Light Decor` Max Discount, then try clicking another editable cell. | Reviewer re-drive: Max Discount changed from `0.00 %` to `333`, `aria-invalid=true`, Save disabled, no validation message, and the invalid field remained active after clicking another input. | Invalid discount should either be rejected with clear validation/recovery or allow the user to move focus and correct it without a trap. | Client validation marks the field invalid but does not surface explanatory text and appears to hold focus in the invalid control. | Confirmed live 2026-07-13 in `evidence-cp-review-2026-07-13/02-A10-A11-C4-detail-repro.txt`. Nav2 baseline absent. Jira: NM-1967 is adjacent but not exact; UNCHECKED - needs human Jira confirm before logging. |
| GREEN/DROPPED | False / not reproducible | A11 | Corporate Pricing - Pricing Detail (New Price validation) | Open office 1604 Corporate Pricing Details for `2021-PB6`, switch to Pricing Detail, enter `5646464` in `Analog Mixer 12 - 23 Ch` New Price. | Reviewer re-drive: field kept `5646464`, set `aria-invalid=true`, and Save stayed disabled; no persistence or New Price shift was reproduced. | Invalid high New Price should be blocked from save. | Existing client validation is blocking the invalid high value. | DROP per LR-044 class MISREAD/not-reproduced-as-defect. Evidence: `evidence-cp-review-2026-07-13/03-A11-detail-repro.txt`. Jira: no logging needed unless a different repro is supplied. |
| RED | Product bug | A12 | Corporate Pricing - Product Group Override | Open office 1604 Product Group Override, observe grid, and fetch `GET /navigator/api/location/corporate-price-pg-override?localOfficeId=1604`; compare office 1606 and export. | Reviewer re-drive: 1604 grid body showed `0 items found` / `No results`; 1604 API returned HTTP 500 `An item with the same key has already been added. Key: 4543`; 1606 API returned 200; export returned 8,995 rows total and 731 rows for 1604. | Office 1604 override grid should load existing override rows without server error. | Server-side response builder likely throws on duplicate product-group key 4543 for office 1604. | Confirmed live 2026-07-13 in `evidence-cp-review-2026-07-13/01-A12-override-red-repro.txt`. Nav2 not applicable. Jira: NM-1961 not a match; UNCHECKED - needs human Jira confirm before logging. |
| YELLOW | Product / design question | C4 | Corporate Pricing - Pricing Detail grid structure | Open office 1604 `2021-PB6` Pricing Detail and inspect the product-group source list and detail grid. | Reviewer re-drive: grid headers were `ID, Product Group Name, Price, New Price, Max Discount`; source list had 3,707 draggable items; grid had 2,430 input rows; no page-size control; sample rows were flat product-group rows. | Confirm whether a large flat grid is intended, or whether grouping/pagination/search affordances are expected for usability. | Current React table renders the management grid as a full flat product-group table. | Confirmed behavior, not enough intent for RED. Evidence: `evidence-cp-review-2026-07-13/02-A10-A11-C4-detail-repro.txt`. Nav2 baseline absent. Jira/product: UNCHECKED - needs human confirm before logging. |
| YELLOW | Product / UX question | C5 | Corporate Pricing - New Pricebook (Price Year) | Open New Pricebook Equipment, type `20.5`, `12`, and `abcd` in Price Year; then fill Name + Year `20.5` + one strategy and click Save. | Reviewer re-drive: decimal `20.5` and short `12` remained; `abcd` reverted to prior `12`; with Year `20.5`, Save was enabled but click caused no URL change, no dialog, no validation text, and zero `/navigator/api/` calls. | Price Year should enforce the accepted year format at entry or show a clear save-time validation error. | Text input/component validation marks invalid but Save enabled state and save-click feedback are inconsistent. | Confirmed live 2026-07-13 in `evidence-cp-review-2026-07-13/04-C5-price-year-spotcheck.txt`. Nav2 baseline absent. Jira: NM-2057 lead, UNCHECKED - needs human Jira confirm before logging. |
| YELLOW | Product / UX question | C6 | Corporate Pricing - New Pricebook lifecycle | Create/save a new pricebook, then inspect Details, Search, menus, and row-level actions for delete/deactivate/remove/archive/inactivate. | Fighter staging reports saved pricebook `ZZZ-QA-DELETE-CHECK-0713` at `/details/a3cd2012-c289-43ee-9932-bdf1de901a39` and found no delete/deactivate/remove affordance; current tests also treat creation as irreversible via UI. | Product should confirm whether no UI recovery path is by design, admin-only, or missing. | Delete/deactivate may be omitted from the UI or intentionally handled outside Navigator Cloud. | YELLOW, not RED without product intent. Nav2 baseline absent. Jira: no matching delete/deactivate NM in accessible crossref; UNCHECKED - needs human Jira confirm before logging. |
| YELLOW | Data provenance question | C7 | Corporate Pricing - Product Group Override office 1604 data | Use A12 repro and compare grid/API failure with override export for office 1604. | Reviewer re-drive: export has 731 rows for location 1604; grid is empty because the 1604 API returns HTTP 500 duplicate key 4543, not because data was cleaned. | No data restore should be requested unless product finds separate missing data; fix should target the A12 server error. | Same backend duplicate-key condition as A12; export uses a different code path and still exposes 1604 rows. | Answered YELLOW; dedup to A12, no separate bug. Evidence: `evidence-cp-review-2026-07-13/01-A12-override-red-repro.txt`. Nav2 not applicable. Jira: same as A12, UNCHECKED - needs human Jira confirm before logging. |
| YELLOW | Automation support | D2 | Corporate Pricing - all screens | Inspect live DOM for `data-testid` attributes across Search, New Pricebook, Details, Pricing Strategy, Pricing Detail, and Override. | Staged live inventory: Search has 5 instances / 3 unique generic values (`e2e-card-header`, `e2e-card-title`, `e2e-checkbox` x3); New Pricebook, Details tabs, and Override have 0 field-specific testids. | Add stable field/control-specific `data-testid` attributes for automation reliability. | Corporate Pricing components mostly lack testid props beyond generic Search card elements. | YELLOW automation enablement, not product defect. Evidence: `evidence-cp-pricebook-2026-07-13/d2-results.json`; Jira N/A. |

## ASK

- contract-fix: External worker reports under `C:\Users\rutvi\.claude\delegation\reports\` were permission-denied in this session, and the named detail staging file was absent from the repo. Hypothesis: accessible staging/evidence is sufficient for the 8 named rows; ruled out: any additional accessible Corp Pricing 2026-07-13 staging file.
- contract-fix: Live Jira/Rovo was unavailable here. Rows marked UNCHECKED-JIRA need human Jira confirmation before Encore logging.
