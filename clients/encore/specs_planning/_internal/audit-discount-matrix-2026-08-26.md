---
date: 2026-08-26
auditor: WATCHDOG (fresh context — AUD-017; did not author any audited artifact)
scope: "NM-3530 Phase 3 items 1-5 — PLAN_NM3530_DISCOUNT_MATRIX_COVERAGE_QUICK.md:399-409"
temporal_anchor: "AUD-008 — full NM-3530 execution state as of activity-log 2026-08-26T05:25 (post Phase-2-audit remediation); prior phase2-audit-nm3530-2026-08-26.md findings spot-checked as remediated, not re-litigated"
browser_tool: "none — static artifact audit under a hard no-browser constraint (main session owns the suite/auth state); reads + greps + node checks only"
verdict: YELLOW
---

# Phase-3 Audit — NM-3530 Discount Matrix QUICK coverage (items 1–5)

**Overall verdict: YELLOW** — the substance is real and strong (60/60 TC parity PASS re-run by this auditor, machine denominator complete at 17/20/20 = union 35, a genuine bug harvest with machine evidence, honest loudly-recorded corrections), but the mid-day LOA populated-state correction was never propagated into the **field inventory**, leaving four falsified out-of-scope dispositions and a stack of contradicted frontmatter in the one artifact the closure gate cites; both bug JSONs carry `baselineComparison` values outside the LR-034 enum (the deny-gate's exact target class); the Add Year / Export / Import QUICK floor flagged by the Phase-2 audit (O-2) is still open; and the RWP case MD ships references to a nonexistent `TC-DSM-CMX-*` namespace.

**Gate evidence run by this auditor (not trusted from prior sessions):**
- `npm run check:tc-parity` → `PASS: All spec TCs are present in both markdown and XLSX deliverable` (1228 = 1228; no `TC-DSM` in the divergence FLAG list — flags are pre-existing CPR/LOS/LOC modules)
- `node scripts/check-per-test-baseline.mjs` → `PASS … 21 registered (3 waived, 18 enforced), 0 warnings`
- `node scripts/check-save-route-parity.mjs` → `PASS … all 2 registered save route(s)`
- ALL-091 heading-marker grep on all 3 case MDs → 0 hits; split workbooks exist (`testcases/discount-matrix/discount-matrix-{search-criteria,region-weekly-peaks,location-activation}.xlsx`)
- LR-060 state: `.claude/state/execution-completion-warnings-69bb5de1….json` exists but belongs to `PLAN_DISCOUNT_OPTIMIZATION_AUTOMATION.md`, session 2026-08-10 — foreign plan/session, no verdict floor from it.

Phase-3 items 6–7 (`check:spec-quality` on working tree; suite green ×2 consecutively) are **outside this audit's scope** and remain with the main session. Note for that session: per the Phase-2 audit's O-1 and the 05:25 activity row, green ×2 currently stands at ×1 on the final code — the second consecutive green on the final text is still owed.

---

## Item 1 — FCC completeness (§2 case sets + §2.1 oracle) — **PARTIAL**

Reference: `field-case-generation.md` §2/§2.1. Catalog: `field-case-catalogs/discount-matrix-2026-08-25.md`.

**What holds (verified against MD + spec text, not the catalog's own claims):**
- **GAV Discount Threshold (Numeric)** — the full §2 row is realized: min/mid/max (CRT-015/-010/-016), min−1 honestly documented as not-enterable with the floor proven by 0 (catalog line 26), max+1/far-OOR/decimal-step (017/018/019), all five §2 negative cells (020–025), save-cycle new-fill/edit-persist/LR-009 revert (010/011/026). §2.1 on the flagship rejection case is exemplary: `discount-matrix-criteria.spec.ts:251-256` asserts `isThresholdInvalid` + Save-disabled (announced) **and** Tab-escape via `isThresholdFocused() === false` asserted before any cleanup key (escapable).
- **Five dropdowns (Country/Currency/Business Tier/Year/Region)** — option sets verbatim, every option or both list ends exercised (CRT-002/003/004/007-009/027-030; RWP-002/003/012/015/011/016), open/dismiss-not-an-edit (CRT-006, RWP-010). The §2 DOM-tamper negative is explicitly deferred to deep in the catalog (line 54) and the CRT test plan out-of-scope table — honest, recorded.
- **Peak-flag checkbox triplet** — check/uncheck/revert/save-reload/data-rule all present (RWP-017/014/018/019/007) against the *measured* model (MCP rows 13–15); the missing §2 `toggle off→save` cell is explicitly out-of-scoped in the RWP test plan ("Server acceptance of a zero-classification week … stops before Save"), which is the correct LR-040-D anti-calcification posture while DSM-OBS-3 is open.
- **LOA date cell** — `deferred-to-DEEP: LOA date-cell editor (…persisting grid mutation; QUICK covers the affordance record only…)` in the catalog (line 83), reason ≥20 chars, no co-appearing classification token (LR-072 G1 clean).
- **LOA Active toggle** — brain-first probed (`dsm-loa-active-editor-probe.json`; MCP row 19), full edit-discard cycle authored (LOA-009, spec `location-activation.spec.ts:137-164`).

**What does not hold:**
- **Add Year / Export / Import** — the plan's own §1b table (plan :299-300) sets the QUICK floor: Add Year covered *per launcher* (dialog renders + server overlap rejection + Cancel/Esc discards for the non-mutating branch), Export asserts a **download event** (`waitForEvent('download')`). Delivered: enabled-state asserts only (RWP-009). No irreversibility-probe record exists anywhere (plan :308-318 made it BLOCKING context). The deferral exists only as agent-authored rows in the RWP test plan's out-of-scope table — the plan's `## Deferral Authorization` block is empty (correct in that nothing was self-signed, but that means the deferral is *unauthorized*). Phase-2 audit O-2 routed exactly this to Phase 3; it is still open. → **P3-F2 (HIGH — HALT-and-ask per the plan's own item-2 instruction).**
- **CRT-018 (far out-of-range)** carries only the §2.1 *announced* half (`criteria.spec.ts:261-268` asserts invalid + Save-disabled); no escapable/Tab assertion in MD or spec (sibling 017 has it; the probe measured all 15 values escaping, but the case does not assert it). → **P3-F12 (LOW).**
- **LOA search box (Plain text)** has no §2 boundary/negative set and no disposition token recording the absence — mitigated: the filter is entirely non-functional (BUG-DSM-LOA-001), so its input contract is unknowable until fixed, but that reasoning is nowhere recorded. → **P3-F13 (LOW).**
- **Template promotion skipped** for the Yes/No label-button boolean editor — plan §1b (:303) and LR-057's no-taxonomy-row path both end in "promote a new §2 template row"; `field-case-generation.md` is unmodified (git status clean on it). → **P3-F16 (LOW).**
- Stale contradicted claims inside the RWP MD (NEEDS-LIVE-CONFIRM ×2 vs the same file's MCP rows 13–15; RWP-019 "has not been run yet" vs the green runs). → **P3-F15 (LOW).**

## Item 2 — Surface completeness (§3 families vs plan §1c) — **FAIL**

All seven §3 families are *addressed* at the catalog level (`discount-matrix-2026-08-25.md` §2 Axis-2 table: result-fidelity / sorting / render-state / empty-vol / persistence covered; pagination + combination out-of-scope with ≥20-char fresh reasons). But three things fail the item:

1. **The walk artifact's per-surface §3 dispositions contradict the catalog and are falsified.** `field-inventories/discount-matrix-2026-08-25.md:215` (LOA row) still reads `out-of-scope: result-fidelity=the surface returns 0 matching locations…`, `out-of-scope: sorting=…there are no rows to order`, `out-of-scope: combination=…disabled while zero locations are loaded`, `out-of-scope: persistence=Save is disabled and no row can be edited so nothing can be persisted` — every reason cites the **rescinded** zero-locations reading (walk-evidence :78-110 "RESOLVED … the whole investigation was chasing a loading-window artifact"; TC-DSM-LOA-009 proves rows editable and Save enabling; LOA-010/-011 exist for result-fidelity/sorting). The honest LOA-persistence disposition (deep-tier deferral, shared-data mutation) exists only in the LOA test plan's out-of-scope table (:35) — never in the LR-065 disposition slot. → **P3-F5 (MED)**, riding on **P3-F1**.
2. **The io class is open** (see P3-F2 above) — under §5 class 6/8 and HARD STOP #11b(b), `covered-by-TC: TC-DSM-RWP-009` on Export/Import/Add Year is a presence/enabled-state-only row (the SILENT-OMISSION class): no BEFORE/AFTER effect delta, no download, no dialog.
3. **Plan §1c proposals diverged without a deviation record** → **P3-F9/P3-F10 (MED each)**:
   - CRT result-fidelity: plan proposed `out-of-scope` ("the criteria bar returns no row set of its own"); catalog instead claims coverage via CRT-007/8/9/27-30 — but those cases assert only the selected value plus a settle that **tolerates a no-op**: `waitForRequerySettle` (`discount-matrix.page.ts:215-218`) asserts `skeleton count == 0`, which passes instantly if no re-query ever fired. The CRT test plan itself admits the effect is asserted "only as loading-and-settling" (:33). The family's L1 must-assert ("returned rows actually match the query") is not delivered by any CRT case.
   - RWP empty-vol: plan proposed probing the `LA / AL` region (the exact NM-3293 defect region — "assert the observed state and RCA any zero-row result"); delivered RWP-005/-006 (volume + loading discrimination) and RWP-003's note explicitly says it "proves only that the option exists". The NM-3293 probe was dropped with no D-row and no disposition.
   - RWP persistence: plan proposed "`Cancel` behaviour per NM-3485 (it previously reverted *saved* values and left Save enabled)"; no RWP case ever exercises Cancel-after-dirty (RWP-010 asserts it disabled pristine; 014/017/018 restore by re-clicking). Dropped silently.

RWP §3 out-of-scope rows in the inventory (:214) are fine — pagination/sorting/result-fidelity/combination reasons are fresh and machine-anchored. LOA sorting resolved live per the plan's probe requirement (LOA-011 + BUG-DSM-LOA-002 + legacy no-sort baseline). Dirty-state guard at QUICK: delivered on all three surfaces (CRT-026, RWP-018, LOA-009).

## Item 3 — Walk completeness — **FAIL**

**What holds:** machine denominators are complete and honest — `dsm-crt-skelgate.json` `completion_record: {status:"complete", element_count:17}`, RWP/LOA both `{status:"complete", element_count:20}`, union 35 with composition 11 shared + 6 CMX + 9 RWP + 9 LOA (inventory :151-155); every one of the 35 manifest rows carries a disposition token (no blanks); the A△B review set is fully classified with a single root cause named (disabled-at-rest lens gap, :197-208), so `CrossCheck: clean` is substantiated; zero `deferred-to-DEEP` rows exist in the manifest, so LR-072 G1 has nothing to violate (the one live deferral token sits in the catalog, correctly formed); `Walk_Mode: quick` is stamped (:12) and matches the plan's `CoverageMode: quick`.

**What fails:**
- **Four falsified dispositions (P3-F1, HIGH).** Manifest rows :192-195 (`struct:input|Select date`, `struct:button|Open calendar`, `struct:button|Yes`, `struct:button|No`) are all `out-of-scope: NM-3253 reproduces this control on cloudapps-dev only; both authorized e2e offices return zero locations so it cannot be driven here — gap tracked by TC-DSM-LOA-009`. Every clause is falsified by the same day's correction: both offices carry 2041 rows (walk-evidence :78-88), the escalation was withdrawn (:103-110), the Yes/No pair **is driven** by TC-DSM-LOA-009 (spec :137-164), and Open calendar **was probed** (LOA MD MCP row 20). "Gap tracked by TC-DSM-LOA-009" is doubly wrong — LOA-009 is the *coverage*, not a gap tracker. The 100% Coverage_Ratio claim currently rides on out-of-scope text that is false. (Stale-not-fabricated: the truthful record exists loudly in walk-evidence/catalog/MD — the inventory simply never received the correction pass. This is the exact class the plan's own Phase 0.5 doctrine item names: *verdicts written from pre-fix evidence are re-derived, not inherited*.)
- **Inventory frontmatter/prose contradicts its own module record (P3-F8, MED):** `:14` "four Location Activation row controls are out-of-scope **pending the data escalation**" (withdrawn); `:14` case counts "CRT 14, RWP 14, LOA 10" (real: 30/19/11 = 60, catalog :8); `:19` `baselineScope: partial` (baseline artifact :5 now reads `complete (CRT + RWP + LOA all captured)`); the Live-state caveat row `:38` ("data-blocked on e2e … Escalated … unresolved") and the LOA blockquote `:80-85` ("Only the empty/disabled contract is assertable until the escalation returns") both still assert the rescinded reading; `:52`/`:73` field-row notes say More-information/Open-calendar rows "stay undispositioned" while the manifest dispositions both.
- **Walk-evidence frontmatter is stale in closure-relevant keys (P3-F7, MED):** `walk-evidence-discount-matrix-2026-08-25.md:8-11` still reads `Coverage_Ratio: 0/35 (0%)`, `CrossCheck: pending`, `baselineScope: partial`, and carries no `Walk_Mode` key. The plan's HUNTER Per-Identity row cites this artifact, so the LR-062 Cx closure path (and any coverage grep) can key on these values at DONE-flip and fail or floor a flip that the inventory alone would pass.
- **No opener-frontier record (P3-F11, MED):** the plan's acceptance line says "opener frontier empty", but no artifact records frontier-EMPTY or blocker rows, and the machine record contradicts "empty": `dsm-crt-skelgate.json` `stateGraphExhaustion: {containersFound: 23, neverOpened: 23}` (samples are app-shell chrome — office switcher, Home, Inbox, Actions, Commissions, Tax). In-module, three revealed states were never traversed and are nowhere named as frontier/blockers: the Add Year dialog, the Import chooser, the More-information popover (CRT-013's own note: "deliberately opened no header popovers" — a presence-only TC dispositioning an unopened opener). §20/#11b requires the artifact to *record* the frontier state either way.
- **The 15% out-of-scope cap** — 13/35 manifest rows are out-of-scope (6 CMX + 4 falsified LOA rows + 3 structural containers) vs the `coverage-manifest.mjs:623` 15% cap. **Recorded as open, owner-deferred (Phase 0.05 routes (a)/(b)/(c)) — not a new finding**, per standing instruction. Note: honest re-dispositioning under P3-F1 (Yes/No → covered-by-TC; date/calendar → deferred-to-DEEP) reduces it to 9/35 (~26%) — still over the cap, so the Phase 0.05 owner decision remains required either way.

## Item 4 — Bug-loop closure — **PARTIAL**

**What holds:** both `reports/bugs/BUG-DSM-LOA-001.json` / `-002.json` carry the full LR-034 schema — numbered concrete `stepsToReproduce` (5 steps each), `mcpEvidence` pointing at machine artifacts that exist on disk (`dsm-loa-affordance-probe.json`, `…probe2.json`), `requirementSource` citing NM-2221's own scope line, `affectedTests` naming TC-DSM-LOA-010/-011, and `baselineEvidence` citing the (now-real) baseline artifact section. Both TCs exist in MD + spec and assert the CORRECT behaviour, fixme'd inside the body so the grep shape survives. The reclassification trail is honest and on the record (filed `baseline-absent` per activity log 04:34, reclassified 05:25 after the authorized legacy clicks were spent; D-10/D-11 record both grants).

**What fails:**
- **`baselineComparison` values are outside the LR-034 enum (P3-F3, HIGH — CRITICAL-class per the standing anti-assumption scan).** LR-034 (LEARNED_RULES.md:132) and the deny-gate (`check-bug-baseline.mjs:38-43` `VALID_BASELINE_ENUM`) define exactly `regression-from-baseline | intentional-UX-change | baseline-absent | not-checked`. On disk: BUG-001 `"baseline-structural (affordance present on legacy; legacy function not driven)"` (novel value + free text in the enum field), BUG-002 `"baseline-match"` (novel value). The gate DENIES precisely this on any Edit/Write (`:111-122`), so the values reached disk through a path the gate does not inspect; no D-row records the schema deviation. The *content* is honest — the enum genuinely lacks a value for "matches baseline; the ticket text diverges from both" — but honesty-over-schema on a gated field, silently, is the wrong resolution: the legal moves were map-to-nearest-enum with the nuance in `notes`, or extend enum + rule text + gate together (LR-020) with owner sign-off.
- **Neither fixme names its bug ID (P3-F14, LOW).** `location-activation.spec.ts:102,170` carry plain-English reasons with no `BUG-DSM-LOA-…` (plan acceptance: "every skip names its bug ID"; LR-034 Step 6). Mitigation: this matches the dominant repo convention (grep of all `fixme|skip(` reasons across `clients/encore/tests/` shows plain-English reasons throughout), and forward pointers exist in the LOA test plan rows (:74-75) plus reverse pointers in each bug's `affectedTests`.

Baseline-before-filing (the Phase-0.5b axis) was already caught as Phase-2 F-2 and remediated — spot-checked, not re-litigated: `old-site-baseline/discount-matrix-2026-08-25.md:5` `baselineScope: complete`, LOA + RWP sections present with per-zone tables, and BUG-002's classification correctly flipped on the legacy evidence.

## Item 5 — Observations audit (ALL-045) — **PARTIAL**

- `walk-evidence-discount-matrix-2026-08-25.md` carries `## Observations` (:112) with a `### Bugs / Defects` bucket that is emphatically **not** rubber-stamped: DSM-OBS-1 (silent rewrite table, 15-value probe), DSM-OBS-2 (unnamed limit), DSM-OBS-3 (zero-classification week), DSM-OBS-4/5 (the two filed bugs), an NM-3238 no-repro record, and a shared-data-drift discussion item — each with evidence pointers. Credible against the walk's findings; no HARD-STOP-#3 concern.
- **The `### Suggestions / Improvements` heading is ABSENT (P3-F6, MED).** Heading census of the file (grep `^#{2,3}`) shows `## Observations` → `### Bugs / Defects` → next heading `## RWP walk` (:217). The four suggestion bullets (:201-213 — criteria testids, the retracted enumerator lead, the answered NM-3238 thread, the threshold error-message ask) sit inside the Bugs/Defects bucket after the discussion item. Content exists — the two-bucket *structure* mandated by ALL-045 does not, so any bucket-keyed audit/grep reads the Suggestions bucket as missing. (The field inventory :109-122 and the baseline artifact :124-133 both carry both buckets correctly.)

## Cross-check — MDs ↔ specs ↔ plans

- **ID sets are identical in all three layers**: MD `## TC-` headers 30/19/11, spec `test(` titles 30/19/11, plan scenario rows 30/19/11; `check:tc-parity` PASS re-run by this auditor; the two known-defect fixmes are exactly TC-DSM-LOA-010/-011 (spec :98-109, :166-183) matching the stated expectation.
- **P3-F4 (HIGH):** `discount_matrix_region_weekly_peaks_test_cases.md:46` claims criteria coverage lives at "`TC-DSM-CMX-001` … `-007` and `TC-DSM-CMX-034` … `-042`", and RWP-013's Notes (:297) repeat "`TC-DSM-CMX-001` … `-007`". **No `TC-DSM-CMX-*` exists in this repo's specs, MDs or workbook** (repo grep: the only hits are `.claude/state/collab-3343-review/**` — the parked NM-3343 Company Matrix work, an excluded namespace under this plan's Scope lock). The criteria coverage actually lives at `TC-DSM-CRT-001…-030` — the LOA MD's parallel rows (:73, :243) cite it correctly. RWP-013's Notes cell ships to the client workbook, pointing a reviewer at IDs that do not exist in their deliverable.

## Findings table

| ID | Sev | File | What | Why it matters | Suggested fix |
|---|---|---|---|---|---|
| P3-F1 | high | `field-inventories/discount-matrix-2026-08-25.md:192-195` | 4 LOA manifest rows (`Select date`, `Open calendar`, `Yes`, `No`) carry out-of-scope reasons citing the rescinded zero-locations reading + withdrawn escalation; `Yes`/`No` are actually driven by TC-DSM-LOA-009, `Open calendar` was probed (LOA MD MCP row 20) | The 35/35 Coverage_Ratio claim rides on falsified disposition text in the artifact the Cx closure gate cites; the correction pass (walk-evidence/catalog/MD) skipped the inventory — the plan's own "re-derive, don't inherit" doctrine item | Re-disposition: `Yes`/`No` → `covered-by-TC: TC-DSM-LOA-009`; `Select date` + `Open calendar` → `deferred-to-DEEP: <id> (persisting shared-data grid mutation; QUICK records the affordance only)`; sync the :14 composition note |
| P3-F2 | high | catalog :92-98 vs plan :299-318; inventory :181-183; RWP plan :34-35 | Add Year / Export / Import QUICK floor (dialog non-mutating half; `waitForEvent('download')`) undelivered; inventory dispositions all three `covered-by-TC: TC-DSM-RWP-009` (enabled-state only); no irreversibility-probe record; deferral exists only as unsigned test-plan out-of-scope rows; Phase-2 O-2 routed this here and it is still open | Presence-only rows on io/opener controls are the SILENT-OMISSION class; the plan itself says anything still open at item 2 is a HALT-and-ask, and the empty Deferral Authorization block means the deferral is unauthorized | HALT-and-ask the owner: either author the non-mutating QUICK cases (Add Year dialog renders + Cancel discards; Export download event) or obtain an owner-signed Deferral Authorization naming them; re-disposition the three inventory rows accordingly |
| P3-F3 | high | `reports/bugs/BUG-DSM-LOA-001.json:22`, `BUG-DSM-LOA-002.json:22` | `baselineComparison` = `baseline-structural (…free text…)` / `baseline-match` — outside the LR-034 enum that `check-bug-baseline.mjs:38-43` hard-denies; values reached disk via a path the PreToolUse gate does not see; no D-row records the deviation | An S1 deny-gate's exact target class shipped anyway; downstream triage/tooling keys on the enum; silent schema-fork on a gated artifact | Map to nearest legal value (`intentional-UX-change` is wrong here — propose `baseline-absent`→no; correct move: keep nuance in `notes`/`baselineEvidence` and set a legal value, OR extend enum + LR-034 text + gate in one owner-approved change per LR-020; add a D-row either way |
| P3-F4 | high | `discount_matrix_region_weekly_peaks_test_cases.md:46,297` | References to `TC-DSM-CMX-001…-007` / `-034…-042` — a namespace that exists nowhere in this repo's specs/MDs/workbook (only in the parked NM-3343 collab-review state); the real coverage is `TC-DSM-CRT-001…-030` (LOA MD cites it correctly) | RWP-013's Notes ship in the client workbook, pointing the reviewer at IDs absent from their deliverable and at the excluded Company Matrix namespace | Replace both references with `TC-DSM-CRT-001…-030`; rebuild the workbook (`planner:post-complete`); re-run `check:tc-parity` |
| P3-F5 | med | inventory :215; catalog :98; LOA plan :35 | LOA `persistence` disposition falsified at the inventory layer ("Save is disabled and no row can be edited" — disproven by LOA-009); catalog's persistence row omits LOA; the honest deep-tier deferral lives only in the LOA test plan; inventory's LOA result-fidelity/sorting/combination rows also contradict the catalog | LR-065 requires the truthful per-surface disposition on the walk artifact; a falsified out-of-scope is an undispositioned family in truth | Rewrite the inventory LOA §3 row: `behavior-cases: render-state, empty-vol, result-fidelity, sorting` + `deferred-to-DEEP`/`out-of-scope` with the corrected reasons (persistence → deep-tier shared-data deferral; combination → fresh reason) |
| P3-F6 | med | `walk-evidence-discount-matrix-2026-08-25.md:201-213` | `### Suggestions / Improvements` heading absent — suggestion bullets ride inside `### Bugs / Defects` after the discussion item | ALL-045 mandates BOTH buckets; bucket-keyed audits/greps read Suggestions as missing (an incomplete-walk signal) | Insert the `### Suggestions / Improvements` heading above the bullet run at :201 |
| P3-F7 | med | walk-evidence :8-11 | Frontmatter still `Coverage_Ratio: 0/35 (0%)`, `CrossCheck: pending`, `baselineScope: partial`, no `Walk_Mode` — contradicts the inventory (35/35 clean) and the completed baseline | The HUNTER Per-Identity row cites this artifact; Cx/coverage checks can key on these values at DONE-flip and fail or floor a flip the inventory would pass | Update the four keys to the resolved values (or annotate them as superseded-by-inventory pointers) |
| P3-F8 | med | inventory :14, :19, :38, :52, :73, :80-85 | Non-disposition staleness: "pending the data escalation" (withdrawn), case counts 14/14/10 (real 30/19/11), `baselineScope: partial` (baseline is `complete`), Live-state caveat + LOA blockquote still assert data-blocked/escalation-open, field-row notes say rows "stay undispositioned" while the manifest dispositions them | The single most-cited planning artifact self-contradicts on five load-bearing claims; the next consumer inherits a rescinded reading | One correction pass over the inventory mirroring the walk-evidence's dated-correction style |
| P3-F9 | med | catalog :92; CRT plan :33; `discount-matrix.page.ts:215-218` | CRT result-fidelity claimed via CRT-007/8/9/27-30, but the cases assert only selection value + a settle that tolerates a no-op (`toHaveCount(0)` on skeletons passes instantly if no re-query fired); plan §1c had proposed `out-of-scope` for CRT | The family's L1 must-assert (returned rows match the query) is not delivered; the catalog overclaims; the §1c divergence is unrecorded | Either assert a real delta in ONE case (skeleton-appearance transition or a captured re-query request per LR-056) or re-disposition CRT result-fidelity per the plan's own proposal; add a D-row |
| P3-F10 | med | RWP MD/plan vs plan §1c :333-334 | Two §1c proposals dropped silently: the NM-3293 `LA / AL` empty-vol probe (RWP-003 proves option-existence only; walk halted before non-Atlanta regions) and the NM-3485 RWP Cancel-revert behaviour (no case exercises Cancel-after-dirty on RWP) | The proposals targeted the module's own defect history — exactly the surface class the owner widened scope for; silent narrowing without a D-row | Author the two probes (or record explicit dispositions + D-rows): select LA/AL once and assert observed state with RCA-on-zero; one RWP dirty→Cancel→discard case |
| P3-F11 | med | `dsm-crt-skelgate.json` (`stateGraphExhaustion`); inventory (absent section) | No frontier record anywhere; machine says `containersFound: 23, neverOpened: 23` (app-shell chrome); in-module untraversed openers (Add Year dialog, Import chooser, More-information popover) not named as frontier/blocker rows | The plan's acceptance line claims "opener frontier empty"; the machine record contradicts it and §20/#11b requires the artifact to record frontier-EMPTY or named blockers | Add a frontier note to the inventory: out-of-module chrome enumerated-not-opened (named), in-module unopened openers cross-referenced to their P3-F2 dispositions |
| P3-F12 | low | CRT MD :371-384; `criteria.spec.ts:261-268` | TC-DSM-CRT-018 (far-OOR) asserts announced only — no escapable/Tab assertion | §2.1 mandates BOTH halves on every out-of-range case; the probe measured escape but the case doesn't assert it | Add the Tab-escape assert (copy 017's step 5) |
| P3-F13 | low | catalog :76-84 | LOA search box has no §2 Plain-text boundary/negative set and no token recording why | Item-1 letter violation; strongly mitigated — the filter is wholly non-functional (BUG-DSM-LOA-001), so the input contract is unknowable until fixed | Add a one-line disposition to the catalog: §2 set blocked-by-BUG-DSM-LOA-001, authored when the fix lands |
| P3-F14 | low | `location-activation.spec.ts:102,170` | Fixme reasons don't name BUG-DSM-LOA-001/-002 (plan acceptance + LR-034 Step 6) | Forward traceability from the spec is by prose only; mitigated by repo-wide convention + plan-row and `affectedTests` pointers | Append the bug ID (or NM ref) to each fixme string or an adjacent comment, if the owner wants the letter honored |
| P3-F15 | low | RWP MD :45, :189, :405 | Stale claims: NEEDS-LIVE-CONFIRM ×2 contradicted by the same file's MCP rows 13-15 + TC-017; RWP-019 "has not been run yet" post-green-runs | RWP-007's Notes cell ships; a confirmed model documented as unconfirmed misleads the next reader | Refresh the three spots to the measured model / run history |
| P3-F16 | low | `field-case-generation.md` §2 (unmodified) | Label-button boolean editor template promotion skipped (plan §1b :303; LR-057 brain-first path ends in §5 promotion) | Next module with a Yes/No label-button boolean re-derives from scratch | Append the §2 row (or an explicit decision that click-to-edit-grid-cell subsumes it) |

**Open items (not findings):** (a) 15% out-of-scope cap breach 13/35 — Phase 0.05 owner decision open, owner-deferred; honest re-dispositioning (P3-F1) lowers it to 9/35, still over the cap. (b) Phase-3 items 6–7 (working-tree `check:spec-quality`; suite green ×2 on final code) — with the main session; green ×2 stands at ×1 on the final text per O-1/activity 05:25.

**Phase-2 audit remediation spot-check (all 7 verified real, none re-litigated):** F-1 D-table now under `## Plan-Deviations log` (plan :550-562), Deferral Authorization empty; F-2 both legacy clicks spent, `baselineScope: complete`, BUG-002 flipped on legacy evidence; F-3 `SBC — ` describes present in all three specs (:32/:43/:30); F-4 `criteria.spec.ts:91,94` uses the shared `S.inpGavThreshold` constant; F-5 recorded in D-10 as deliberate; F-6 `discount-matrix.ts:13` comment now states the country-scoped proof; F-7 gate edit in D-10 + save-dialog answer recorded in walk-evidence (:352-355).
