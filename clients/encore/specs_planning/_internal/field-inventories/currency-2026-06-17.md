# Field Inventory — Currency (Location Settings)

**Module**: currency
**Client**: encore
**MCP_Session_Date**: 2026-06-17
**MCP_Session_Tool**: Playwright CLI
**MCP_Tool_Reason**: Grid walkthrough (3×4 currency grid — Radix checkboxes + Radix merchant comboboxes + read-only code cells) + 3 cross-field grid-rule probes; token-efficient deep-shadow-pierce eval; no visual/CSS assertion, no fresh-passkey need (LR-038 v2; LR-054 Table 2).
**Author_Identity**: HUNTER
**Page_URL**: https://cloudapps-e2e.encoreglobal.com/navigator/locations/1604/settings/location
**Test_Entity**: Office 1604 (Parker Palm Springs)

---

## URL(s) visited

- `https://cloudapps-e2e.encoreglobal.com/navigator/locations/1604/settings/location`

Tabs observed (Radix tablist — top-level + sub-tabs):

- Top-level: `Basic Information` (active by default), `Location Management History`
- Sub-tabs (under Basic Information): `Local Information` (active by default), **`Currency`**, `Pricing`, `Account and Address`, `Legal`, `Notes`, `Shared Setup Locations`, `Auto Add-On`

Currency activation: real Playwright `click` on `[data-testid="location-settings-sub-tab-currency"]` (raw-JS `.click()` does NOT flip `aria-selected` — Radix needs the full pointer sequence; navigation.md §B). Post-`goto` render is async — poll for the sub-tab testid before asserting. The whole grid renders inside the `<next-location-settings>` **open** shadow root (deep-pierce eval reaches it; Playwright locators pierce natively).

---

## Live-state caveat

| Field | Live (2026-06-17) | Documented default | Drift reason |
|---|---|---|---|
| USD Selected | checked | checked (`DEFAULT_CURRENCY='USD'`) | — |
| USD Is Default | checked | checked | — |
| USD Merchant | `316370 - PSAV US/USD` | `MERCHANT_DATA.usd.display` | — |
| CAD Selected | unchecked | unchecked | — |
| CAD Is Default | unchecked + **disabled** | disabled until selected | — |
| CAD Merchant | `316446 - PSAV Canada/CAD` | 1 option (`MERCHANT_DATA.canada`) | shown even while CAD unselected (merchant value is independent of Selected) |
| MXN Selected | unchecked | unchecked | — |
| MXN Is Default | unchecked + **disabled** | disabled until selected | — |
| MXN Merchant | (empty) | 0 options ("No Matches Found") | office-1604-seeded; no merchants |
| Save button | disabled (pristine) | disabled on clean load | — |

**No drift** — live matches the test-data constants exactly. Merchant option counts (USD=2 / CAD=1 / MXN=0) are **office-1604-seeded server config**, not framework-controlled. (1604 was never mutated — no Save was issued at any point this session; every rule probe was reverted in-memory and discarded via beforeunload-accept.)

---

## Field Inventory

### Currency grid (3 currencies × 4 columns) — office 1604

| Field | data-testid | Control Type | Default Value (2026-06-17) | Validation Rules | Enabled/Disabled States | Cross-field deps | Notes |
|---|---|---|---|---|---|---|---|
| USD Currency Code | (col-1 cell of USD row — no per-cell testid; row anchored by text) | read-only cell | "USD" | none | always static | none | **affordance: none** (live click-probe: 0 interactive descendants — no input/button/link/contenteditable/combobox) per LR-057 |
| CAD Currency Code | (col-1 cell of CAD row) | read-only cell | "CAD" | none | always static | none | **affordance: none** (live-probed, 0 interactive descendants) |
| MXN Currency Code | (col-1 cell of MXN row) | read-only cell | "MXN" | none | always static | none | **affordance: none** (live-probed, 0 interactive descendants) |
| USD Selected | `location-settings-checkbox-currency-USD-selected` | Radix checkbox (`button[role=checkbox][aria-checked]`) | checked | toggle valid; ≥1 currency must remain selected | always enabled | unchecking auto-disables + unchecks USD Is Default | |
| CAD Selected | `location-settings-checkbox-currency-CAD-selected` | Radix checkbox | unchecked | toggle valid | always enabled | selecting enables CAD Is Default | |
| MXN Selected | `location-settings-checkbox-currency-MXN-selected` | Radix checkbox | unchecked | toggle valid | always enabled | selecting enables MXN Is Default | |
| USD Is Default | `location-settings-checkbox-currency-USD-default` | Radix checkbox | checked | single-default mutual exclusion | enabled (USD selected) | checking clears other rows' Is Default; disabled+unchecked if USD unselected | |
| CAD Is Default | `location-settings-checkbox-currency-CAD-default` | Radix checkbox | unchecked | single-default | **disabled** (CAD unselected) | enabled only when CAD selected; checking clears USD/MXN Is Default | live: CAD-select → `disabled:true→false` |
| MXN Is Default | `location-settings-checkbox-currency-MXN-default` | Radix checkbox | unchecked | single-default | **disabled** (MXN unselected) | enabled only when MXN selected | |
| USD Merchant | `location-settings-select-currency-USD-merchant` | Radix combobox | `316370 - PSAV US/USD` | 2 options | always editable | none | options: `316370 - PSAV US/USD`, `316426 - Encore Bahamas/USD` |
| CAD Merchant | `location-settings-select-currency-CAD-merchant` | Radix combobox | `316446 - PSAV Canada/CAD` | 1 option | always editable | none | option: `316446 - PSAV Canada/CAD`; value displays even when CAD unselected |
| MXN Merchant | `location-settings-select-currency-MXN-merchant` | Radix combobox | (empty) | 0 options | always editable | none | opening shows "No Matches Found" (office-seeded; no merchants) |
| Grid table | `location-settings-table-currency` | table | 3 `tbody tr` rows | n/a | n/a | n/a | rows = USD, CAD, MXN (live-read order) |
| Save button | `location-settings-btn-save` | button | disabled (pristine) | enables on change; **disabled when 0 currencies selected** | shared left-panel Save | n/a | shared `clickSaveWithDialog('btnSaveCurrency')` |

**Column headers** (live text): `location-settings-table-currency-col-code` → "Currency Code", `-col-selected` → "Selected", `-col-is-default` → "Is Default", `-col-merchant` → "Merchant".

### Grid rules (cross-field) — live-exercised this session

| Rule | Live probe (2026-06-17) | Result |
|---|---|---|
| Cascade-enable (Selected → Is Default) | check CAD Selected | CAD Is Default `disabled:true→false`; Save enabled |
| Single-default (mutual exclusion) | check CAD Is Default (with USD default set) | USD Is Default `checked:true→false`; CAD Is Default `checked:true` |
| At-least-one-selected | uncheck USD (only selected currency) | USD Is Default auto-disabled+unchecked; **Save disabled** (preventive guard — escapable: re-select re-enables Save). Rejection-Affordance Oracle: announced via disabled control + escapable. |

---

## Labels + Section Names

- No section heading inside the Currency tabpanel — the grid (`<table data-testid="location-settings-table-currency">`) renders directly inside `[data-testid="location-settings-sub-tab-content-currency"]`.
- Column labels verbatim: "Currency Code", "Selected", "Is Default", "Merchant".
- Currency row labels verbatim (col-1): "USD", "CAD", "MXN".

---

## Save-cycle observations

**Save button behavior**:
- Default on fresh load: **disabled**.
- Enables when: any grid field changes from its server-saved state (smart diff — reverting to saved state RE-DISABLES Save; verified live by net-zero restore: re-check USD Selected + Is Default → Save returned to disabled).
- Disables when: save completes, OR form reverts to baseline, OR 0 currencies selected (validation guard).
- testid: `location-settings-btn-save` (shared left-panel Save).

**Save dialog** (shared per LR-012):
- Triggered by: Save click with ≥1 pending change.
- testid: `dlgSaveChanges` (shared); confirm = `btnSaveChangesConfirm`, cancel = `btnSaveChangesCancel`.
- Title/body verbatim (carried from existing TC-014/017 + shared dialog): "Save Changes" / "Are you sure you want to save the changes?".

**Dirty-state behavior** (LR-026):
- Reload with dirty form → native `beforeunload` fires (verified live: a dirty-form `goto` raised a `beforeunload` modal blocking navigation until `dialog-accept`). Existing TC-026 asserts this.
- The page object's `reloadAndNavigateToCurrencyTab()` registers a `dialog` auto-accept handler around `page.reload()`.

---

## Observations

### Bugs / Defects

None identified this session. All documented behaviors reproduced exactly (defaults, 3 grid rules, merchant counts, read-only code cells, Save-disable validation). No silent no-op, no 4xx/5xx surfaced during the (read-only) probes.

---

### Suggestions / Improvements

none
## Staleness signal

- **Last verified**: 2026-06-17
- **Fresh-until**: 2026-07-01
- **Stale-after**: 2026-07-17
- **Refresh triggers**: Encore release announcement; MODULE_REGISTRY.md schema change; generator spot-check disagreed with this artifact; client-flagged DOM change to the Currency tab; office-1604 merchant reseed (would change USD=2/CAD=1/MXN=0 counts).

---

## Network-request evidence

Not separately captured this session — **no Save was issued** (server-state safety; office 1604 is shared by every Location spec). Save path is the shared `clickSaveWithDialog('btnSaveCurrency')` page-object method → backend `PUT /navigator/api/location/update-properties` (the LR-056 endpoint, live-traced in `auto-addon-2026-06-11`; Currency reuses the identical shared save handler — `location-currency.page.ts:191-193`). The existing TC-022/023/024/027 round-trip persistence tests are the live proof that this save path persists Currency changes (passing spec = live proof).

---

## Known gaps

- **CAD / MXN merchant save-cycle**: data-thin on office 1604 — CAD has 1 option (no alternate to round-trip), MXN has 0 ("No Matches Found"). Not deterministically exercisable as a select-alternate→save→reload cycle on 1604. Recorded as LR-040(c) deferral (steering #2); only USD merchant (2 options) supports a round-trip, already proven by existing TC-022.
- **Merchant Currency audit column / Radix grid UX**: new-site-only surface (baseline-absent per LR-ENC-001 — see `old-site-baseline/currency-2026-06-17.md`). Currency History routed to the LM-History per-column suite, OUT of this module's scope.
