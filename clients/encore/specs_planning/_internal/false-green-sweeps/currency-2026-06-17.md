# False-Green Sweep — Currency

**Subplan**: SUBPLAN_CURRENCY_FCC.md (Phase 0.5 — pre-build WATCHDOG half)
**Identity**: WATCHDOG
**Date**: 2026-06-17
**Targets**: `clients/encore/tests/locations/location-currency.spec.ts` (25 literal `test()` + `UNSELECTED_CURRENCY_STATES` loop = **27 runtime** TC-LOC-CUR-001..027) + `clients/encore/src/pages/locations/location-currency.page.ts`
**Method**: 12-sweep grep (master §False-Green Sweep table, incl. Sweep 12 `UNPROBED-AFFORDANCE`/LR-057) + live-walk corroboration (Playwright CLI, office 1604, 2026-06-17)

## Verdict summary

**Zero confirmed FALSE-GREEN.** One **STATE-LEAK** (the LR-019 exposure — the subplan's named load-bearing gap: TC-001 inline first-test-only baseline + annotation-only `dependencyGate` chains) → routed to Phase 1.5 (HEALER RCA) + Phase 3 (BUILDER fix). All other sweeps CLEAN. Matches the Legal/Auto-Add-On sweep precedent (GREEN). HALT threshold (>3 confirmed FALSE-GREEN) NOT reached.

## 12-sweep classification

| # | Pattern | Spec hits | Page-object hits | Verdict | Evidence / reasoning |
|---|---|---|---|---|---|
| 1 | `.catch(() => {})` / `.catch(() => false)` on action | 0 | ~14 (L38,44,139,141,156,159,182,208,211,220,222,229,231,245,267) | **CLEAN** | every one is a visibility/state **probe** (`isVisible`/`isDisabled`/`getAttribute`), a dialog `waitFor({state:'hidden'})` **cleanup**, or an intentional interrupted-reload (L267 `triggerBeforeunloadAndStay`, where the reload is *expected* to be blocked by beforeunload). None swallow a load-bearing assertion. The save-cycle TCs (021-024,027) assert state **after `reloadAndNavigateToCurrencyTab()`**, so a swallowed save surfaces as a failed post-reload assertion — the catch cannot mask a false GREEN. |
| 2 | bare `page` destructure alongside custom fixture | 0 | — | **CLEAN** | every test destructures `{ locationCurrencyPage, dependencyGate }` only; no bare `page` |
| 3 | `.toBeHidden()` / `.toHaveCount(0)` on missing element | 0 | — | **CLEAN** | none present |
| 4 | `.isVisible()` / `.isEnabled()` in `if`/ternary | 0 | 4 (L202 `if(isDisabled)`, L220/229 `if(dialog.isVisible)`, L243 `if(!visible)`) | **CLEAN** | all 4 are **page-object control-flow guards** (clickSaveAndCaptureDialog / cancelCurrentDialog / confirmSaveDialog / getDialogErrorText). The *tests* assert concrete outcomes on the returned `SaveDialogType` (TC-014/017/025 `expect(dialogType).toBe('save-changes')`) — not vacuous `if`-skips that mask failure |
| 5 | `force: true` + `.catch()` | 0 | 0 | **CLEAN** | none present |
| 6 | all-negative-assertion tests | n/a | — | **CLEAN** | TC-003/004 (loop) assert positive default state (`checked=false`, `disabled=true` ARE the documented USD-vs-others defaults); persistence TCs assert positive post-reload state |
| 7 | stale `test.skip` / `test.fixme` | 0 | 0 | **CLEAN** | no skip/fixme anywhere (confirms subplan's "zero skips" — see HEALER LR-021 skip-audit) |
| 8 | `page.on()` listener on built-in `page` | 0 | 2 (L46, L265) | **CLEAN** | both are **dialog** handlers (`page.on('dialog', ...)`) properly added + `removeListener`ed in `finally` — for reload-beforeunload handling, NOT request/response save-observers. LR-056 (request-listener URL trap) does not apply; no `page.on('request')`/`waitForResponse` anywhere |
| 9 | `page.waitForTimeout` as sole sync | 0 | 0 | **CLEAN** | no fixed sleeps; page object syncs via `waitForAngularStable()` + `waitFor({state})` |
| 10 | assertions after `page.*` setup checking via `<pageObject>.*` | 0 | — | **CLEAN (surface)** — but see STATE-LEAK | setup + assertions both route through `locationCurrencyPage.*` consistently; no mixed-surface mismatch. The *baseline-source* defect lives here (next section) |
| 11 | `expect.poll()` timeout > 10s | 0 | — | **CLEAN** | only `expect.poll` is TC-005 (`{ timeout: 5_000 }`) — under 10s |
| 12 | `UNPROBED-AFFORDANCE` (LR-057) | 0 | — | **CLEAN** | read-only Currency Code cells: probed by TC-018 (`isCurrencyCodeReadOnly` counts inputs) AND live-re-probed this session (0 interactive descendants → `affordance: none`). Merchant comboboxes: probed by TC-008/009/010 (option lists) + TC-019 (accessibility). No affordance asserted-true-but-unprobed |

## STATE-LEAK finding (the load-bearing gap — subplan's named LR-019 exposure)

| Finding | Class | Evidence | Disposition |
|---|---|---|---|
| Default-state baseline lives ONLY in TC-001's body (`spec L20-30`: check USD Selected+IsDefault, set USD merchant, uncheck CAD/MXN, `clickSave`); `beforeEach` (`L9-13`) has only a nav-guard, **no `ensureDefaultState`** | **STATE-LEAK** (LR-019 exposure) | per-test retry re-runs `beforeEach` but NOT TC-001's body; serial blocks + `dependencyGate(['TC-LOC-CUR-001'])` are **annotation-only since 2026-05-08** → TC-001 no longer guarantees-runs-first. ~22 of 27 tests carry `dependencyGate(['TC-LOC-CUR-001'])` for ordering they no longer get. A net-zero-vulnerable assertion (e.g. TC-015 toggle-then-Save-enables, TC-021 select-CAD-then-persist) fails against correct app behavior on a dirty start. Same shape as the 2026-05-27 Legal incident (LR-019 graduating case) | **Phase 1.5 (HEALER)**: RCA + remediation record. **Phase 3 (BUILDER)**: add hardened `ensureDefaultState()` (Legal/Account-Address bounded-retry pattern — read → re-select → save → **reload+re-read** → throw after 3), wire into `beforeEach` after the nav-guard. TC-001's body stays unchanged (STRICT-LINE-D) — it just no longer carries sole responsibility |

## Pre-existing observations (NOT false-green; existing bodies untouchable per STRICT-LINE-D)

| Observation | Pattern | Verdict |
|---|---|---|
| TC-001 `expect(getGridRowCount()).toBe(3)` | LR-022 structural-count | **acceptable carve-out** — the 3-currency count IS the feature under test ("grid displays 3 currencies"). Existing body, not edited |
| TC-008 `toHaveLength(2)` / TC-009 `toHaveLength(1)` (merchant options) | LR-022 structural-count | **acceptable carve-out** — option count IS the feature ("USD shows 2 options"); each also asserts the specific merchant IDs are present. Existing bodies, not edited |

These are noted for completeness; they are NOT in scope to change (STRICT-LINE-D keeps TC-001..027 bodies byte-for-byte unchanged; only the shared `beforeEach` is modified for the LR-019 baseline).

## Routing

- STATE-LEAK → Phase 1.5 (HEALER `rca-currency-baseline-remediation-2026-06-17.md`) + Phase 3 (BUILDER `ensureDefaultState()` + `beforeEach` wiring).
- All CLEAN sweeps → no action.
- Zero confirmed FALSE-GREEN → no HALT; proceed to Phase 1.5.

**Constraint**: this is the *pre-build* WATCHDOG session. The Phase 5 FCC-completeness audit is a SEPARATE session (AUD-017 — no self-grading) — to be run by a fresh `audit` subagent.
