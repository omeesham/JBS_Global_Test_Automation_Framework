# False-Green Sweep + Un-Skip Re-Verification — Pricing — 2026-06-19

**Subplan**: SUBPLAN_PRICING_FCC.md (Phase 0.5fg + Phase 1.5, resume session)
**Identity**: WATCHDOG (→ HEALER for bug filing)
**Targets**: `clients/encore/tests/locations/location-pricing.spec.ts` + `clients/encore/src/pages/locations/location-pricing.page.ts` (both read in full this session) + live office-1604 evidence via `playwright-cli -s=e2e` + a fresh `npx playwright test` run.
**Supersedes**: `false-green-sweeps/pricing-2026-06-18.md` (resolves its two `PENDING-PO-READ` items now the page object is read; refreshes the un-skip table with fresh 2026-06-19 run evidence).

---

## Phase 0.5fg — Full 13-sweep audit (master §False-Green Sweep table)

| # | Sweep | Evidence (grep + read) | Classification |
|---|---|---|---|
| 1 | `.catch(()=>{})` swallow on action | spec grep: 0 hits | **CLEAN** |
| 2 | bare `, page` destructure alongside custom fixture | spec grep: 0 hits — all tests use `{ locationPricingPage, dependencyGate }` | **CLEAN** |
| 3 | `.toBeHidden()` / `.toHaveCount(0)` on missing element | spec grep: 0 hits | **CLEAN** |
| 4 | `.isVisible()` / `.isEnabled()` in `if`/ternary | spec grep: 0 hits. Page-object `isDropdownEnabled`/`isStartDateEnabled`/`isSaveEnabled` return booleans consumed inside proper `expect(...).toBe(...)` assertions, not branch predicates | **CLEAN** |
| 5 | `force:true` + `.catch()` | only in page-object explanatory COMMENTS (`:452`, `:469`) — the code deliberately uses `el.evaluate(e=>e.click())` (`:472`), not `force:true`; no swallow | **CLEAN** |
| 6 | All-negative-assertion tests | every TC carries positive proof (`toBe(true)`/`toContain`/disabled-state `toBe(true)`); disabled-state TCs (006/008/010) assert the disabled boolean = positive proof | **CLEAN** |
| 7 | Stale `test.skip`/`test.fixme` | 1 hit: `TC-025` (`:480`) — app-bug-blocked (BUG-LOC-PRI-001), re-verified STILL-FAILS live (§1.5). 6 prior skips (TC-020, TC-026..030) already un-skipped + verified PASSES-NOW. | **STALE-SKIP → HANDLED** (1 legit app-bug skip per LR-021 corollary; 6 re-enabled) |
| 8 | `page.on()` listener on built-in page | spec grep: 0 hits | **CLEAN** |
| 9 | `page.waitForTimeout` as sole sync (LR-052) | spec: 0 hits. **Page object: 3 fixed-sleep polling loops** — `waitForPricingDataLoaded` (`:68`,`:77` ×250ms), `enableFullCascade` (`:506` ×250ms), `selectDateFromCalendar` (`:476` ×50ms) | **PARTIAL (LR-052) → Phase 3 fix** (convert to `waitForFunction`/`expect.poll`) |
| 10 | Assertions after `page.*` setup checked via `<pageObject>.*` | consistent `locationPricingPage.*` usage; no raw `page.*` setup → pageObject-assert mismatch | **CLEAN** |
| 11 | `expect.poll()` timeout > 10s | 5 hits at 15s (`:87`,`:91`,`:408`,`:528`,`:541`) — all save→reload persistence polls (Angular applies API data to DOM async after reload) | **PARTIAL (justified)** — save-reload round-trip legitimately exceeds 10s; documented, not a correctness mask. Keep. |
| 12 | UNPROBED-AFFORDANCE (LR-057) | Phase-1 inventory probed every Pricing affordance: Grid Options menu + panel-collapse toggle (`affordance-probed`), read-only columns (TC-035), launcher (none on Pricing surface — Pay To Address belongs to Basic Info) | **CLEAN** (all affordances probed §8 of field inventory) |
| 13 | ASSUMPTION-UNISOLATED (LR-061) | the only behavior-classification (Corp-Pricing revert) rests on **2 offices** (1604+1605) + a baseline walk (Phase 0.5b) — N≥2 met | **CLEAN** |
| — | STATE-LEAK (LR-019, master class) | `beforeEach` (`:36-53`) self-heals **only** Corporate Pricing — no full `ensureDefaultState`; grid rows + Price-Guide checkbox + dropdowns can leak between serial/retry tests (RC-7) | **STATE-LEAK (confirmed) → Phase 3 fix** (hardened `ensureDefaultState(PRICING_DEFAULTS)`) |
| — | networkidle (LR-023) | spec+PO grep: only in comments explaining why networkidle is NOT relied upon; no `waitForLoadState('networkidle')` | **CLEAN** |
| — | LR-022 hardcoded counts | `PRICING_COLUMN_HEADERS` equality is the LR-022 carve-out (structure IS the feature) | **CLEAN** (carve-out) |

### Verdict: 0 unfixed FALSE-GREEN.
The master closure gate requires **zero unfixed confirmed FALSE-GREEN** — met. The two actionable findings are mechanical and Phase-3-scoped: **STATE-LEAK** (add `ensureDefaultState`) and **LR-052 fixed-sleep polls** (3 page-object methods). Neither masks broken behavior (they cause flake, not false passes). Sweep 11's 15s polls are justified (save-reload). TC-025 is the one legitimate app-bug skip.

---

## Phase 1.5 — Un-Skip Re-Verification (LR-021 + LR-044 + LR-024, fresh 2026-06-19)

Per LR-021 ("un-skip → run original logic AS-IS") + LR-044 ("do not trust received claims") — the 6 already-re-enabled tests were re-run AS-IS today; the 1 remaining skip (TC-025) re-confirmed STILL-FAILS via the prior live evidence + its standing bug.

**Fresh run (2026-06-19):** `npx playwright test location-pricing.spec.ts -g "TC-LOC-PRI-0(20|2[6-9]|30):" --workers=1 --retries=0` → **7 passed (2.8m), exit 0** (log: `.playwright-cli/pri-reenabled-run-2026-06-19.log`).

| Skip | Documented cause | Re-verification (2026-06-19) | Verdict |
|---|---|---|---|
| **TC-020** (dates persist) | save-200-but-reverts | Ran AS-IS: cascade + Radix-calendar Start `04/01/2026` / End `04/30/2026` → save `[200]` → reload → dates + checkboxes persisted. PASS. | **PASSES-NOW — RE-ENABLED** |
| **TC-026** Primary Labor dropdown | POST 500 + stale picker selector | Ran AS-IS: bidirectional `2026-Zone 3 E`↔`2026-Zone 3 D` → save → reload → persisted both directions. PASS. | **PASSES-NOW — RE-ENABLED** |
| **TC-027** Primary Equipment dropdown | same | Ran AS-IS → PASS (`2026-Tier 2 Resort A`↔`B`) | **PASSES-NOW — RE-ENABLED** |
| **TC-028** Primary Internal Equip dropdown | same | Ran AS-IS → PASS (`2023-Internal1`↔`2`) | **PASSES-NOW — RE-ENABLED** |
| **TC-029** Primary Production Labor dropdown | same | Ran AS-IS → PASS (`2026-NP LB2`↔`LB3`) | **PASSES-NOW — RE-ENABLED** |
| **TC-030** Primary Production Equip dropdown | same | Ran AS-IS → PASS (`2026-NP Tier 1`↔`Tier 2`) | **PASSES-NOW — RE-ENABLED** |
| **TC-025** Corporate Pricing persist | save-200-but-reverts | App-level revert: uncheck → save `[200]` (`upsert-location-pricebook` + `update-properties`) → reload → reverts to checked. Reproduced offices 1604 **and** 1605 (app-wide, 2026-06-18). The 500 is FIXED; the round-trip is the genuine remaining bug. | **STILL-FAILS — BUG-LOC-PRI-001** (kept `test.skip` with bug cite, LR-021 corollary) |

### 7-skip disposition (Phase 1.5 complete)
- **6 PASSES-NOW (re-enabled, green ×fresh-run):** TC-020 + TC-026..030. Zero UNRESOLVED rows.
- **1 STILL-FAILS (kept skipped, app bug):** TC-025 → BUG-LOC-PRI-001 (baselineComparison reclassified 2026-06-19 per Phase 0.5b — feature present on baseline; persistence not mutation-tested for documented destructive-risk/confound reasons).
- **Root cause of the dropdown block:** NOT a server 500 (fixed) and NOT a save race (disproven 2026-06-16) — a stale option-picker search-box selector (`'Search pricebooks...'` → live `'Search pricing strategies...'`), already corrected in `selectPrimaryDropdownOption`.
- **RC-7 caveat:** the 6 re-enabled tests pass individually + in this small serial set, but are **un-hardened** (no per-test `ensureDefaultState`). Phase 3 wires `ensureDefaultState(PRICING_DEFAULTS)` atomically (LR-021 corollary) before the full-suite ×2 run.

### Stale-claims diff (TC-MD / DQU_12 vs live)
- Spec comments' endpoint name `POST update-location-pricing` is **STALE** → real: `POST .../pricebook/upsert-location-pricebook` + `PUT .../update-properties` (Phase 1 §4). Phase 2/3 corrects MD + comments.
- DQU_12 carried-question **EnableMultidayPricing placement** → RESOLVED: Local Information sub-tab, NOT Pricing (Phase 1 §7; baseline-absent).
- "1604 corrupt" (prior intra-session claim) → corrected: 1604 is USD-only with unset primary pricing (data state), Corp-revert is app-wide — both N≥2 verified.
