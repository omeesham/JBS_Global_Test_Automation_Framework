---
artifact: drift-note
subplan: SUBPLAN_PARITY_W1_01_DECISIONS_AND_DRIFT_PRETRIAGE
parent: PLAN_MD_CSV_SPEC_PARITY_AND_LOCAL_OFFICE_SPLIT
date: 2026-05-26
agent: WATCHDOG
scope: file-only re-verification of parent §Findings + §B.5 against current codebase state
stalePercent: ~10% (2 minor drifts out of 13 claims) — below 30% HALT threshold
---

# Drift Check — W1-01 (2026-05-26)

Re-verification of every parent (PLAN_MD_CSV_SPEC_PARITY_AND_LOCAL_OFFICE_SPLIT) claim against current file state. Source of truth = `Read` / `Grep` output from this session (cited file:line below).

## Per-claim verdicts

| # | Parent claim | Current state | Verdict |
|---|---|---|---|
| 1 | SSL: 5 fixmes at TC-031/032/007/026/030, all BUG-LOC-SHR-001 | `location-shared-setup-locations.spec.ts:249` (TC-031), `:364` (TC-032), `:532` (TC-007), `:876` (TC-026), `:971` (TC-030) — all `test.fixme(true, ...)` citing BUG-LOC-SHR-001 family | **VERIFIED** |
| 2 | MGH: TC-006/007/019 skipped | `:83-86` TC-006 `test.skip(true, 'Office 1604 has 2900+ rows -- always multi-page')`; `:88-94` TC-007 `test.skip(true, 'Requires a location with zero history rows')`; `:211` TC-019 `test.skip('TC-LOC-MGH-019: …')` | **VERIFIED** |
| 3 | MGH: TC-013/014 assert fixed/translated headers | `:164-168` TC-013 asserts `col28='Calculate LDW on Net Amount'`; `:171-181` TC-014 asserts col37='Calculate CAC on Net Amount', col38='Terms and Conditions' | **VERIFIED — fixed-headers expectation present** |
| 4 | ECT-018: missing from spec | `grep -c TC-LOS-ECT-018 local-office-ect.spec.ts` → 0 | **VERIFIED** |
| 5 | ECT-001/010: spec implements + asserts | TC-001 at `:27` (existence/location-name check), TC-010 at `:145` (`Labor cost — non-numeric reverts silently`) | **VERIFIED — TC-010 codifies silent-revert behavior** |
| 6 | LGL-015/016/017: OMITTED in spec | `location-legal.spec.ts:173` "TC-LOC-LGL-015 OMITTED: Country cascade test requires left-panel Country selector"; `:176-178` "TC-LOC-LGL-016/017 OMITTED: Sort order assertion — MCP-verified BOTH dropdowns NOT sorted ... Logged as APP BUG" | **VERIFIED — comment-only OMITTED status (no Jira ticket cited)** |
| 7 | PRI-018/019/022: readOnly + popover validation | `location-pricing.spec.ts:304` TC-018 title `Start Date validates -- readOnly input prevents invalid date entry`; `:320` TC-019 `End Date validates -- readOnly`; `:358` TC-022 `Grid validates all rows -- missing date shows validation error` | **VERIFIED** |
| 8 | BAS-068: MD has it, spec doesn't | MD `local_office_settings_test_cases.md:291` `## TC-LOS-BAS-068: Date Offset — Return Rejects Negative Values`; `grep -c TC-LOS-BAS-068 local-office-settings.spec.ts` → 0 | **VERIFIED** |
| 9 | BAS-048: test.skip | `local-office-settings.spec.ts:773` `test.skip('TC-LOS-BAS-048: Room toggle round-trip — toggle inactive → save → reload → verify', …)` | **VERIFIED** |
| 10 | CUR-001: "3 rows, 4 column headers visible" | `location-currency.spec.ts:15` `TC-LOC-CUR-001: Navigate to Currency tab; 3 rows, 4 column headers visible` | **VERIFIED** |
| 11 | HIS-7 TC-002: bare `toBeGreaterThan(0)` column count | `clients/encore/specs/local-office/local-office-history.spec.ts:31` `expect(columnCount).toBeGreaterThan(0)`; `:52` navBtnCount `>0`; `:63` sortButtonCount `>0` | **VERIFIED — multiple bare `>0` assertions where MD says exactly 42 columns** |
| 12 | LI-EXTRA: already in MD+CSV+spec | Spec: 13 TC-LOC-LI-* tests at lines 169/215/231/243/257/269/281/314/394/408/422/436 + extras; MD has 14 entries (064/065/066/067/068/069/071/072/073/074/075/076/077/SKIP-BILLING); CSV: `grep -c` → 14 hits | **VERIFIED — already resolved post-SP00; no W2-06 walk needed** |
| 13 | smoke_seed: worktree mirror at `.claude/worktrees/loving-allen-408532/clients/encore/tests/specs/smoke/seed.spec.ts` | Actual path: `.claude/worktrees/loving-allen-408532/clients/encore/tests/seed.spec.ts` (worktree mirror exists but at `tests/`, NOT `tests/specs/smoke/`). Current tree has NO smoke/ dir or seed file. `git log --all -- "**/seed.spec.ts"` returns no commits for current-tree paths. Zero references in `src/`, `config/`, `playwright.config.ts`. | **DRIFTED — worktree path differs from parent claim by one directory level; scaffolding signal strengthened (no current refs, no git history)** |

## Additional drift — BUG-LOC-SHR-001 JSON file is missing from git tree

`navigation.md` §C Exploration Registry SSL row cites `reports/bugs/BUG-LOC-SHR-001.json` as a "Complete" findings file. The W1-01 closure validator (`scripts/validate-plan-closure.mjs` C3 check) flagged this path as missing. Verification:

- `find . -name "BUG-LOC-SHR-001*"` → 0 hits.
- `git ls-tree -r HEAD --name-only | grep SHR-001` → 0 hits.
- `git log --all -- "**/BUG-LOC-SHR-001.json"` → 0 commits.

Spec comments at `clients/encore/specs/locations/location-shared-setup-locations.spec.ts:249/364/532/876/971` continue to reference the bug ID inline (the `BUG-LOC-SHR-001` identifier itself), but the standalone JSON file at `reports/bugs/` was never created or got removed without trace. Verdict: registry entry is aspirational; inline spec citations remain authoritative for the SSL HAS-BUG-CITATION classification. W1-01's E4 classification still holds (per user 2026-05-26 authorization).

Recommended downstream cleanup (NOT in W1-01 scope): either create the JSON file via LR-034 filer protocol so navigation registry matches reality, or update navigation.md to drop the missing-file citation. Routes to `/encore-questions` for triage.

## Plan-body typo (informational)

W1-01 plan body line 102 reads:

> SSL | shared_setup_locations | 6 fixmes (current: TC-031/032/007/026/030) | …

— but lists 5 TC IDs and line 106 explicitly says "current spec state has 5 fixmes". Header label "6 fixmes" is stale carry-over from 2026-05-21 snapshot. Not blocking; flagged for transparency.

## Activity log since 2026-05-26

`agent-activity-log.md` last touched 2026-05-26T15:37. Recent rows (2026-05-26T14:20+) include: SP00 CSV sanitization (11 CSVs), per-TC reasons + cell sanitization across 11 CSVs, 11-parallel Opus subagent run. Net effect on W1-01 scope: SP00 work confirmed complete; no further drift on §Findings table introduced between 2026-05-26 morning and now.

## Stale% computation

- Claims re-verified: 13
- DRIFTED verdict: 1 (smoke_seed worktree path)
- Plan-body typo (non-claim): 1
- Stale ratio: ~7.7% (1/13) — **far below 30% HALT threshold**

## Verdict

**PASS — proceed with Phase 2+ as planned.** Drift is minor (one worktree-path off-by-one-dir + one plan-body typo). All §B.5 row claims verified. SP00's CSV augmentation (12-col schema) is still in place and the LI-EXTRA parity work is documented complete.
