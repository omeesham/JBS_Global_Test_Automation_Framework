---
Module: notes
Client: encore
baseline_Session_Date: 2026-05-11
baseline_Session_Tool: Claude in Chrome (CiC) — inherits user's live Chrome session per LR-038 v2 auth-heavy row; nav2 SSO session was active in the user's browser, no re-login needed (SP-OSB-01 finding reproduced 2026-05-11)
Author_Identity: WATCHDOG
baseline_URL: https://navigator2.training.psav.com/#/setup/locationdetail/1604
Observed_URL_After_Click: https://navigator2.training.psav.com/#/setup/locationdetail/1604 (Notes sub-tab activates in-place — no URL change; single-URL architecture per LR-ENC-001 §3)
Test_Entity: Office 1604 (Parker Palm Springs)
baselineScope: full
---

# Baseline — Notes sub-tab (old-site nav2, Location Management → Basic Information → Notes)

> Read-only observation log per LR-045 + LR-ENC-001. Specs do NOT run against this baseline; selectors do NOT transfer. This file documents **intended behavior** of the Notes feature on the legacy site for Phase 2 diff classification (regression-from-baseline / intentional-UX-change / baseline-absent).

---

## §1 — Access + identity

- **Browser tool**: Claude in Chrome (CiC). LR-038 v2 auth-heavy row applies — nav2 inherits Chrome user session at navigation.
- **Auth observed**: navigated to `https://navigator2.training.psav.com/#/setup/locationdetail/1604` → momentary redirect to `/#/login/exp/%7Csetup%7Clocationdetail%7C1604` while `<app-login>` rendered → SSO inheritance completed → returned to `/setup/locationdetail/1604` with full location detail loaded. No re-login prompt. Reproduces SP-OSB-01 §1 access verdict.
- **Page subject** (`document.title`): `Navigator Order Entry 2026.03.12.978.1` (build stamp).
- **Angular version**: 17.3.12 (`<app-root ng-version="17.3.12">`).
- **Top-level tab structure** (matches LR-ENC-001 §3 "ONE URL with embedded tabs"):
  - Tab 1: **Basic Information** (active by default on land)
  - Tab 2: **Location Management History**
- **Sub-tab navigation under Basic Information** (DOM order, all rendered as `<a class="nav-link">` inside `<ul class="nav nav-tabs">`):
  1. Local Information
  2. Currency
  3. Pricing
  4. Account And Address
  5. Legal
  6. **Notes** ← scope of this baseline
  7. Shared Setup Locations
  8. Auto Add-On
- **Notes sub-tab activation**: click `<a class="nav-link">` with `innerText="Notes"` → `<a class="nav-link active">` flips; URL does NOT change; tab pane swaps via `<tab class="tab-pane active">`.

---

## §2 — Notes sub-tab DOM inventory (live walk 2026-05-11)

**Container shape**:

```html
<tab class="tab-pane active">
  <div class="tab-content">
    <app-location-detail-notes>
      <div class="container-fluid ng-untouched ng-pristine ng-valid">
        <div class="location-detail-notes">
          <textarea
            formcontrolname="Notes"
            maxlength="1000"
            class="form-control ng-untouched ng-pristine ng-valid"
            rows="2" cols="20">
          </textarea>
        </div>
      </div>
    </app-location-detail-notes>
  </div>
</tab>
```

**Field inventory** (single field — entire Notes sub-tab is one control):

| Field | Selector (LR-ENC-001 zero-parity: no data-testid) | Control Type | Default (office 1604, 2026-05-11) | Validation | Notes |
|---|---|---|---|---|---|
| Notes textarea | `app-location-detail-notes textarea[formcontrolname="Notes"]` OR `.location-detail-notes textarea` OR `textarea[formcontrolname="Notes"]` | textarea (Angular Reactive Form `FormControlName`) | empty string (`value=""`, valueLen=0) | `maxlength="1000"` HTML attribute — **HARD limit, browser-enforced**. Beyond 1000 chars the browser will not accept further keystrokes/paste | `rows=2 cols=20` — small inline textarea; Angular `ng-untouched ng-pristine ng-valid` at baseline; no `placeholder`, no `aria-label`, no `name=` (Reactive Forms uses `formcontrolname` instead) |

**Controls NOT present on baseline** (architectural divergence from new site — see §3):

- **NO** Add button
- **NO** Delete button
- **NO** row management (no `<tbody>` with `<textarea>`-per-row; no nth-row indexing)
- **NO** character counter UI element
- **NO** progress bar
- **NO** "No Notes Available" empty-state row (because there's no table — empty state = empty textarea)
- **NO** "Add" placeholder behavior

**Save button** (NOT inside Notes section — global, top-of-page):
- Selector: `button.btn.btn-success.me-2[type="submit"]` with `innerText="Save"`. Lives in the location-detail header (not within Notes sub-tab); shared across all sub-tabs of Basic Information.
- State at baseline (Notes empty, form pristine): **disabled** (`button.disabled = true`).
- Sibling top buttons: `<button class="btn btn-success ms-4">` (no text — likely Save split-button trigger or icon Save), `<button class="btn btn-info me-4">Print</button>`.

**Form context** (parent container):
- `<div class="panel panel-default">` (Bootstrap 3 panel) wraps the entire location-detail page.
- `<div class="container-fluid">` is the Angular form root (carries `ng-pristine` / `ng-dirty` state).

---

## §3 — Architectural divergence vs new site (cloudapps-e2e.encoreglobal.com)

This is the load-bearing finding of this baseline walk. New site implementation diverges fundamentally from old-site; classification recommendations in §6 flow from these deltas.

| Aspect | OLD site (nav2 baseline, 2026-05-11) | NEW site (per locations_notes_test_cases.md, dated 2026-04-14 + MCP_VERIFICATION_LOG 2026-03-17) | Divergence kind |
|---|---|---|---|
| Surface | Single `<textarea formcontrolname="Notes">` | Multi-row table; each row has its own `<textarea name="notes.notes.{i}.note">` | **Intentional UX change** (paradigm shift: scalar → table) |
| Add control | None (textarea is always present, edit in place) | `<button>Add</button>` scoped via `[data-testid="location-settings-section-notes"]`; click creates new row | **Intentional UX change** (new affordance for multi-note) |
| Delete control | None | Conditional Delete button per row — appears when row has content OR when ≥2 rows exist | **Intentional UX change** (linked to row paradigm) |
| Max length | **1000 chars, HARD** (`maxlength="1000"` HTML attribute — browser blocks input beyond 1000) | **4000 chars, SOFT** (no HTML maxlength; counter shows overage past 4000; paste/programmatic can exceed) | **Intentional UX change** (capacity expansion + enforcement model flip from hard→soft) |
| Char counter | None | Live counter `"X/4000 (N Left)"` updates per keystroke; class `text-[11px]`; no data-testid | **Intentional UX change** (new affordance) |
| Progress bar | None | `<div role="progressbar">` fills proportionally with char usage | **Intentional UX change** (new affordance) |
| Empty state | Empty textarea (default) | "No Notes Available" empty-state row in `<tbody>` until first Add click | **Intentional UX change** (table-paradigm-specific) |
| Selectors | `formcontrolname="Notes"` (Angular Reactive Forms) — zero `data-testid` | `data-testid="location-settings-sub-tab-notes"`, `location-settings-section-notes`, `location-settings-btn-save` (left-panel shared) | **Intentional architectural shift** (selector-discipline introduction) |
| Save button location | Top of page, global (shared across all sub-tabs of Basic Information) | Left panel `[data-testid="location-settings-btn-save"]` (shared across all Location Settings sub-tabs incl. Notes) | Functionally equivalent (both shared/global), positionally different — minor UX |
| Save gating | Direct save on click (no dialog observed on baseline today; could not confirm fully — observation-only per LR-ENC-001) | Save click opens `[role="alertdialog"]` with title "Save Changes" + body "Are you sure you want to save the changes?" + Cancel/Save buttons | **Intentional UX change** (confirmation gate added on new site per LR-012) |

---

## §4 — Multi-row UX absence (deepest divergence — drives §6 classification)

Old site fundamentally does NOT support the multi-note pattern that the new-site TC file (28 TCs) is largely structured around. ~20 of the 28 TCs reference multi-row mechanics:

- TC-LOC-NTS-003 "Add second note row and verify Delete button behavior"
- TC-LOC-NTS-004 "Multi-row counter includes delimiter per row boundary"
- TC-LOC-NTS-005 "Delete a row and verify counter decreases"
- TC-LOC-NTS-014 "Add multiple rows and verify sequential textarea positions"
- TC-LOC-NTS-015 "Delete a middle row and verify remaining rows shift"
- TC-LOC-NTS-016 "Empty row has no Delete button — Delete appears on first keystroke"
- TC-LOC-NTS-017 "Delete last remaining row restores empty state"
- TC-LOC-NTS-024 "Multi-row persistence — 3 rows save+reload+verify"
- TC-LOC-NTS-026 "Partial deletion persistence — delete middle row, save, verify remaining"
- (plus 11 supporting TCs that touch multi-row mechanics indirectly)

**Classification**: every one of these is **baseline-absent** in the literal multi-row sense — the OLD site has no row paradigm at all. But this is NOT a regression — the old site predates this feature. Per LR-ENC-001: `baselineScope: baseline-absent` is allowed (NOT a HALT) when the feature is net-new on the active site. **The right classification for Phase 2 is `intentional-UX-change`**, not `regression-from-baseline`, because the new-site multi-row paradigm is the documented direction (REQUIREMENTS.md + Jira 1712).

Phase 2 (HEALER) MUST classify the entire multi-row TC cluster as `intentional-UX-change` rather than `baseline-absent` (which would imply escalation to `/encore-questions`). The new-site behavior is the canonical/desired behavior; the old-site single-textarea is the legacy pattern being replaced.

---

## §5 — Max-length enforcement flip (hard → soft)

The single concrete **regression-from-baseline candidate** worth flagging: old site enforces a 1000-char HARD limit; new site enforces a 4000-char SOFT limit (per TC-LOC-NTS-007 + TC-LOC-NTS-021). This is two flips at once:

1. **Capacity**: 1000 → 4000 chars (4× expansion).
2. **Enforcement model**: HARD (browser-blocking via `maxlength` HTML attribute) → SOFT (counter shows overage; paste/programmatic can exceed 4000).

Per LR-034 bug-filing protocol, this is **NOT a bug** — the new-site behavior is documented and intentional (capacity expansion is positive; enforcement-model flip is a deliberate product choice favoring permissive entry + user-visible counter signal over strict blocking). Phase 2 MUST classify as `intentional-UX-change`.

**However**: if Phase 2 finds the new site rejects pastes beyond 4000 chars in some browsers (i.e. soft limit isn't actually soft), that would be a regression — confirm during Phase 1a.

---

## §6 — Recommended Phase 2 baseline-diff classifications (preview)

Phase 2 Step 5 emits the `## Baseline diff` section in `notes-2026-05-11.md` (field-inventory artifact). Pre-classification recommendation, to anchor that decision:

| Divergence | Classification | Rationale |
|---|---|---|
| Multi-row table vs single textarea | `intentional-UX-change` | New product paradigm; REQUIREMENTS.md + Jira 1712 drives multi-note feature. Old site is legacy single-note. |
| Char counter UI added on new site | `intentional-UX-change` | New affordance, supports the larger 4000-char capacity. |
| Progress bar added on new site | `intentional-UX-change` | New affordance, paired with char counter. |
| Add/Delete row controls | `intentional-UX-change` | Required for multi-row paradigm. |
| "No Notes Available" empty-state row | `intentional-UX-change` | Table-paradigm-specific affordance. |
| Save dialog confirmation on new site | `intentional-UX-change` | Per LR-012 the new site introduced shared "Save Changes" alertdialog across Location Settings sub-tabs. Old site has no such dialog observed today. |
| `data-testid` selectors on new site | `intentional-UX-change` | New site introduced data-testid discipline; old site uses Angular `formcontrolname` (LR-ENC-001 zero-parity). |
| Max length 1000 (HARD) → 4000 (SOFT) | `intentional-UX-change` | Documented capacity + enforcement-model flip. Confirm pastes >4000 work on new site in Phase 1a (if blocked, escalate as regression). |
| Save button position (top vs left panel) | `intentional-UX-change` | UI redesign; both are shared-global Save behavior. |

**Net new-site features without old-site equivalent** (`baseline-absent` candidates for `/encore-questions` escalation per LR-ENC-001): none material. Notes is a pre-existing feature with a complete redesign — not net-new. All deltas are intentional UX evolution.

**Regressions from baseline**: **zero observed at baseline-walk time**. No HALT per Phase 0.5b gate (the strict "regression-from-baseline count > 5 → STOP" line is satisfied at count=0).

---

## §7 — Staleness signal

- **Last verified**: 2026-05-11
- **Fresh-until**: 2026-05-25 (14-day window per AAE-D6)
- **Stale-after**: 2026-06-10 (30-day window — HALT past this date)
- **Refresh triggers**: Encore engineering changes nav2 Notes UI; nav2 SSO auth breaks; new TCs added to `locations_notes_test_cases.md` that reference fields not covered in §2.

---

## §8 — Cross-refs

- LR-ENC-001 (Encore baseline truth source — nav2 architectural divergence preview)
- LR-045 (baseline-truth workflow — free-form artifact format)
- LR-040 (closure-gate — every divergence classified into a/b/c)
- REQ-014 (Phase 1a baseline-first walk)
- PLN-049 (`Baseline_Artifact` frontmatter linkage in field-inventory)
- OSB-ACCESS-VERIFY-2026-04-24.md (precedent — 6-section free-form artifact shape)
- `clients/encore/specs_planning/test-cases/setup/locations/locations_notes_test_cases.md` (target test-cases file, 28 TCs, dated 2026-04-14)

---

## FCC-lens divergences (added 2026-05-21, OWNER directive SUBPLAN_NOTES_FCC_PILOT, HUNTER identity)

Field type per `field-case-generation.md` §2: **Multi-row FormArray** (the only input field on Notes).
Case templates applicable: positive (1 row, N rows), BVA (empty, max=4000, max+1=4001 via paste),
negative (newlines, whitespace, HTML literal), save-cycle (add+save, edit+save with all 4 edit ops:
overwrite/append/prepend/partial-replace/clear; delete-first/middle/last+save; delete-all+save).

Display-only sub-fields (counter, progress bar, empty-state): NOT save-cycle; covered by existing TCs.

Save-cycle archetypes per ARCH-013 — applicable:
1. Dirty-state preserved across in-page tab switch (TC-010 exists)
2. Save-then-discard rollback (TC-027 exists — Cancel dialog)
3. Save-then-reload server persistence (TC-009 exists — and 12 others)
4. Navigate-away unsaved-changes alertdialog (TC-011 exists — beforeunload)
5. Sequential-save HIST row count (TC-033 exists)
6. Cross-tab save isolation (NOT covered today — FCC gap)

Cross-field archetypes per ARCH-014 — applicable: D3 (conditional visibility — Delete button) only;
D1/D2/D4/D5/D6 N/A (Notes has no in-Notes cross-field dependency).

Net-new FCC gaps vs existing coverage: see field-case-catalog (PLANNER deliverable, Phase 3).
