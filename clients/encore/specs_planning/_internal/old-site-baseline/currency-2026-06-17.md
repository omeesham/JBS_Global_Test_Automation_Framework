---
module: currency
client: encore
baselineScope: baseline-partial
baseline_url: https://navigator2.training.psav.com/#/setup/locationdetail/1604
observed_url: https://cloudapps-e2e.encoreglobal.com/navigator/locations/1604/settings/location
session_date: 2026-06-17
author_identity: HUNTER
test_entity: Office 1604 (Parker Palm Springs)
---

# Old-Site Baseline — Currency (Location Settings)

**Baseline truth source** (LR-ENC-001): `navigator2.training.psav.com` (old Navigator UI — Angular + PrimeNG + Bootstrap, `name=`/`id=`/class selectors, ZERO `data-testid`). Observation-only — no selector parity, no specs run against baseline.

## 1. Access

- e2e CLI session (`state-load clients/encore/.auth/encore-state.json`) **SSO-bridges to nav2** after a short settle: first `goto` landed on `/#/login/exp/%7Csetup%7Clocationdetail%7C1604` (the deep-link login-experience route, target preserved); after ~4-6s the page hydrated to authenticated location-detail content (no Microsoft sign-in prompt). Confirms LR-ENC-001 shared-MS-SSO bridge (same pattern as the Auto-Add-On / Account-Address baseline rows).
- Architectural divergence per LR-ENC-001 holds: old site = ONE URL `/setup/locationdetail/1604` with embedded top-level tabs (Basic Information + Location Management History) and embedded sub-tabs (Local Information, **Currency**, Pricing, Account And Address, Legal, Notes, Shared Setup Locations, Auto Add-On). The old-site Currency tab is an `<a class="nav-link">Currency</a>` (no testid).

## 2. Currency management — PRESENT on baseline

The old site **HAS a "Currency" sub-tab** under the location detail. It is NOT a net-new-on-e2e feature. Clicking it rendered a grid with the **same column structure and currencies** as the new site:

**Old-site Currency grid observed** (panel text, observation-only):

- Columns: `Selected` · `IsDefault` · `Merchant` (plus the currency-code column)
- Rows: **USD**, **CAD**, **MXN**
- USD merchant value: `316370-PSAV US/USD` (same merchant ID as the new site's `MERCHANT_DATA.usd`)

→ **Column set + currency set + USD merchant = parity** with the new-site Radix grid.

## 3. What was NOT captured (honest scope — `baseline-partial`)

- Per-row **checked-state** isolation on the old site: the AngularJS location-detail form keeps the whole Basic-Information control set mounted in the DOM alongside the Currency panel; old-site per-currency Selected/IsDefault states were not separated from the surrounding form controls. New-site live walk is the authoritative default-state source (see `field-inventories/currency-2026-06-17.md`).
- Old-site **save behavior / grid-rule enforcement** (cascade-enable, single-default, at-least-one): not driven (observation-only; new-site save-cycle + all 3 rules captured live in the field inventory).
- Reason for `partial` not `full`: column/currency/merchant parity proven; per-row state + rule enforcement NOT driven on baseline (and not needed — new-site is observed truth per ALL-024).

## 4. Baseline diff (observed-vs-baseline)

| Observation | Classification | Note |
|---|---|---|
| Currency tab present on BOTH sites with same columns (Selected, IsDefault, Merchant) + same currencies (USD/CAD/MXN) + same USD merchant (316370) | **parity** | feature carried forward; no regression, no item-set divergence |
| New-site uses Radix checkboxes (`button[role=checkbox]`) + Radix comboboxes + `data-testid` selectors; old-site uses PrimeNG/Bootstrap + `name=`/`id=`/class (zero testid) | **intentional-UX-change** (framework migration) | expected per LR-ENC-001 zero-selector-parity; not a bug |
| **Merchant Currency audit column** | **baseline-absent** (new-site only) | per LR-ENC-001 explicit baseline-absent list; record `baselineScope` partial, NOT a HALT. Currency History routed to LM-History per-column suite (out of scope here). |
| Single-default / at-least-one-selected enforcement *UX* (Save-disable guard; Radix mutual-exclusion) | **intentional-UX-change** | the single-default *concept* exists on baseline (the `IsDefault` column); the new-site enforcement (live-confirmed: Save-disable guard + auto-clear cascade) is the Radix re-implementation |

**No regression-from-baseline identified.** No `/encore-questions` escalation needed — column/currency/merchant parity holds and the new-site grid rules are an intended re-skin of the baseline single-default model.

## 5. Provenance

- Live observation only (2026-06-17). No user-fact contradicts the new-site behavior; the old-site Currency grid corroborates the intended model (multi-currency selection, single default via `IsDefault`, per-currency merchant). New-site is the automation target (zero selector parity).
