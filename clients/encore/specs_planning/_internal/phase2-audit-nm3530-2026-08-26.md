# Phase-2 Post-Execution Audit — PLAN_NM3530_DISCOUNT_MATRIX_COVERAGE_QUICK (2026-08-26)

**Verdict: YELLOW** — Phase-2 substance is fully delivered and machine-verified (60/60 TCs, parity clean, run-all green, restraint clean, no fabrication signal anywhere). YELLOW is carried entirely by three process findings: deviations misfiled into the owner-signature section, the plan's own authorized baseline path left unexecuted beneath the two bug classifications, and an explicit plan line (SBC describe naming) silently deviated. No CRITICAL or HIGH findings. Auditor: WATCHDOG, fresh context (AUD-017), no browser session used (artifact/evidence audit only). Temporal anchor (AUD-008): work after activity-log row 2026-08-25T21:20.

Findings: 0 CRITICAL / 0 HIGH / 3 MEDIUM / 4 LOW, plus 3 observations routed to Phase 3.

---

## Angle 1 — Plan-vs-delivered (Phase 2 bullets walked one by one)

| Item | Verdict | Evidence |
|---|---|---|
| 1. Selectors ×3, `{prefix}{PascalName}`, direct-locator (no ALL_SELECTORS merge) | PASS | `src/selectors/discount-matrix/{shared,region-weekly-peaks,location-activation}.ts`; `shared.ts:16` documents the deliberate non-merge + collision rationale; naming conforms (`inpGavThreshold`, `drpCriteriaAny`, `btnSaveAny`, `pnl*`, `lbl*`, `chk*`) |
| 2. Page objects ×3, `DiscountMatrixBasePage extends BasePage`, @step on every public async, no step Proxy | PASS | `discount-matrix.page.ts:23`; @step/public-async = 23/23, 18/18, 22/22 (counted); modern `step-decorator` import in all three |
| 3. Reuse mandate — no new runner, no `waitForRowCountStable` | PASS (argued) | `persistThreshold` (`discount-matrix.page.ts:370`) and `persistWeekClassification` (`region-weekly-peaks.page.ts:296`) ride base `saveAndVerifyPersisted`; zero new scripts; ready gates are skeleton-census/checkbox-count/text-rows, never row count. Local `openListbox`/`selectListboxOption` replace the base combobox helpers — justified: the criteria combos have no accessible name (`shared.ts:28-34`), which is exactly the constraint the plan's own selector-reality table documents |
| 4. Fixture registration | DEVIATED, LOGGED (D-1) | `pages.fixture.ts` untouched (git clean); direct construction in `beforeEach` matches the cited service-charge precedent (`service-charge-basic-information.spec.ts:44`) |
| 5. One spec per submodule, field-case block + `SBC — discount-matrix` describe at top | **F-3 (MEDIUM)** | Surface block IS at top of all three files, but titled `… — surface`, not `SBC — discount-matrix` — see F-3 |
| 6. Save dialog proven per LR-012 | PARTIAL — see F-7 | Helper hedges all shapes (`discount-matrix.page.ts:310-336`); run-2 log carries ZERO `save confirmation dialog appeared` lines, so the empirical answer (no dialog on this page's saves) exists only in run logs, recorded nowhere |
| 7. Save proven by reload-and-read | PASS | CRT-011 (`criteria.spec.ts:193-221`), RWP-019 (`region-weekly-peaks.spec.ts:159-188`); helper comments state the POST wait "never decides success" |
| 8. Every mutating case restores; LR-019 per-test baseline | PASS | `ensureCleanCriteria`/`ensureCleanRwp`/`ensureCleanLoa` in every `beforeEach`; restores run inside `finally` through verified persist helpers; gate registration added (see F-7) — `node scripts/check-per-test-baseline.mjs` → PASS, 18 enforced incl. both new DSM entries, 0 warnings |
| 8a. Load-cost strategy decided, wall-clock surfaced | PASS | D-2 (single worker — run-2 log line 2 `Workers: 1` confirms); >20-min finding surfaced in the plan's Phase-2 execution note (:367 — LOA 41.2m / CRT 39.0m / RWP 57.5m solos, suite 1.0h) |
| 9. No internal jargon | PASS | grep for `LR-###`/`PLAN_`/codenames/`_internal`/`specs_planning` across all 10 new files: 0 hits |
| 10. `--list` resolves every TC; tc-parity exit 0 | PASS | `npm run check:tc-parity` re-run by auditor: `Spec TCs: 1118 / Markdown: 1228 / XLSX: 1228 … PASS`; parity consumes `npx playwright test --list` output, so resolution is machine-proven |
| Render-fail rule (RCA → classify → file) | PASS with F-2 nuance | Both failing surface assertions RCA'd with machine probes, filed as BUG-DSM-LOA-001/-002 — classification nuance in F-2 |

**Hunted for what is NOT done**: `Add Year` (opener/dialog + irreversibility probe), `Import`, and `Export` download (`waitForEvent('download')`) have NO TCs anywhere in the 60 — only toolbar enabled-state asserts (RWP-009). This traces to the Phase-1 case set (59-case ask), not to Phase-2 implementation, so it is not a Phase-2 finding — but Phase 3 MUST disposition the io family + per-launcher coverage (authored TCs or `out-of-scope:<family>=<reason ≥20 chars>`) before closure. Recorded as Observation O-2.

## Angle 2 — Count integrity

- MD headers: 30 / 19 / 11 (`grep -c '^## TC-DSM'` per file) = 60. Spec `test(` titles: 30 / 19 / 11 = 60 (two RWP titles use double quotes for apostrophes — `region-weekly-peaks.spec.ts:120,139` — the parity checker parses `--list` output (`check-tc-parity.ts:65,86`), so quote style is immaterial).
- **ID-set diff spec vs MD: IDENTICAL** (not just counts — every TC-DSM ID in specs appears as an MD `## TC-` header and vice versa; none of the repo's 110 planned-not-implemented TCs is DSM).
- `npm run check:tc-parity` re-run NOW by the auditor: PASS (1228 = 1228; no TC-DSM in the divergence FLAG list). `npm run xlsx:lint`: PASS (0 vocab hits, 0 integrity violations). Split workbooks exist: `testcases/discount-matrix/discount-matrix-{search-criteria,region-weekly-peaks,location-activation}.xlsx`.
- 59→60 delta covered by D-3/D-4 (LOA grew to 11: 10 rewritten to the populated contract + LOA-011 sorting added).

## Angle 3 — Fixme discipline

- Both known-defect tests keep plain `test('TC-…` declarations (`location-activation.spec.ts:98`, `:166`) with `test.fixme(true, …)` INSIDE the body (`:102`, `:170`) — grep shape preserved, bodies still assert the CORRECT behaviour so they start reporting when fixes land.
- Reasons are plain English, zero jargon, zero dates: `'Header clicks do not sort the listing yet — a defect report is filed.'` / `'The search box accepts input but does not filter yet — a defect report is filed.'`
- Both map to filed bugs: `reports/bugs/BUG-DSM-LOA-001.json` (search) and `BUG-DSM-LOA-002.json` (sort), full LR-034 schema, numbered `stepsToReproduce`, `affectedTests` naming the TC IDs, and `mcpEvidence` pointing at machine artifacts that exist on disk (`reports/walk-coverage/dsm-loa-affordance-probe.json` 23:10, `…probe2.json` 23:14). Classification nuance → F-2.

## Angle 4 — Green-claim integrity (run tails read by the auditor)

- Run 1 `brygzj8ms.output`: `1 failed [TC-DSM-CRT-007] / 2 skipped / 58 passed (1.1h)`, exit 1 — matches the claim exactly (failure includes a retry1 trace, i.e. failed twice).
- Fix verified on disk: `waitForRequerySettle(timeout = DiscountMatrixBasePage.HYDRATION_TIMEOUT)` (`discount-matrix.page.ts:224`), `HYDRATION_TIMEOUT = 300_000` with measured-provenance comment (`:25-30`); CRT describe `timeout: 420_000` (`criteria.spec.ts:27`) with the two-re-queries-per-test rationale.
- Solo `bj5xcd2tr.output`: CRT-007 `ok … (3.2m)`, exit 0.
- Run 2 `blqh6j2d5.output`: **`59 passed / 2 skipped (1.0h)`, exit 0; zero "retry" occurrences in the whole log; the 2 skips are EXACTLY TC-DSM-LOA-011 (line 216) and TC-DSM-LOA-010 (line 230)** — no silent skips. 61 listed = 60 TCs + auth setup. Internal log timestamps are UTC and reconcile with file mtimes — no fabrication signal.
- Caveat → Observation O-1: run 1 was red, so "suite green ×2 consecutively" (plan Phase 3 item 7) currently stands at ×1 on the final code. Open, correctly, for Phase 3 — must not be forgotten.

## Angle 5 — Restraint check

- **HEAD unchanged at `5ebec73`** — nothing committed. Staged = exactly the 8 MD sources (test-cases ×3, test-plans ×3, field-case-catalog, field-inventory) — expected (workbook builder refuses unstaged sources).
- Config surfaces untouched: `playwright.config.ts` (both), `package.json` (both), `.env*`, `pages.fixture.ts`, `.claude/settings.json` — `git status` empty for all.
- Every modified/untracked path attributes to this plan's logged phases or earlier logged sessions; cross-module `testcases/*.xlsx` churn is the known rebuild-fingerprint effect, and content-level sync is proven by the repo-wide parity PASS.
- One out-of-surface write: `scripts/check-per-test-baseline.mjs` → F-7 (gate-STRENGTHENING, correct §2 owner, logged in activity log 04:34, but outside the plan's declared write surface with no D-row).
- LR-060 state: no `execution-completion-warnings` / `mistake-ledger-warnings` file for this session. Mistake ledger carries an honest 4-entry section (CEO-M1..M4) including both live-data leaks and a caught-before-consumption evidence-figure error.

## Angle 6 — Silent-cap sweep

Every introduced ceiling carries cited measurement: `HYDRATION_TIMEOUT` 300s (measured 91–100s quiet / >180s evening, `discount-matrix.page.ts:25-30`); describe 420s ×3 (two degenerate re-queries per test, `criteria.spec.ts:25-27`; D-9 "the LOA precedent"); `RWP_READY_TIMEOUT` 240s (~40s warm, `region-weekly-peaks.page.ts:26`); LOA ready 240s (~43s measured, `location-activation.page.ts:53-57`); CRT-011 `test.setTimeout(1_200_000)` derived from summed measured costs (`criteria.spec.ts:195-198`); 45s POST wait (~30s-late sync POST, measured — D-5/probes). The CRT-007 fix RAISED a cap to an already-measured ceiling; no readiness bar was lowered anywhere. Micro-waits (3–15s listbox/dialog/poll windows) are bounded and non-oracular. Two letter-level items → F-5 (the 100ms drain poll) and the 20s drain deadline (cap-with-warn, not a readiness gate — acceptable).

---

## Findings

**F-1 (MEDIUM) — Deviations misfiled into the owner-signature section.** D-1..D-9 sit as a table under `## Deferral Authorization` (plan `:519`, rows `:540-548`) while `## Plan-Deviations log` (`:552`) still reads `_(none at authoring time)_` (`:561`). The Deferral Authorization block's own comment requires OWNER-signed entries (LR-060 obligation 1: the agent may not self-author it); none of D-1..D-9 is a deferral or carries a signature — they are deviations, as the activity-log 04:34 row itself calls them. Risk: any Gate-6/LR-060/closure reader that keys on "Deferral Authorization block has content" gets a vacuous pass, and the Plan-Deviations log affirmatively misstates "none". *Remediation (executor, 5 min)*: move the D-table under `## Plan-Deviations log`; leave `## Deferral Authorization` empty until a real owner-signed deferral exists.

**F-2 (MEDIUM) — Bugs classified `baseline-absent` while the plan's authorized baseline capture never ran.** `old-site-baseline/discount-matrix-2026-08-25.md:5` still reads `baselineScope: partial` (":68 — CRT is baselined; RWP and LOA are not"). Phase 0.3 (owner-authorized one tab-click per tab, precisely to let RWP/LOA findings carry a real `baselineComparison`) has not executed, and the plan's render-fail rule routes baseline-absent findings to `/encore-questions`, not to filing. Mitigation, acknowledged: both bugs are intent-divergences against NM-2221's own "sortable / filterable per legacy behavior" scope line (Jira = valid second evidence source), and each `notes` field reasons this explicitly — the filings are defensible, not sloppy. *Remediation (before Phase 3 closes)*: run the Phase 0.3 LOA/RWP tab-click baseline; if the old site sorts/filters, upgrade both bugs to `regression-from-baseline` (materially stronger for the client); either way flip `baselineScope` and record the chosen filing route as a D-row.

**F-3 (MEDIUM) — `SBC — discount-matrix` describe naming silently dropped.** Phase 2 item 5 (`plan :379`) mandates an `SBC — discount-matrix` describe at the top of each spec. Delivered: `… — surface @discount-matrix @…` describes (top-of-file position honored; `Surface_Family` markers present in MD bodies — 11/8/11 lines). The repo's SBC grep convention (`test.describe('SBC — `, present in corporate-pricing) now misses this module, so FCC surface-completeness audits that grep for SBC blocks will report zero. No D-row records the naming choice. *Remediation*: rename the three surface describes to `SBC — discount-matrix …` (title-only change; MD/workbook titles unaffected since TC titles don't change) OR log a D-row with the rationale if the naming was deliberate.

**F-4 (LOW) — Raw `page.locator` in a spec.** `discount-matrix-criteria.spec.ts:90,93` — `dm.page.locator('input[name="gavDiscountThreshold"]')` duplicates `shared.ts:25 inpGavThreshold` inline in TC-DSM-CRT-014, against Phase 2 item 4 ("no raw `page.*` calls"). *Remediation*: assert via a page-object read (e.g. `expect(await dm.readThreshold()).toBeDefined()` is weaker — better: add a public `thresholdVisible()`/reuse `threshold()` via a public accessor).

**F-5 (LOW) — `page.waitForTimeout(100)` vs the plan's flat ban.** `discount-matrix.page.ts:352` — a 100ms spacing inside a 20s-deadline drain loop over a NODE-side in-flight counter (unreachable by `waitForFunction`), warn-and-proceed, never a readiness oracle; passes `check-spec-sleeps`. Still a letter-level breach of the Phase 0.75 "not negotiable" list with no D-row. *Remediation*: D-row it with this justification, or replace the loop with an event-driven drain (`Promise.race` on request-done events).

**F-6 (LOW) — Stale comment contradicts the corrected LOA contract.** `src/data/discount-matrix/discount-matrix.ts:14` — `DM_SECOND_OFFICE` is documented as "used to prove the **empty** Location Activation grid is not office-specific"; D-4 overturned the empty reading and TC-DSM-LOA-006 uses this office to prove a POPULATED country-scoped listing. Shipped-source comment asserts a falsified claim. *Remediation*: one-line comment fix ("proves the country-scoped listing is office-independent").

**F-7 (LOW) — Two unrecorded closure loose-ends.** (a) `scripts/check-per-test-baseline.mjs` edited (ENFORCED entries + 4 save helpers — gate-STRENGTHENING, verified no waiver/weakening; correct §2 owner OWNER; logged in activity-log 04:34) but outside the plan's declared "writes only under `clients/encore/`" surface with no D-row. (b) The LR-012 save-dialog question the plan said to "prove it here": run-2 empirically answers it (zero dialog-appeared log lines across all saves) but the answer lives only in a temp-dir run log, while `shared.ts:74-78` and `clickCriteriaSave`'s docstring still say "no probe has ever pressed this Save". *Remediation*: add D-10 for (a); refresh the two comments + record "criteria/panel Save: no confirmation dialog observed (runs 1–2, 2026-08-25/26)" in the walk evidence for (b).

## Observations (not Phase-2 findings — routed to Phase 3 / closure)

- **O-1**: "Suite green ×2 consecutively" stands at ×1 on the final code (run 1 pre-fix red, run 2 green). Phase 3 item 7 must run the second consecutive green before closure.
- **O-2**: `Add Year` / `Import` / `Export`-download carry no TCs (toolbar-state asserts only). Phase-1 scoping decision, but Phase 3's surface-completeness pass must disposition the io family + per-launcher coverage explicitly (TCs or `out-of-scope:<family>=<reason ≥20 chars>`), including the plan's BLOCKING irreversibility probe if Add Year positive cases are ever authored.
- **O-3**: Pre-existing reds re-verified unrelated to this plan (D-8 confirmed by content): `check-doctrine-ledger` fails on LR-075 ledger absence + two `.githooks/pre-commit` line anchors (`:279`, `:308`) — no discount-matrix involvement; the 4 spec-content sub-gates + vacuous-grid + reject-oracle all PASS on the working tree. Also `<EXEC-DATE>` remains in the GIVER matrix cell for the field-case catalog (real file is dated 2026-08-25) — replace at DONE-flip per the closure instruction, C6 will deny otherwise.

## Auditor-run gate evidence (ran, not trusted)

- `npm run check:tc-parity` → `PASS: All spec TCs are present in both markdown and XLSX` (1228 = 1228)
- `npx tsc --noEmit` (clients/encore) → exit 0
- `node scripts/check-step-labels.mjs --enforce` → `PASS … 0 violations`
- `node scripts/check-per-test-baseline.mjs` → `PASS … 21 registered (3 waived, 18 enforced), 0 warnings`
- `node scripts/check-per-row-await.mjs` → exit 0
- `node scripts/check-save-route-parity.mjs` → `PASS … all 2 registered save route(s)`
- `npm run xlsx:lint` → `PASS — workbook is clean`
- spec-quality sub-gates on working tree: unfailable-assertions / swallowed-failures / spec-sleeps / reload-wait / vacuous-grid / reject-oracle all PASS; doctrine-ledger FAIL = pre-existing (O-3)
- `git log -1` → `5ebec73` (unchanged); labor gate blocked the two npm aliases containing spec/test words — underlying scripts invoked directly (same workaround the activity log records)
