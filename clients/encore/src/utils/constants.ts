export class AppConstants {
  static readonly PAGE_LOAD_TIMEOUT_MS = 60000;

  static readonly ACTION_TIMEOUT_MS = 15000;

  static readonly NAVIGATION_TIMEOUT_MS = 30000;

  static readonly ELEMENT_WAIT_TIMEOUT_MS = 20000;    // Microsoft SSO pages average ~10s to load
}
