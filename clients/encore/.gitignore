# Build artifacts
node_modules/
reports/
logs/
test-results/
test-results-*/
playwright-report/
playwright/.cache/
*.tsbuildinfo

# Agent-internal artifacts kept on disk but never shipped
scripts/walks/
src/selectors/SELECTOR_CATALOG.md

# Local env overrides (never commit real env files) — env files live at client root post-2026-06-05 POM restructure
.env.local
.env.*.local
.env.server

# Internal artifacts — never ship to client
CLAUDE.md
.auth/
# Client requires NO GitHub workflows in the deliverable — never track or ship .github
.github/

# Playwright debug session artifacts (may contain live session cookies — never ship, never commit)
.playwright-cli/
.playwright-mcp/

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
