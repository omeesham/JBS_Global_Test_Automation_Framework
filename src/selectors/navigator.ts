/**
 * Navigator Cloud Application Selectors (to be discovered as needed).
 * Examples: navDashboard, navUserMenu, navLogout, global search elements.
 */
export const NavigatorSelectors = {
  // Navigation elements - to be added when specific test cases require them
} as const;
