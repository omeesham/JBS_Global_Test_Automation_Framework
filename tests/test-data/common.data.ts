/**
 * Shared test constants used across 3+ spec files.
 * MNT-009: values used identically in multiple specs belong here, not redefined per spec.
 */

/** Standard test office — The Parker Palm Springs (ALL-013). */
export const OFFICE_NO = '1604';
